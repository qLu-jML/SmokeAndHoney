[2026-04-03 CDT] WP2: Scene-wide feeder system -- feed_stores separation
  scripts/core/HiveSimulation.gd:
  - Added feed_stores field (consumed before honey_stores daily)
  - Removed feeder NU from _get_zone_nu() to prevent double-counting
  - Stores security check now includes feed_stores
  scripts/world/barrel_feeder.gd:
  - Changed daily injection from honey_stores to feed_stores (not sellable)
  - Added notify_placed() for quest event emission
  - Added try_refill() -- player uses sugar syrup on empty feeder to refill
  - Empty feeder notification via NotificationManager
  - Prompt shows "[E] Refill / Pick Up" when empty
  scripts/core/player.gd:
  - Feeder placement calls notify_placed() for quest system
  - Empty feeder interaction: refill if player has sugar syrup, else pick up

[2026-04-03 CDT] WP1: Quest engine with Year 1 chain and event hooks
  scripts/quests/quest_defs.gd (new):
  - 5 Year 1 quests: First Light, Sugar Water Days, Girls Are Building,
    First Pull, Battening Down -- each auto-starts the next on completion
  - Static data class with start_conditions (month gating) and chain ordering
  scripts/autoloads/QuestManager.gd:
  - Added notify_event() bus for game systems to report actions
  - Year 1 chain quests auto-start on day 1, chain forward on completion
  - Deferred start for month-gated quests (checked each day_advanced)
  - Compound winter-ready check for Battening Down (60+ lbs honey + mite wash)
  - Preserved existing daily quests and complete_objective() system
  scripts/ui/InspectionOverlay.gd, hive_management.gd, harvest_yard.gd, honey_house.gd:
  - Added QuestManager.notify_event() calls for inspection_opened, super_added,
    harvest_complete

[2026-04-03 CDT] Fix settings menu layout -- Mute checkbox overlapped by Back button
  scripts/ui/PauseMenu.gd:
  - Increased SETTINGS_H from 120 to 134 so the Mute Music checkbox and
    Back button no longer overlap; Back button repositions automatically
    via SETTINGS_H - 22

[2026-04-03 CDT] Fix PauseMenu _exit_tree() wrong variable names
  scripts/ui/PauseMenu.gd:
  - _exit_tree() referenced nonexistent vars: _slider_music, _slider_sfx,
    _slider_master, _check_mute -- fixed to _music_slider, _sfx_slider,
    _master_slider, _mute_checkbox
  - Promoted local master slider var (ms) to member var (_master_slider)
    so _exit_tree and _on_settings can reference it directly

[2026-04-03 CDT] Fall harvest start: 2D/2S hive, full honey, inventory rework
  scripts/core/HiveSimulation.gd -- init_as_fall_harvest() expanded:
  - Now creates 2 deep boxes (bottom: brood nest, top: mostly honey stores)
    plus 2 supers filled with S_CAPPED_HONEY
  - Honey stores bumped to 32 lbs, pollen to 220 for 2-deep colony
  - Fixed integer division (i / grid_cols -> int(float(i) / float(grid_cols)))
  scripts/world/home_property.gd -- Dev bypass updated:
  - Inventory: gloves, bucket grip, comb scraper only (3 slots)
  - new_game_mode=1, current_day=113 (fall start)
  scripts/core/player.gd -- Starting inventory + chest:
  - Player gets: gloves, bucket grip, comb scraper
  - Chest gets: axe, hammer, hive tool, 5 deep bodies, 5 supers, 20 frames,
    1 queen excluder

[2026-04-03 CDT] Rearrange starting inventory: tools in inventory, hive parts in chest
  scripts/core/player.gd:
  - Player inventory: axe, gloves, hammer, hive tool, bucket grip (5 tools)
  - _stock_starting_chest() now adds 5 deep bodies, 5 supers, 20 frames,
    1 queen excluder to the storage chest on fresh games
  - Added chest_stocked guard to prevent re-stocking on scene changes
  scripts/world/home_property.gd:
  - Dev bypass inventory updated to match (5 tools, no hive parts)
  scripts/autoloads/GameData.gd:
  - Added chest_stocked flag

[2026-04-03 CDT] Expand starting inventory with hive components
  scripts/core/player.gd + scripts/world/home_property.gd:
  - Added to starting inventory: 5 deep bodies, 20 frames, 1 honey bucket, 1 queen excluder
  - Updated both player._ready() defaults and home_property dev bypass to match
  - Existing items unchanged (axe, gloves, hammer, hive tool, 5 super boxes)

[2026-04-03 CDT] Fix honey super stacking gap (v3) + overlay z-index
  scripts/world/hive.gd -- Two fixes for super stacking visual:
  - Frame overlay z_index lowered from Z_BOXES+1 to Z_BOXES so that later-added
    boxes (supers) naturally draw over earlier overlays (deep frames). Previously
    the deep's frame overlay at z=6 bled through supers at z=5, causing a visible
    artifact/gap between the deep and first super.
  - BOX_CAVITY remains 11.0 (correct for the 22-row cavity shared by all boxes)
  - Added temporary debug prints for stacking position verification

[2026-04-03 CDT] Fix honey super stacking gap (v2)
  scripts/world/hive.gd -- Box-on-box visual stacking overlap fix:
  - Added BOX_CAVITY constant (11.0 display px = 22 source rows * 0.5 scale)
  - Both deep and super open-top sprites share the same cavity structure:
    2 bright rim rows + 20 dark interior rows = 22 source rows
  - Stacking loop tracks prev_cavity; each box overlaps into the cavity of the
    box below it, hiding the dark interior gap
  - v1 used SUPER_CAVITY=6.5 (based on 41-28 sprite size diff) which was too
    small -- left 4.5px of dark cavity visible. v2 uses the actual 22-row cavity
  - Verified math for 1D/3S and 2D/3S: all supers get 9.5px visible front face,
    deeps get 19px visible front face, lid unchanged at 12px overlap

[2026-04-02 CDT] DevPlan Updated with Quest Expansion Changes
  Smoke_and_Honey_DevPlan.html -- Phase 3 quest/NPC sections updated:
  - Task 3.2 (Bob): updated from 8 quests to 12 (8 core + 4 farewell arc)
  - Task 3.5 (Remaining NPC quests): rewritten with all expanded chains and 97 total quest count
  - Task 3.11 (Dr. Harwick): updated to milestone-based veterinary chain (10 quests by hive count)
  - Task 3.12 (Darlene): expanded to 10-quest mentorship chain with all quest names
  - Task 3.13 (June): expanded to 10-quest community connection chain with all quest names
  - Task 3.14: renamed Walt & Kacey Harmon to Paul & Kacey Rowland, added 6-quest chain description
  - New NPC entries added: Carl Tanner (3.14b, 10 quests), Rose Delacroix (3.14c, 10 quests),
    Silas Crenshaw (3.14d, 4 quests), Lloyd Petersen (3.14e, 4 quests), Agnes Pfeiffer (3.14f, 3 quests)
  - All "Harmon" references updated to "Rowland" (challenge events, apiary locations)
  - Last updated note revised with April 2 quest expansion summary

[2026-04-02 CDT] Quest Summaries Added to Story Bible + GDD Quest Chains Expanded
  story_bible.html -- Part VI (Quest Chains -- Complete Reference) updated:
  - Every quest in the game now has an individual summary (1-2 sentences each)
  - Bob: 12 quests (Q1-Q8 core + Q9-Q12 farewell) all with summaries
  - Darlene: expanded from 6 to 10 quests (added Q7-Q10: Story, Third Generation, Last Stand, Cedar Bend Beekeeper)
  - Dr. Harwick: expanded from 5 to 10 milestone-based quests (M1-M10, triggered by hive count)
  - Carl: expanded from 4 to 10 quests (added Q5-Q10: Custom Order, Broken Extractor, Bee Package Day, Seasonal Display, Old Supply Ledger, Honey Corner)
  - Rose: expanded from 4 to 10 quests (added Q5-Q10: Short-Handed Morning, Wildflower Order, Beeswax Candles, Honey Pie, County Food Festival, Honey Wall)
  - June: expanded from 3 to 10 quests (added Q4-Q10: Lost Order, Rural Route 4, Sticky Letter, Swarm in Mailbox, Special Delivery, Garden, Annual Report)
  - Frank (5), Rowlands (6), CVBA (4), Silas (4), Lloyd (4): summaries added, counts unchanged
  - Sidebar navigation updated with new quest counts
  Smoke_and_Honey_GDD.html (675,996 -> 698,092 bytes) -- Section 8.4 updated:
  - Overview paragraph and Quest Chain Overview table updated with new counts
  - Darlene, Dr. Harwick, Carl, Rose, June chains expanded with full GDD-style entries
  - Dr. Harwick restructured from event-triggered to milestone-based (hive count triggers)
  - Design notes updated for Rose, June, and Carl to reflect expanded chains
  - Quest Interconnection Map updated for June's new quest structure
  Total quest count: 97 quests across all NPC chains

[2026-04-02 CDT] Asset Audit Cross-Reference + Shadow/Lighting Systems Approved
  Smoke_and_Honey_GDD.html (666,855 -> 675,996 bytes) --
  Shadow System (S10.1.5) added to GDD - APPROVED:
  - 12 shadow assets: character blob, hive shadows (Langstroth + Warre),
    building shadow atlas (14 buildings), 5 tree shadow types (round, wide,
    orchard, weeping, bare/winter), prop shadow atlas (8 generic shapes),
    fence line shadow, interior object shadows
  - Implementation: CanvasLayer below sprites, NW light source, SE shadows
  - Shadow color: deep warm brown #1A0E06 at 25-35% opacity (never pure black)
  Lighting System (S10.1.6) added to GDD - APPROVED:
  - 16 lighting assets: 4 seasonal palette reference cards, 7-period
    time-of-day color tint reference, 5 point light sprites (window glow,
    lamp post, player lantern, fireplace flicker, diner neon sign),
    candle/lantern small, 7 interior ambient overlays, window daylight
    spill (4 variants), moonlight tint, overcast tint, golden hour rays,
    smoker smoke glow, bee swarm glow, snow reflection boost
  - Time-of-day system: 7 periods from dawn (#FFE8C0 15%) to night (#4060A0 35%)
  - Seasonal palettes: spring/summer/fall/winter base tone shifts
  - Interior lighting: baked ambient layers per scene, window daylight spill
  Asset Audit cross-referenced from Five Cats Studios report:
  - 67 gap assets identified and approved (missing from registry)
  - 28 shadow/lighting assets approved (new visual systems)
  - Registry totals: 203 existing + 67 gap + 28 shadow/light = 298 total
  - Phase breakdown: P1: 76 (was 61), P2: 104 (was 72), P3: 76 (was 40), P4: 42 (was 30)
  Smoke_and_Honey_Art_Assets.xlsx updated with 4 new audit sheets:
  - "Audit - Gap Assets (67)": all 67 missing assets with phase/priority
  - "Audit - Shadows (12)": shadow system asset table
  - "Audit - Lighting (16)": lighting system asset table
  - "Audit - Combined Totals": 23-category breakdown with running totals
  GDD table of contents updated with S10.1.5 and S10.1.6 entries
  Shadow system tech spec row updated to reference new section

[2026-04-02 CDT] Master Art Asset Spreadsheet Created
  Smoke_and_Honey_Art_Assets.xlsx (NEW) --
  5-sheet Excel workbook as master art asset reference:
  - "Existing Assets": 957 assets catalogued with category, path, GDD section,
    lighting/shadow status
  - "Required - Not Created": 53 required assets from GDD/Story Bible not yet
    in project, prioritized by phase (Phase 1-4)
  - "Phase Summary": Overview of 4-phase art production pipeline
  - "Art Style Reference": All GDD visual specifications in one table
  - "Website Cross-Reference": Placeholder for Five Cats Studios asset audit
    cross-reference (requires mounting Five Cats project folder)
  NOTE: SmokeAndHoney_AssetAudit_Report.html from Five Cats Studios project
  is not accessible from this session. Mount that project folder to complete
  the cross-reference and verify 4-phase art table with lighting/shadows.

[2026-04-02 CDT] Story Bible Created + GDD Cohesiveness Pass
  story_bible.html (NEW, root directory) --
  Standalone narrative reference document restructured from Complete Story:
  - All name changes applied: Whitfield->Baker, Marshalltown->Albion,
    Walmart->ShopMart, Harmon->Rowland, Walt->Paul, Dorothy->Gloria
  - Ellen Harwick: PhD from University of Iowa (not Iowa State), from Iowa City
  - Rose Delacroix: culinary school in Winston-Salem NC (not Des Moines),
    engaged to man from Marion
  - June Wellman: mother married man from Waterloo
  - Darlene taught Mae Wellman to pickle beets
  - Paul Rowland: intimidation/toilet story, secret kindness
  - Ellen/Kacey Iowa State vs Iowa rivalry added
  - Bandit the Cat added (Bob's gray tabby)
  - Agnes Pfeiffer 3-quest chain (Study Break, Ride to Waterloo, Application)
  - Pastor Jim Henning memorial scene
  - Mead-making winter crafting chain confirmed
  - Player Journal confirmed
  - Family visits confirmed (3 visit events across Years 2-4)
  - Part VIII restructured: Confirmed Additions vs Things to Consider
  - Part IX added: Things to Consider (rival beekeeper, winter content, etc.)
  Smoke_and_Honey_GDD.html (654,221 -> 666,855 bytes) --
  Cohesiveness pass syncing GDD with Story Bible:
  - All name changes applied throughout GDD (batch Python replacements,
    verified 0 remaining stale references)
  - Agnes Pfeiffer NPC added to NPC table with full quest chain summary
  - Agnes 3-quest chain added to quest section (Study Break, Ride to
    Waterloo, The Application: +150 XP, +50 Standing)
  - Bandit the Cat added to NPC table and "After Bob" section
  - Pastor Jim Henning added to NPC table (memorial scene only)
  - Paul Rowland NPC entry expanded: intimidation/toilet story, 12% speech,
    connections to Bob pesticide argument
  - Ellen Harwick NPC entry expanded: Iowa PhD, Iowa/Iowa State rivalry with
    Kacey, sentinel hive program, memorial appearance
  - Player Journal section added (7.x) with year-by-year tone table
  - Mead narrative arc design note added (Bob teaches Y2, Long Table Y3,
    Frank's market Y4+, Rose's toddy menu)
  - Family Visits section added to hospice arc (3 visits: parents Y2,
    sibling Y3, family holiday Y4)
  - Quest economy updated: 66->69 quests, 6,125->6,275 XP, 2,210->2,260
    Standing

[2026-04-02 CDT] GDD Major Update: Story Pass Content + Age-Agnostic Revisions
  Smoke_and_Honey_GDD.html (631,628 -> 654,221 bytes) --
  Age-agnostic revisions:
  - Removed all "twelve-year-old", "twelve years old", "Age 12-17" references
    from both GDD and Complete Story HTML
  - Player character profile no longer specifies age; works for any character
  - Bob's "at twelve" reference removed; Eli's age mentions replaced with
    age-neutral language throughout
  Uncle Bob profile overhaul:
  - Changed "Late 50s" to "Early 60s" with full Vietnam veteran backstory
  - Added Agent Orange / liver failure cause of death (was cardiac event)
  - Added wife name "Sandy" throughout
  - Added "What Cedar Bend knows about Bob" community knowledge block
  - Updated Q7 dialogue: "My liver says maybe not even that" (was heart)
  - Updated mom's phone call: "His liver" (was heart trouble)
  New quest content added to GDD:
  - Bob Farewell Quest Chain (Q9-Q12): The Workshop Key, The Deed and the
    Lawyer, The Rounds, The Last Inspection (475 XP, 195 Standing)
  - Telling the Bees quest with three-beat foreshadowing design notes
    (100 XP, 50 Standing)
  - 4 Holiday Quests: Quickening Gift, Beam Day Booth, Fire Keeper,
    A Place at the Table (250 XP, 120 Standing)
  - 4 Community Quests: Cribbage Lesson, Casserole Circuit, County Fair
    Day, Bob's Memorial Service (225 XP, 120 Standing)
  Quest economy updated:
  - Total quests: 53 -> 66
  - Total XP: 5,075 -> 6,125 (26% of Master Beekeeper requirement)
  - Total Standing: 1,725 -> 2,210
  - Quest chain overview table expanded with new quest categories
  NPC profiles updated with relationship web connections:
  - Bob, Darlene, Rose, Lloyd, Carl, Frank, Silas, June all have
    documented cross-NPC connections and memorial roles
  New section 9b: The Hospice Arc
  - Bob's health decline by year (Year 1-5 progression table)
  - Death scene design (peaceful, off-screen, hospice philosophy)
  - "After Bob" section: inheritance, Sandy's smoker, permanent hive,
    the chair that's never moved, the final inspection beat
  - Design note on Nathan's real uncle and hospice chaplain background
  research/design/Smoke_and_Honey_Complete_Story.html --
  - All age-specific references removed (twelve-year-old, Age 12-17,
    Sixteen years old, etc.) replaced with age-neutral language
  Smoke_and_Honey_DevPlan.html -- Verified unchanged (140,938 bytes)

[2026-04-02 CDT] Story Pass: Add "Telling the Bees" tradition to hospice arc
  research/design/Smoke_and_Honey_Complete_Story.html --
  - Added "Telling the Bees" as emotional climax of Bob's death sequence:
    player knocks on every hive and tells the bees their keeper has died
  - Three foreshadowing beats planted across Years 1-3:
    Year 1: Darlene mentions "old Polish superstition" during Q1
    Year 2: Bob confesses on porch he told bees when Helen died
    Year 3: Bob's letter (delivered by June) contains explicit instruction
  - Darlene's backstory updated: she told her bees when Teddy died
  - "Telling the Bees" quest added to community quest chain (required,
    triggers immediately after death, unlocks Memorial Service)
  - Real beekeeping tradition dating back centuries; perfect thematic fit

[2026-04-02 CDT] Major Story Pass: Complete Narrative Design Document
  research/design/Smoke_and_Honey_Complete_Story.html -- Full-length narrative
  design report in S&H visual style covering:
  - Uncle Bob redesigned as Vietnam veteran with liver failure (Agent Orange)
    as homage to Nathan's real uncle Bob
  - Player character made creator-agnostic (character creator at start,
    all references use "the player" not fixed "Eli" name)
  - All 11 NPCs deepened with backstories, cross-relationships, and
    Midwestern cultural detail (casserole economy, gossip networks, etc.)
  - Relationship web: 10 documented NPC-to-NPC connections (Bob/Lloyd
    mushroom walks, Rose/Darlene pie rivalry, Carl/Frank cribbage nights,
    Silas's wordless bench for Bob, Rose's 8-year Tuesday pecan pie ritual)
  - Small-town drama arcs: Harmon pesticide rift, post office closure
    fight, diner's quiet financial struggles, Lloyd vs county road
  - Hospice questline (Bob Q9-Q12): The Workshop Key, The Deed and the
    Lawyer, The Rounds, The Last Inspection -- peaceful death after Year 5
  - 4 holiday quests woven into story (Quickening Gift, Beam Day Booth,
    Fire Keeper, A Place at the Table)
  - 4 community quests (Cribbage Lesson, Casserole Circuit, County Fair
    Day, Bob's Memorial Service)
  - Year-by-year story arc (Years 1-5+) with emotional progression
  - 7 suggestions for added depth: Family Visits, Rival Beekeeper, Agnes
    the Waitress, Church/Spiritual, Mead Crafting, Bob's Dog, Player Journal
  Protected files verified unchanged (631628 + 140938 bytes).

[2026-04-02 CDT] Add 4 seasonal holidays (Dark Tower tone, original names)
  TimeManager.gd -- HOLIDAYS constant array with 4 entries:
    The Quickening Morn (Greening 12, Spring) -- Easter/renewal analog
    Founder's Beam (High-Sun 19, Summer) -- Independence Day/heritage analog
    The Reaping Fire (Full-Earth 7, Fall) -- Halloween/bonfire night analog
    The Long Table (Deepcold 21, Winter) -- Christmas/solstice analog
  TimeManager.gd -- holiday_started signal, get_todays_holiday(),
    get_holiday_name(), is_holiday() helpers; wired into advance_day()
  hud.gd -- HUD date label appends "-- Holiday Name --" on holiday days
  hud.gd -- Daily summary shows golden holiday banner when applicable
  GDD S5.1 -- new "Seasonal Holidays" subsection with full table and
    design note; TOC updated with anchor link
  Names carry the tone of Dark Tower mythic Americana without directly
  lifting series terminology.

[2026-04-02 CDT] GDD: Update Carl Tanner and June Wellman character descriptions
  Carl Tanner -- updated to Black man, added detail about being one of the
  first Black-owned businesses in Millhaven County (grandfather opened 1952).
  June Wellman -- changed from mid-60s to late-20s Asian American woman who
  inherited the post office from her grandmother Mae Wellman (31-year veteran).
  Updated all quest dialogue to reflect younger age and family legacy angle.

[2026-04-02 CDT] GDD: Add 5 new NPCs with full profiles and quest chains
  Section 9 NPC table -- added Carl Tanner (Feed & Supply Owner), Rose
  Delacroix (Diner Owner), June Wellman (Postmaster), Lloyd Petersen
  (Woodlot Manager) with backstories, descriptions, and relationship arcs.
  Section 8.4 Quest Chains -- added 4 new quest chain sections:
    Carl Tanner: 4 quests (supply relationship, wholesale account, retail shelf)
    Rose Delacroix: 4 quests (honey menu, pie case, potluck community arc)
    June Wellman: 3 quests (mail-order, post office preservation, Bob's letter)
    Lloyd Petersen: 4 quests (timber walk, linden season, co-stewardship)
  Quest Chain Overview table updated (53 total quests, 5,075 XP, 1,725 Standing).
  Quest Interconnection Map -- 9 new cross-NPC dependencies added.
  Quest Economy totals recalculated to include new chains.

[2026-04-02 CDT] Document: game is text-only, no voice acting
  GDD S10.2 Sound Design -- added Voice Acting row clarifying all dialogue
  is text-only by design. No voice acting in the base game.
  DevPlan Kickstarter section -- added Stretch Goals row with three tiers:
  $15k OST mastering, $20k additional apiary location, $25k voice acting
  for key NPC dialogue (Uncle Bob, Darlene, main quest scenes).

[2026-04-02 CDT] Fix hive lid + add base board layer
  hive.gd -- lid now overlaps 12px downward into the top box so the dark
  open-cavity area is hidden beneath it (LID_OVERLAP constant, z_index +2
  so lid draws above box and frame overlays)
  hive.gd -- added _tex_base (hive_base.png, 54x26) as a layer between
  stand and first deep box in BODY_ADDED, FRAMES_PARTIAL, and COMPLETE
  build states; lid reverted to hive_lid.png

[2026-04-02 CDT] Queen excluder is now data-only (no visual sprite in hive stack)
  hive.gd -- removed _tex_excluder layer from _rebuild_sprite_stack()
  - Excluder on/off status already shown in inspection UI info line and button

[2026-04-02 CDT] Fix hive roof not showing after loading a save
  SaveManager.gd -- hive visual state now saved and restored:
  - _collect_hives() now saves build_state, frame_count, has_lid,
    colony_installed, colony_install_day alongside simulation data
  - _apply_hives() restores these fields before rebuilding the sprite stack
  - Calls activate_simulation() after restore so sprite stack, labels,
    bee swarm cloud, and buzz audio all reinitialize correctly
  - _collect_sim() / _apply_sim() now include has_excluder field
  - Backward compatible: old saves without these fields default to
    COMPLETE state with colony installed (matches existing hive behavior)

[2026-04-02 CDT] Forage tier system: calibrated for 2-hive full production at B-tier
  ForageManager.gd -- scene-wide forage level with hive-count scaling:
  - Replaced local 5x5 tile sampling (get_forage_at) with scene-wide totals
    (get_total_zone_nectar/pollen) so indicator reflects full map resources
  - Forage level divided by colony count (HiveManager.hive_count()) so adding
    a second hive correctly shows reduced per-hive resource availability
  - Removed old _get_tree_forage() helper; tree iteration done inline
  - Normalization ceiling calibrated at 700.0 via full production chain sim:
    B-tier 2 hives peak = Abundant (0.86) = full honey production
    S-tier 2 hives peak = Abundant (1.00) = max build speed + overflowing honey
    F-tier 2 hives peak = Dearth (0.16)   = hives starving
  FlowerLifecycleManager.gd -- season ranking enabled:
  - RANK_WEIGHTS now symmetric bell curve centered on B:
    S=10%, A=20%, B=35%, C=20%, D=10%, F=5%
  - Enabled random season ranking roll (was hardcoded S for dev tuning)
  - NU_SCALE reverted to 250, AREA_SCALE=4 for expanded grass area

[2026-04-02 CDT] Add jarring table sprite, replace bottling table placeholder
  - New Leonardo art: assets/sprites/objects/jarring_table.png (96x72, transparent bg)
  - _create_bottling_sprite() loads and parents sprite under BottlingTable marker
  - Removed _draw_station_box placeholder for bottling table
  - Editor _draw() now shows faint outlines for all 4 stations

[2026-04-02 CDT] Ungroup harvest yard stations for independent editor positioning
  - Added 5 child marker nodes under HarvestYard in home_property.tscn:
    SuperPallet, ScrapedPallet, Extractor, BottlingTable, HoneyBucket
  - Refactored harvest_yard.gd: STATION_POS and BUCKET_OFFSET are now
    runtime vars populated from child node positions (was hardcoded consts)
  - Each station can now be dragged independently in the Godot editor
  - Falls back to original positions if marker nodes are missing
  - All gameplay logic (collisions, interactions, minigames) unchanged

[2026-04-02 CDT] Spread TileMap flower tiles across full grass area
  - TileMap layer 3 (flowers_forage) expanded from 151 tiles to 588 tiles
  - Flower tiles now cover entire grass map (tile x -23..67, y -12..43)
  - Even quadrant distribution: NW 22%, NE 28%, SW 27%, SE 22%
  - Avoids paths, buildings, harvest yard, and hive area
  - Clover dominant (326), aster (143), coneflower (119)

[2026-04-02 CDT] Spread trees and flowers across full property, scale PU/NU
  Trees expanded from 10 to 44 across the full grass area (~2900x1800 px):
  - 34 new SeasonalTree instances placed as editor-draggable scene nodes
  - Species mix: willow, silver_maple, wild_plum, apple, cherry, pear,
    basswood_linden, cottonwood, sycamore, elm_dead
  - All trees avoid dirt paths (y 50-186), buildings, and gameplay zones
  Per-tree PU/NU values divided by 4 (TREE_FORAGE_DATA in seasonal_tree.gd)
  so aggregate forage stays the same despite 4x more trees.
  FlowerLifecycleManager expanded to cover full property:
  - GRASS_SIZE: 1600x900 -> 2960x1824 (from GRASS_ORIGIN -752,-416)
  - NU_SCALE: 250 -> 940 (3.76x to match area increase)
  - Flowers now dynamically grow/spread across all grass, not just yard
  - No-spawn mask still blocks paths and buildings automatically
  Forage balance verified: B-rank season supports 1-2 hives (same as before).

[2026-04-02 CDT] Hive stacking rewrite, walk-behind z-ordering, inventory fix
  Sprite stacking rewrite:
  - Removed hive_base from sprite stack (bottom_deep includes entrance)
  - Stand (24x12) on ground at y=0, first box offset 4px forward so
    the stand landing board peeks out below the box front face
  - All boxes/supers/lid stack flush (no overlap, no gap) from bottom up
  - No overhanging: all 48px-wide boxes align; 54px lid overhangs naturally
  Walk-behind z-ordering:
  - Stand sprite at z_index=0 (ground level, participates in y-sort)
  - All box/lid/frame-overlay sprites at z_index=5 (absolute) so they
    render above the player when walking behind (north of) the hive
  - StaticBody2D collision covers only the stand footprint (28x16 at
    ground level) so the player is blocked from overlapping the stand
    but can walk behind the boxes
  - Area2D interaction zone still covers full hive height for click detection
  Starting inventory fix:
  - Player starts with 5 empty super boxes (ITEM_SUPER_BOX) in inventory,
    not 5 full supers (ITEM_FULL_SUPER) -- no longer spawns carrying a super
  - Updated both player.gd fallback and home_property.gd dev inventory

[2026-04-02 CDT] New hive sprites, 1-3 deep stacking, super frame overlays, limit changes
  Hive sprite overhaul using Nathan's new art assets:
  - New bottom_deep_empty_draft.png (48x60) for the bottom deep box (has hive
    entrance); top_and_middle_deep_empty_draft.png (48x60) for upper deeps
  - New hive_base.png (54x26) and hive_lid.png (54x26) replace old 24px sprites
  - New super_empty_draft.png (48x41) used for all supers with frame overlays
  - Frame overlay sprites (one_frame through ten_frames, 44x20 each) now show
    inside super cavities based on honey fill percentage from simulation snapshot
  - Overlays update each tick without full sprite stack rebuild
  Deep box stacking (1-3 deeps):
  - HiveSimulation.add_deep() limit raised from 2 to 3
  - Bottom deep always uses bottom_deep sprite (shows hive entrance)
  - Upper deeps (positions 2-3) use top_and_middle sprite (no entrance)
  - hive.gd stacking logic picks correct sprite based on deep_idx
  Super limit reduced to 5:
  - HiveSimulation.add_super() limit lowered from 10 to 5
  - Hive management UI updated: "Deeps: N/3  Supers: N/5"
  - Prompt text updated for new max values
  All sprites scaled at 0.5 (DRAFT_SCALE) to match ~24-27px display width.
  Overlap math updated for new bottom_deep/top_mid_deep textures.

[2026-04-02 CDT] Clean up stale pallet sprite and fix starting inventory
  - Removed old pallet_super.png.jpg and its .import/.ctex from project root
    (leftover from sprite replacement, was ghosting in the scene editor)
  - Fixed starting inventory not applying: player.gd _ready() fired before
    home_property.gd could set GameData.player_inventory_valid, so the player
    always used its own fallback (axe, hammer, 40 logs). Updated the fallback
    in player.gd directly to: axe, gloves, hammer, hive tool, 5 full supers.
    home_property.gd dev bypass kept in sync as a secondary path.

[2026-04-02 CDT] Fix county road tree sprites -- use full trees, not tileset fragments
  The cainos_trees asset pack contains both standalone full-tree sprites and
  tileset fragment pieces (canopy corners, trunk sections). 16 of 30 trees
  on the county road were using fragment sprites instead of complete trees:
  - Maple (5 trees) and Pine (5 trees) were using cainos_tree_01 (43x52
    canopy blob with no trunk)
  - Elm (3 trees) and Hickory (3 trees) were using cainos_tree_07 (76x128
    partial trunk/canopy fragment)
  Reassigned to proper standalone sprites:
  - Maple, Elm -> cainos_tree_14 (92x123, full broadleaf tree)
  - Pine, Hickory -> cainos_tree_11 (55x106, full narrower-profile tree)
  Oak (tree_13), Walnut (tree_12), and Ash (tree_06) were already correct.

[2026-04-02 CDT] Full scene conversion: all dynamic objects to editor nodes
  Converted all remaining dynamically-created objects across every scene to
  editor-placed .tscn nodes so they are visible and draggable in the Godot
  editor without touching scripts:
  Exterior ZoneExits (4 scenes):
  - community_garden.tscn: ExitToCedarBend (left edge, 30x270)
  - timber_creek.tscn: ExitToCountyRoad (bottom edge, 960x30)
  - fairgrounds.tscn: ExitToCedarBend (top edge, 1500x30)
  - harmon_farm.tscn: ExitToCountyRoad (top edge, 1500x30)
  Interior walls/collisions:
  - house_interior.tscn: Background Sprite2D, Walls StaticBody2D (5 shapes),
    BedPrompt Label -- all previously created in code via _build_*() methods
  - honey_house.tscn: Walls StaticBody2D with 5 wall collision shapes +
    6 station collision shapes (SuperPrep, FrameHolder, Uncapping, Spinner,
    CanningTable, HoneyShelf)
  Script fallback guards added:
  - community_garden.gd, timber_creek.gd, fairgrounds.gd, harmon_farm.gd:
    _setup_exits() returns early if editor-placed ZoneExit node found
  - house_interior.gd: skips _build_background(), _build_walls(),
    _build_prompt_label() when editor nodes exist
  - honey_house.gd: skips _build_walls() when editor Walls node exists
  Interior scenes (diner, post_office, feed_supply) verified: walls and
  furniture already editor-placed; only UI overlays are dynamic (correct).

[2026-04-02 CDT] Editor-placed exit/door zones + dev mode visibility
  Converted all dynamic scene transitions to editor-placed nodes so they
  are visible and draggable in the Godot editor:
  home_property.tscn:
  - ExitToCountyRoad (ZoneExit) on right edge of World
  - FarmhouseDoor (DoorZone) on Farmhouse building
  - HoneyHouseDoor (DoorZone) on HoneyHouse building
  cedar_bend.tscn:
  - ExitToCountyRoad (ZoneExit) on left edge
  - ExitToGarden (ZoneExit) on right edge
  - ExitToFairgrounds (ZoneExit) on bottom edge
  - DoorZone on CrossroadsDiner, FeedSupply, PostOffice, GrangeHall
  Scripts updated:
  - zone_exit.gd: Added runtime collision debug overlay (z_index=100)
    using collision_debug_draw.gd, toggles with G key dev mode
  - door_zone.gd: Same debug overlay treatment
  - home_property.gd: Skips dynamic creation when editor nodes exist
  - cedar_bend.gd: Same fallback check

[2026-04-02 CDT] Trim tree sprite root flare + collision debug overlay
  Tree sprites: Trimmed root/ground artifacts from the bottom of all 40
  seasonal tree sprites (10 species x 4 seasons). The wide root flare,
  exposed roots, and grassy mound pixels at the base of each tree caused
  the player to render behind ground-level elements when walking nearby.
  Backups saved to research/archive/tree_sprites_pretrim/.
  Collision debug: Created scripts/debug/collision_debug_draw.gd -- a
  shared overlay script that draws green collision outlines at z_index=100
  (absolute) so they always render on top of all game objects. Applied to
  both seasonal_tree.gd and harvest_yard.gd, replacing the old _draw()
  approach that rendered debug at the node's own z_index (hidden behind
  other objects).

[2026-04-02 CDT] Fix tree y-sorting and collision debug
  Problem: Player rendered in front of trees instead of behind them,
  and tree collision shapes were not visible in dev mode.
  Root cause: FarmsteadTrees parent node lacked y_sort_enabled, so all
  10 trees were treated as a single unit for depth sorting.
  Fixes:
  - home_property.tscn: Added y_sort_enabled=true to FarmsteadTrees
    so each tree individually y-sorts against the player
  - seasonal_tree.gd: Added _draw() collision debug that shows green
    trunk collision rectangle when dev mode is active (G key)
  - seasonal_tree.gd: Connected GameData.dev_labels_toggled signal
    with proper cleanup in _exit_tree()

[2026-04-02 CDT] Fix y-sorting and collision debug in harvest yard
  Problem: Player could walk "under" station assets instead of "behind"
  them, and collision shapes were not visible in dev mode (G key).
  Root cause: HarvestYard node had z_index=2 which overrode y-sorting,
  and station sprites used z_index=3-4 instead of y_sort_origin.
  Fixes:
  - home_property.tscn: Replaced z_index=2 with y_sort_enabled=true on
    HarvestYard node so its children flatten into World's y-sort
  - harvest_yard.gd: Replaced z_index on pallet sprites, extractor, and
    bucket with y_sort_origin set to bottom edge of each station
  - harvest_yard.gd: Added _draw_collision_debug() that draws green
    collision outlines over all stations when dev mode is active
  - harvest_yard.gd: Connected GameData.dev_labels_toggled signal to
    queue_redraw so collision debug updates on G key toggle
  - GameData.gd: Added get_tree().debug_collisions_hint toggle to show
    Godot's built-in collision shapes (walls, tilemap physics, etc.)

[2026-04-02 CDT] Add @tool to world scripts so objects render in editor
  Made 6 scripts @tool so their sprites, collision, and labels are visible
  in the Godot editor for drag-and-drop layout:
  - seasonal_tree.gd: shows spring texture, collision shape, rebuilds on
    tree_type change in Inspector
  - harvest_yard.gd: shows pallet sprites, extractor, bucket, station
    collision boxes and labels
  - chest.gd: shows chest sprite, interaction area, and storage label
  - nap_bench.gd: shows bench sprite, collision body
  - barrel_feeder.gd: shows feeder sprite
  - workbench_world.gd (NEW): @tool script for workbench table visuals,
    attached to Workbench node in home_property.tscn
  All game logic (groups, signals, _process, _input) guarded behind
  Engine.is_editor_hint() checks so they only run at runtime.
  home_property.gd _init_workbench simplified to just set up interaction
  hint since visuals are now handled by workbench_world.gd.

[2026-04-02 CDT] Move trees, workbench, harvest yard from script to scene editor
  Converted all 10 farmstead trees from _spawn_trees() in home_property.gd
  to SeasonalTree instance nodes in home_property.tscn. Each tree is now
  a draggable node in the Godot editor for visual placement and collision
  checking. Converted HarvestYard from scripted spawn to editor-placed
  Node2D with harvest_yard.gd script attached. Converted Workbench from
  _spawn_workbench() to editor-placed Node2D (visuals still built by
  _init_workbench()). Removed _spawn_trees(), _spawn_harvest_yard(),
  _spawn_workbench(), and TREE_SCENE/WORKBENCH_POS constants. Map marker
  registration now reads positions from editor-placed nodes dynamically.

[2026-04-02 CDT] Placeholder item sprites and ITEM_SPRITE_MAP completion
  Generated 9 placeholder 32x32 RGBA sprites for items missing icons:
  swarm_trap, scraped_super, barrel_feeder, logs, lumber, axe, hammer,
  bee_suit, propolis. Each has a colored background with 2-letter label.
  Added smoker (already had sprite file) + all 9 new items to
  ITEM_SPRITE_MAP in both hud.gd and chest_storage.gd. All 39 GameData
  ITEM_ constants now have sprite mappings.

[2026-04-02 CDT] Workbench UI layout cleanup
  Fixed overlapping Craft buttons by zeroing custom_minimum_size before
  Godot's layout engine inflates them, then setting size via set_deferred.
  Widened panel (304x178), increased row height (22px), spread columns out,
  centered title and convert button, added divider line between convert
  section and recipe table, added subtle alternating row backgrounds.
  Columns: Recipe / Cost / Makes / Craft now properly spaced with no overlap.

[2026-04-02 CDT] Full codebase documentation and cleanup pass (all 81 scripts)
  Ran godot-forge analysis on all 81 GDScript files and addressed findings.
  Changes applied across every script (no functionality removed or altered):

  1. DOC COMMENTS: Added ## doc-comments to 300+ functions across all scripts
     explaining purpose, parameters, and non-obvious logic.

  2. TYPE ANNOTATIONS: Added explicit type annotations to untyped variables
     throughout the codebase (var x = ... -> var x: Type = ...). Converted
     generic Array declarations to typed arrays where appropriate
     (Array -> Array[Dictionary], Array[String], Array[Node], etc.).

  3. SIGNAL DISCONNECTS: Added _exit_tree() with proper signal disconnects to
     38+ scripts that had .connect() calls without cleanup. This prevents
     errors when nodes are freed but signal sources persist (common during
     scene transitions). Affected scripts across all directories:
     - UI: hud, InspectionOverlay, PauseMenu, MainMenu, map_overlay,
       hive_management, sell_screen, shop_screen, workbench_ui,
       scraping_minigame, bottling_minigame, chopping_minigame,
       extractor_minigame
     - World: harvest_yard, hive, honey_house, home_property, cedar_bend,
       feed_supply_interior, diner_interior, flower_lifecycle_manager,
       dandelion_spawner, weather_particles, house_interior, post_office_interior,
       weather_overlay, timber_creek, door_zone, zone_exit, harmon_farm,
       community_garden, fairgrounds, barrel_feeder, harvestable_tree,
       seasonal_tree, county_road, exit_helper
     - Autoloads: WeatherManager, MusicManager, NotificationManager, HiveManager
     - Core: player

  4. ASCII COMPLIANCE: Verified all 81 scripts contain only ASCII (0x00-0x7F).
     Fixed one non-ASCII character in wildflower_scatter.gd (plus-minus sign).

  5. HEADER COMMENTS: Expanded file-level documentation on key scripts
     (HiveSimulation.gd, CellStateTransition.gd, etc.) explaining architecture,
     dependencies, and inner class structure.

  No functionality was deleted or changed. All protected files verified intact.
  Files changed: All 81 .gd files under scripts/

[2026-04-02 CDT] Simplified starting inventory to axe + hammer + 40 logs
  Player now starts with only 3 items: axe, hammer, and a stack of 40 logs.
  Removed all other starting inventory items (hive tool, gloves, excluder,
  super boxes, comb scraper, bucket grip, jars). Storage chest starts empty
  instead of pre-stocked with equipment. Increased log max stack from 20 to
  40 so 40 logs fit in a single inventory slot. Player must craft/buy/earn
  all beekeeping equipment from scratch.
  Files changed: scripts/core/player.gd (starting inventory, stack size, chest)

[2026-04-02 CDT] Rebuilt workbench UI as clickable mouse-driven interface
  Replaced keyboard-only crafting menu (UP/DOWN/ENTER/C) with clickable buttons.
  Each recipe row now has a "Craft" button; "Convert Logs -> Lumber" is a button
  with energy cost hint. Close button (X) in top-right corner plus ESC still works.
  Buttons auto-disable when requirements are not met (insufficient lumber, energy,
  or missing hammer). Improved error messages to be more specific (shows amounts
  needed vs amounts had). Fixed potential stale-reference issue by using centralized
  _get_player() helper. Added is_instance_valid check on feedback timer callback.
  Files changed: scripts/ui/workbench_ui.gd (full rewrite)

[2026-04-01 CDT] FIX: Toolbar/inventory non-functional on County Road
  Root cause: player.gd referenced the HUD via relative path "../../UI" which
  only resolved correctly in home_property (where player is under World/ node).
  On county_road the player is a direct child of root, so the path went outside
  the scene tree and returned null -- silently breaking slot selection, inventory
  display, and the daily summary overlay.
  Fix: Replaced all three get_node_or_null("../../UI") calls with the global HUD
  autoload singleton, which is always accessible regardless of scene hierarchy.
  Files changed: scripts/core/player.gd (lines 158, 180, 821)

[2026-04-01 CDT] FIX: HUD/toolbar disappearing after chopping minigame
  Root cause: HUD was a per-scene CanvasLayer node ("UI") manually placed in
  main.tscn and home_property.tscn but NOT in county_road.tscn. When transitioning
  scenes, the HUD node was destroyed with the old scene. The chopping minigame
  (also a CanvasLayer) could also overlap and obscure it.
  Fix: Converted HUD to an autoload singleton registered in project.godot.
  As an autoload, it persists across all scene transitions and is always present.
  Set HUD layer=10 so it renders above minigame overlays (layer 1).
  Removed per-scene "UI" CanvasLayer nodes and their children from main.tscn
  and home_property.tscn to avoid duplicate HUD instances.
  Files changed: scripts/ui/hud.gd, project.godot, scenes/main.tscn,
    scenes/home_property.tscn

[2026-04-01 CDT] Added 30 harvestable trees as scene nodes to county road
  Trees are physical Node2D objects under a "Trees" parent node -- each can be
  individually dragged in the Godot editor. Each tree uses a pixel art texture
  (cainos pixel art pack only, no generated art) via @export tree_texture on
  harvestable_tree.gd. Removed programmatic oak_tree.png and pine_tree.png refs.
  The script is now @tool so trees show their textures in the editor preview.
  Species: Oak, Maple, Ash, Walnut, Elm, Hickory, Pine, Birch -- scattered
  across the forest zone (y=-1050 to 220, x=-1400 to 1400) with 80px min spacing.
  Re-added chopping interaction to county_road.gd using get_nodes_in_group().
  Files changed: scripts/world/harvestable_tree.gd, scripts/world/county_road.gd,
    scenes/world/county_road.tscn

[2026-04-01 CDT] FIX: Exit bounce-back loop on scene transition
  Player was spawning inside exit collision zones and immediately re-triggering
  the transition back. Two fixes:
  1) All exits (ZoneExit + dynamic) now disable monitoring for 0.5s after scene
     load, giving the player time to exist near an exit without triggering it.
  2) Spawn offset bumped from 150 to 200px away from the exit.
  Files changed: scripts/world/zone_exit.gd, scripts/world/exit_helper.gd

[2026-04-01 CDT] FIX: Player now spawns near the exit they arrived from
  Rewrote ExitHelper.position_player_from_spawn_side() to find the actual exit
  node leading back to the source scene and spawn the player 150px inward from
  it. Both zone_exit.gd and ExitHelper.create_exit() now store source_scene in
  TimeManager metadata during transitions. The function searches all Area2D
  children for matching target_scene, then offsets the player away from the exit
  so they don't re-trigger it. Falls back to bounds-edge positioning if no
  matching exit is found. Works for all scene transitions universally.
  Files changed: scripts/world/exit_helper.gd, scripts/world/zone_exit.gd

[2026-04-01 CDT] FIX: Green debug box visible at runtime + invisible wall blocking exit
  1) zone_exit.gd _draw() now only renders in editor (Engine.is_editor_hint check).
     Debug rectangles no longer appear during gameplay.
  2) Removed SceneBounds StaticBody2D from county_road.tscn -- its LeftWall at
     x=-1600 was blocking the player from reaching ExitToHome at the same position.
     Scene boundary walls are unnecessary when exits handle the edges.
  Files changed: scripts/world/zone_exit.gd, scenes/world/county_road.tscn

[2026-04-01 CDT] FIX: Camera limits now auto-adapt to every scene
  ExitHelper.position_player_from_spawn_side() now resolves bounds 3 ways:
  1) Explicit bounds arg (e.g. county_road passes SCENE_BOUNDS)
  2) Scene script's SCENE_BOUNDS constant (auto-detected via reflection)
  3) If neither exists, removes all camera limits so it follows freely
  This fixes camera not following player in home_property and any other scene
  that doesn't define explicit bounds. No per-scene changes needed.
  Files changed: scripts/world/exit_helper.gd

[2026-04-01 CDT] FIX: Player spawning offscreen on scene transitions
  The player's Camera2D had hardcoded limits (-1200 to 3000) from an old scene
  layout. When entering county_road (bounds -1600 to 1600), the camera couldn't
  scroll to the left spawn point. Fix: ExitHelper.position_player_from_spawn_side()
  now dynamically sets camera limits to match each scene's bounds on every
  transition. Also bumped spawn margin from 80 to 120 for breathing room.
  Files changed: scripts/world/exit_helper.gd

[2026-04-01 CDT] FIX: CountryRoad instant-skip caused by player boundary colliders
  4 giant CollisionShape2D nodes (3400x26, 376x2592) were parented to the player
  node in county_road.tscn. This made the player's physics body span the entire
  scene, immediately overlapping exit zones and triggering a scene change on spawn.
  Fix: moved all 4 boundary shapes from player to a standalone SceneBounds
  StaticBody2D node at the scene root. Player node now only has its own collider.
  Files changed: scenes/world/county_road.tscn

[2026-04-01 CDT] CountryRoad stripped to editor-only layout (no dynamic spawning)
  Gutted county_road.gd -- removed ALL dynamic spawning: 120 decorative trees,
  60 bushes, 20 harvestable trees, weather overlay, seasonal visuals, mailbox/truck
  interaction, and chopping minigame. Script now only handles: scene ID, zone name,
  minimap/map overlay, and player spawn positioning.
  Exits are physical ZoneExit nodes in the .tscn (draggable in editor):
    - ExitToHome (green) - left side, goes to home_property
    - ExitToCedarBend (blue) - right side, goes to cedar_bend
  Removed Harmon Farm and Timber Creek exits.
  What you see in the editor is now what you get at runtime.
  Files changed: scenes/world/county_road.tscn, scripts/world/county_road.gd

[2026-04-01 CDT] DEV: Added axe, hammer, smoker to dev bypass inventory
  Dev bypass now pre-populates player inventory with all essential tools:
  hive tool, gloves, axe, hammer, smoker. This lets the chopping/crafting loop
  be tested immediately without needing to buy tools at Tanner's first.
  Files changed: scripts/world/home_property.gd

[2026-04-01 CDT] FIX: ESC double-toggle bug (menu opens then immediately closes)
  Root cause: Both hud.gd _input() and PauseMenu.gd _unhandled_key_input()
  processed the same ESC keypress. HUD opens the menu, then PauseMenu closes it
  on the same frame. Fix: HUD now calls set_input_as_handled() after ESC/P.
  Files changed: scripts/ui/hud.gd

[2026-04-01 CDT] FIX: Pause menu buttons rebuilt with custom controls
  Root cause: Godot's Button class has internal minimum-size calculations that
  auto-expand or clip text at the 320x180 viewport, making pixel-perfect layout
  impossible. Replaced _make_button() (Button nodes) with _make_btn() using
  Control + ColorRect (border) + ColorRect (fill) + Label. No Godot Button
  internals involved -- gives 100% pixel control over sizing, spacing, colors.
  Also increased BTN_H from 12 to 14, font_size from 6 to 7, and adjusted panel
  height to 138. Exit confirm dialog also uses custom buttons.
  Cleared .godot cache (imported, shader_cache, uid_cache, class caches).
  Files changed: scripts/ui/PauseMenu.gd

[2026-04-01 CDT] DEV: Bypass main menu for testing
  Changed project.godot main_scene from MainMenu.tscn to home_property.tscn.
  Added fresh-game state initialization to home_property._ready() so defaults
  (money, energy, day 1, Spring, etc.) are set when no save/character exists.
  MainMenu.tscn is preserved and can be re-enabled by reverting main_scene.
  Files changed: project.godot, scripts/world/home_property.gd

[2026-04-01 CDT] OVERHAUL: Pause menu UI rebuilt from scratch
  Problems: menu_panel.png texture had ruled lines bleeding through all elements,
  panel was 160x156 (87% of viewport height) cramming buttons and music controls,
  music slider/mute checkbox cut off at bottom edge.
  Fix: Removed texture overlay entirely (solid dark background only). Shrunk panel
  to 120x100 centered. Moved all volume controls (Music/SFX/Master + Mute) into
  Settings sub-panel only (no duplication on main panel). Clean 4-button layout:
  Resume, Save, Settings, Exit. Exit confirmation dialog also compacted.
  Files changed: scripts/ui/PauseMenu.gd

[2026-04-01 CDT] FIX: ESC key now opens/closes pause menu
  HUD _input() only handled KEY_P for pause toggle -- KEY_ESCAPE was missing.
  Added KEY_ESCAPE to the same match arm so ESC opens the pause menu with
  Resume, Save, Settings, and Exit options as designed.
  Files changed: scripts/ui/hud.gd

[2026-03-31 CDT] TWEAK: Skip character creation on new game
  Start button now bypasses CharacterCreator and goes straight to home_property.
  Sets default name "Beekeeper", backstory "newcomer" if no character exists.
  Files changed: scripts/ui/MainMenu.gd

[2026-03-31 CDT] FIX: Crash on ESC exit from interiors (null viewport)
  Root cause: _exit_supply() (and similar) calls change_scene_to_file() which
  frees the node. The next line get_viewport().set_input_as_handled() then
  crashes because the node is no longer in the tree (viewport is null).
  Fix: Swap the call order -- call set_input_as_handled() BEFORE the exit
  function in all 6 affected interiors.
  Files changed: feed_supply_interior.gd, diner_interior.gd, generic_interior.gd,
    honey_house.gd, house_interior.gd, post_office_interior.gd

[2026-03-31 CDT] FIX: Main menu logo not rendering
  Root cause: Sprite2D (a Node2D) renders below Control nodes in the same
  CanvasLayer. The background ColorRect (a Control) covered the logo.
  Fix: Switched logo from Sprite2D to TextureRect (a Control) with
  EXPAND_IGNORE_SIZE and STRETCH_KEEP_ASPECT_CENTERED for correct sizing.
  Also added Image.load_from_file() fallback using globalize_path().
  Files changed: MainMenu.gd

[2026-03-31 CDT] FIX: Sell honey screen shows 0 jars -- inventory lost on scene change
  Root cause: player inventory only existed on the player Node instance. When
  changing scenes (e.g. entering Feed & Supply), the old player was freed and a
  new one was created with default starting items -- losing all gameplay items.
  SaveManager.apply_to_scene() only ran on home_property, not interiors.
  Fix: Added persistent inventory mirror in GameData autoload.
  - GameData.player_inventory / player_inventory_valid: stores inventory across scenes
  - player._ready(): restores from GameData if valid, else uses starting defaults
  - player.add_item() / consume_item(): sync to GameData after every change
  - SaveManager._apply_player(): syncs to GameData after restoring from save file
  - MainMenu._reset_common_state(): clears GameData inventory on new game start
  Files changed: GameData.gd, player.gd, SaveManager.gd, MainMenu.gd

[2026-03-31 CDT] FEAT: Hierarchical ESC key behavior
  ESC now backs out of whatever screen you're in layer by layer:
    1. If a UI overlay is open (shop, dialogue, map, inspection, etc.) -> close it
    2. If inside a building with no overlay -> exit the building
    3. If on the overworld with nothing open -> open the pause menu
  Changes:
  - HUD._input: removed KEY_ESCAPE (was conflicting by always toggling pause).
    P key still toggles pause directly. ESC goes through the hierarchy.
  - PauseMenu._unhandled_key_input: unchanged -- only fires when no other handler
    consumed the ESC event, which is exactly the "overworld, nothing open" case.
  - map_overlay: added set_input_as_handled() after ESC close.
  - post_office_interior: added set_input_as_handled() after ESC close counter,
    added KEY_ESCAPE as exit key when no counter is open.
  - feed_supply_interior: added KEY_ESCAPE as exit key (alongside BACKSPACE).
  - diner_interior: added KEY_ESCAPE as exit key (alongside BACKSPACE).
  - house_interior: added KEY_ESCAPE to trigger exit.
  - honey_house: added KEY_ESCAPE to trigger_exit in _unhandled_key_input.
  - generic_interior: added _input() with KEY_ESCAPE to trigger exit.

[2026-03-31 CDT] FEAT: Main menu overhaul + pause menu exit + character creator cleanup
  1. MAIN MENU - simplified to Start / Continue / Quit (removed Spring/Fall choice).
     Start always begins in Spring. Logo now uses Sprite2D instead of TextureRect
     to avoid expand/stretch issues in CanvasLayer. Tries load() then Image.load().
  2. PAUSE MENU - "Quit Game" replaced with "Exit" which shows a confirmation dialog:
     "Save before exiting?" with Save & Exit / Exit Without Saving / Cancel buttons.
     New _exit_confirm panel built in _build_exit_confirm().
  3. CHARACTER CREATOR - Removed pronouns section. 3 focus sections instead of 4
     (name/backstory/confirm). Panel height 130. Defaults to they/them.

[2026-03-31 CDT] FIX + FEAT: All NPC shop/menu UIs redesigned + Godot error fixes
  1. FEED SUPPLY SHOP (feed_supply_interior.gd) -- complete UI rewrite:
     - Carl Tanner's shop now uses the same black-bg/gold-border button system as shop_screen.gd.
     - 11 item rows as clickable Buttons with selection highlight (StyleBoxFlat, offset-based layout).
     - Seasonal items show "[out of season]" and are grayed/disabled when not available.
     - Control row: [-] qty [+] [BUY] [SELL HONEY] [CLOSE] -- all mouse-clickable.
     - Keyboard nav: W/S select, A/D qty, E buy, Esc close.
     - Bulletin board UI also updated: removed deprecated set_anchors_preset(15) enum errors,
       now uses proper offset-based positioning with gold border styling.
     - Fixed unused "season" variable warning (renamed to _season).
     - Replaced unicode bullet chars in bulletin text with ASCII asterisks.
  2. DINER MENU (diner_interior.gd) -- complete UI rewrite:
     - Rose's diner menu now uses same black-bg/gold-border button system.
     - 5 menu item rows (ROW_H=12, slightly taller for readability) with selection highlight.
     - Shows availability: "Not served now" or "Already eaten" for unavailable items.
     - Control row: [ORDER] [CLOSE] buttons. Keyboard: W/S select, E order, Esc close.
     - Removed deprecated set_anchors_preset(15) and mouse_filter=2 enum errors.
     - Removed old toast system; error/feedback uses standard _err_lbl pattern.
  3. CEDAR BEND PHYSICS FIX (cedar_bend.gd):
     - _on_door_entered() and _try_enter_grange() now use call_deferred for scene changes.
     - Fixes "Removing CollisionObject during physics callback" error.
  4. ENUM CASTING FIXES across all three files:
     - Replaced set_anchors_preset(15) with proper offset-based layout (no enum needed).
     - Replaced mouse_filter = 2 with Control.MOUSE_FILTER_IGNORE.

[2026-03-31 CDT] FIX + FEAT: Dev buttons, super carry mechanic, frame return, pallet sprite
  1. INSPECTION OVERLAY DEV BUTTONS: +Day / +Month buttons invisible against dark footer bg.
     Normal bg raised Color(0.28,0.20,0.06) -> Color(0.50,0.34,0.09), bright amber. Text -> white.
  2. ITEM_SCRAPED_SUPER added to GameData. Stack max=1. Carry visual: player.gd shows carried super
     sprite for both ITEM_FULL_SUPER and ITEM_SCRAPED_SUPER (same hive_super.png).
  3. SCRAPED PALLET PICKUP: now gives player ITEM_SCRAPED_SUPER and shows carry visual. Player must
     physically walk the scraped super to the extractor (not auto-loaded).
  4. EXTRACTOR REQUIRES ITEM_SCRAPED_SUPER IN HAND: consumes it on interaction, immediately returns
     ITEM_SUPER_BOX to inventory, then launches extraction minigame.
  5. FRAME RETURN: _on_extraction_complete() gives player ITEM_FRAMES equal to frames extracted.
  6. SCRAPED PALLET SPRITE: SUPER_BOX_SCALE_SCRAPED 4.0 -> 2.0 (matches hive/pallet super size).

[2026-03-31 CDT] FEAT: NPC menu redesign -- sell_screen and shop_screen clickable buttons
  1. SELL SCREEN (sell_screen.gd) -- full UI overhaul:
     - Replaced old keyboard-only interface with proper clickable [-] [SELL] [CANCEL] [+] buttons.
     - New _on_minus() / _on_plus() callbacks; _refresh() disables buttons at qty bounds (can't
       go below 1 or above max jars; SELL greys out when 0 jars available).
     - Removed stale _make_label() helper (replaced by _abs_label() in previous session).
     - Qty label no longer shows < > arrow text; buttons communicate directionality visually.
  2. SHOP SCREEN (shop_screen.gd) -- full redesign from scratch:
     - FIXED CRITICAL BUG: old layout rendered 12 rows x 20px = 240px inside a 149px usable
       area -- ALL rows below row 7 were completely off-screen.
     - New layout: PANEL_X=16, PANEL_Y=2, PANEL_W=288, PANEL_H=178 (fills most of 320x180 viewport).
     - 12 item rows as clickable Button nodes (ROW_H=10px, font_size=7) -- mouse-clickable to select.
     - Selection highlighted with gold border and gold text via pre-built StyleBoxFlat objects
       (swapped in _refresh(), avoids per-frame garbage allocation).
     - Control row at bottom: [-] x(qty) [+]  ...  [BUY] [CLOSE] -- all mouse-clickable.
     - W/S keyboard navigation still works; A/D adjusts qty; E buys; Esc closes.
     - BUY button auto-disables when player cannot afford selected item.
     - Keyboard hint + error/feedback label both fit inside panel (layout verified at 178px height).
  3. HIVE MANAGEMENT (hive_management.gd) -- auto-close on super pickup:
     - _on_remove_super() now calls _close() + return after giving player ITEM_FULL_SUPER.
     - Empty supers (no honey) still show status message and stay open as before.

[2026-03-31 CDT] DOC: Complete Visual Asset Inventory for pixel artist
  Extracted and compiled ALL visual/art content from GDD (Sections 1-20):
  1. Game logo & branding specifications (1200x1200px, smoker design, typography)
  2. Player character sprite: beekeeper_spritesheet.png (960x2880px, 8-dir, idle/walk/run)
  3. NPC characters: Uncle Bob, Darlene, Frank, Dr. Ellen, Walt/Kacey, Silas (all 120x120px, 8-dir)
  4. Buildings: House, Honey House, shed, Feed & Supply, Diner, Market stall, town buildings
  5. Hive equipment: Modular sprites (base, deep, super, excluder, lid) + 5 hive stand variants
  6. Inventory items: 25 hotbar item icons (32x32px each: honey, frames, tools, treatments, etc.)
  7. Tilesets: Grass, dirt, stone, water, trees, wildflowers, fences, vegetation (16x16px base)
  8. UI elements: Langstroth Frame Design System (panels, buttons, fill bars, dialogs, notifications)
  9. Hive frame inspection: 320x192px front-facing view with hex cell grid, color-coded states
  10. Minigames: Queen Finder (bee sprites), uncapping, extraction, bottling, grafting, mating
  11. Environmental: Seasonal tints, particles (bees, smoke), weather effects, location-specific assets
  12. NPC dialogue: Portrait frames, speech bubbles, interaction visuals
  13. Quest/progression: Markers, knowledge log icons, level-up visuals
  14. Props & miscellaneous: Crafted goods, containers, tools, detailed decorative items
  15. Color palette: Reference muted earth tones, amber, cream, greens (with hex codes)
  16. Animation states: 8-directional idle/walk/run framework for all characters
  17. Title screen & pause menu UI assets
  18. Complete implementation checklist (5 phases: core, NPCs, environmental, minigames, advanced)
  Deliverable: research/art/COMPLETE_VISUAL_ASSET_INVENTORY.md (20,000+ word specification)
  Ready for: Pixel artist iteration and Leonardo AI asset generation prompt development

[2026-03-31 CDT] FIX + FEAT: Pallet visuals, scraper fork, toolbar icon, cedar bend crash, devmode shortcuts
  1. SUPER PALLET -- 4 individual super sprites: replaced single oversized (4x) sprite with 4 Sprite2D
     nodes in a 2x2 grid (2x scale, 48x28px each) using hive_super.png. Supports stacking up to 4 full
     supers. Prompt updates to show N/4. supers_on_pallet tracks count; correctly decrements as scraped.
  2. SCRAPING MINIGAME BRUSH -- metal fork tines: replaced transparent yellow ColorRect with uncapping
     fork visual (horizontal spine + MAX_TINES=20 metallic silver tines with highlight and tip). Tine
     positions recalculate dynamically on every cursor move to match clamped brush area.
  3. COMB SCRAPER TOOLBAR ICON -- added ITEM_COMB_SCRAPER -> "uncapping_knife.png" to hud.gd
     _load_item_textures() ITEM_SPRITE_MAP. Scraper now shows correct icon in hotbar.
  4. CEDAR BEND CRASH -- uncle_bob.gd extends Node2D but scene assigned it to type:Node. Fixed by
     changing UncleBobScript node type from Node to Node2D in cedar_bend.tscn.
  5. DEVMODE SHORTCUTS (active when dev labels are on):
     - Scraper brush is 2x larger (BRUSH_HALF_X 5->10, BRUSH_HALF_Y 3->6).
     - Scraping one side of one frame auto-completes ALL remaining frames for that super (fills scraped
       pallet to 10/10 instantly). No 0.7s delay, no side-B requirement.
     - Filling one jar at the bottling table auto-fills ALL available jars (up to 40, limited by honey
       and inventory). Bottling minigame overlay is skipped entirely.

[2026-03-31 CDT] FIX: Jar overlap, flower z-order, sell crash, debugger warnings (round 2)
  1. BOTTLING TABLE JARS: Fixed jar overlap in _draw_jar_stacks() -- jars were 8px tall with only
     2px vertical spacing (6px overlap). Changed to full jar_h + 2px gap, 5 per stack instead of 10.
     Same fix in bottling_minigame.gd _draw_filled_jar_on_table() -- 10px jars had 3px spacing.
  2. FLOWER Z-ORDER: Set harvest yard z_index=2 in home_property.gd so bottling table (and other
     stations) render above flower sprites (z_index=1 from dandelion_spawner).
  3. SELL CRASH FIX: exit_helper.gd was calling change_scene_to_file() directly inside a
     body_entered physics callback, causing "Removing CollisionObject during physics callback"
     errors and crashes. Changed to call_deferred("change_scene_to_file", ...) to defer scene
     transition to next frame.
  4. DEBUGGER WARNINGS ROUND 2:
     - extractor_minigame.gd: suppress _extractor_circle unused class var
     - bottling_minigame.gd: suppress _jar_complete unused class var; rename duplicate 'timer'
       variables to done_timer/reset_timer/cancel_timer/finish_timer
     - county_road.gd: suppress sprite_key unused variable
     - harvestable_tree.gd: suppress tree_interact unused signal
     - cedar_bend.gd: suppress _current_scene_path unused class var
     - ScrapeFrame.gd: suppress cells_b shadowed_variable in fill_from_hive_data()
     - hive_management.gd: replaced set_anchors_preset(15) with Control.PRESET_FULL_RECT
     - scraping_minigame.gd: suppress integer_division in wire guide calculation
     - hud.gd: suppress integer_division in hotbar centering
     - NotificationManager.gd: suppress unused 'slot' variable in _animate_in_then_out()

[2026-03-31 CDT] FIX: Debugger warning cleanup (harvest_yard, hud, BeeOverlay, weather_particles)
  1. harvest_yard.gd -- _action_extractor: added @warning_ignore("unused_parameter") for 'player'
     param (player interaction not yet implemented in extractor minigame).
  2. harvest_yard.gd -- _update_bucket_visual: renamed 'var show' to 'var bucket_visible' to
     eliminate shadowing of CanvasItem.show() method.
  3. harvest_yard.gd -- super box Sprite2D nodes added: _super_box_sprite and _scraped_box_sprite
     display hive_super.png at 4x scale on both pallets. Visibility and color modulate driven by
     _super_placed / _super_box_on_scraped state and frame count fill fraction.
  4. harvest_yard.gd -- extractor sprite scale set to Vector2(0.5, 0.5) so 128x96 PNG fits 64x64
     station footprint.
  5. hud.gd -- _on_dev_toggled: renamed 'is_visible' param to 'panel_visible' (shadowed CanvasLayer
     method). Also renamed 'var tr' in _make_icon to 'var icon' (shadowed Object.tr() method).
     Added @warning_ignore("unused_variable") before 'var SHORT' dict (kept for future short-label
     display, never yet referenced).
  6. BeeOverlay.gd -- added @warning_ignore("unused_parameter") before _create_queen() and
     _pick_new_target() -- both accept a 'spread' float param that is not yet consumed.
  7. weather_particles.gd -- added @warning_ignore("unused_private_class_variable") before
     _ground_y. Added @warning_ignore("unused_parameter") before _draw_snow() and _draw_splashes()
     -- cam_offset params carried for future scrolling support but not yet read.

[2026-03-31 CDT] ART: Leonardo AI sprites for honey spinner + bucket; TileSet error fix
  1. HONEY SPINNER SPRITE (objects/honey_spinner.png): Replaced gray circle placeholder with
     Leonardo AI extractor barrel art (128x96, transparent bg). harvest_yard.gd now creates
     a Sprite2D node (_extractor_sprite) instead of drawing a circle in _draw().
  2. HONEY BUCKET SPRITES:
     - honey_bucket.png (48x48): Full bucket -- Leonardo art with orange honey visible.
     - honey_bucket_empty.png (48x48): NEW -- empty bucket with cream interior (derived from full).
     harvest_yard.gd preloads both textures in _create_bucket_sprite(), _update_bucket_visual()
     switches between them based on _bucket_honey_lbs and moves the sprite between yard/table.
  3. HARVEST_YARD.GD CLEANUP: Removed unused _station_sprites and _bucket_node variables
     (eliminated two GDScript reload warnings visible in debugger).
  4. TILESET ERROR FIX: Upscaled 5 flower tile textures from 16x16 -> 32x32 (nearest-neighbor)
     -- clover_mature, aster_mature, coneflower_mature, dandelion_mature, goldenrod_mature.
     TileSetAtlasSource in home_property.tscn specifies texture_region_size 32x32; 16x16 textures
     caused repeated "create_tile: Cannot create tile -- outside texture" errors at startup.
  PENDING ASSETS (not yet generated in Leonardo): pallet (new version), jarring/bottling table.
  Protected files verified: GDD 603,634 bytes, DevPlan 139,726 bytes.

[2026-03-31 CDT] FEATURE: Beeswax tracking, winter reserve warning, HUD standing/smoker display, quest system, name injection
  1. BEESWAX TRACKING (task 2.22): Scraping minigame already collects beeswax at ~0.735 lbs per frame
     and stores in _bucket_beeswax_lbs. _try_give_beeswax() converts fractional lbs to ITEM_BEESWAX
     for inventory when >= 1 lb accumulated. Formula: 4900 cells * 0.00015 = 0.735 lbs per frame.
  2. WINTER RESERVE WARNING (task 2.23): When player removes marked super in Fall, checks hive's
     honey_stores via new get_honey_stores() method. If < 60 lbs, fires warning notification
     "Warning: colony only has X lbs honey -- 60 lbs needed for winter!" Harvest still allowed.
     Added hive.gd::get_honey_stores() -> float method to query simulation.last_snapshot.
  3. HUD STANDING DISPLAY (task 3.0, partial): Added _standing_lbl to bottom bar (amber/gold color)
     showing "Standing: [tier]". Polls GameData.get_community_standing_tier() every frame.
     Also added _smoker_lbl showing "SMOKED" (orange) when player._smoker_active is true.
     Added _update_standing() and _update_smoker_status() methods with caching.
  4. QUEST MANAGER EXPANSION (task 4.1-4.3, partial):
     - Added quest_data dict storing quest definitions with objectives, rewards
     - Added complete_objective(quest_id, obj_key, amount) to track objective progress
     - Added is_quest_active(id) and is_quest_complete(id) aliases
     - Added _init_quest_definitions() with bob_intro (+25 XP) and 3 daily tasks:
       * daily_inspection: Inspect 1 hive (+10 XP, +$5)
       * daily_harvest: Remove 1 super (+15 XP, +$10)
       * daily_market: Make 1 market sale (+20 XP)
     - Daily quests auto-reset each day and auto-start on init/reset
  5. SELL SCREEN NAME INJECTION (task 5.0): Sell screen greeting now uses player name from
     PlayerData singleton: "Hey [name], I'll pay $X/jar!" if player name exists, else fallback.
     Updated sell_screen.gd _build_ui() to check Engine.has_singleton("PlayerData") and inject
     player_name into greeting message.
  Protected files verified: GDD 603,634 bytes, DevPlan 139,437 bytes.

[2026-03-30 CDT] FIX: FrameRenderer 0-dimension guard + scraping 100% completion
  1. FRAMERENDERER CRASH FIX: Added guards to render_lod(), render_honeycomb(),
     and _ensure_canvas_for() to prevent Image.create() with 0-pixel dimensions.
     If grid_cols or grid_rows is 0, returns a 1x1 fallback texture instead of
     crashing with "The Image width specified (0 pixels) must be greater than 0".
  2. SCRAPING COMPLETION: Changed uncapping threshold from 95% to 100% in
     scraping_minigame.gd _update_progress(). Player must now uncap every single
     capped cell before the frame side is marked done.
  Protected files verified: GDD 603,634 bytes, DevPlan 139,437 bytes.

[2026-03-30 CDT] ART: New honey bucket and extractor spinner sprites from Leonardo AI
  Generated pixel art sprites via Leonardo AI (Lucid Origin, 1024x1024, Dynamic/Fast)
  using Smoke and Honey collection for style consistency.
  Nathan selected B1 (white plastic honey bucket) and S3 (copper/wood extractor drum).
  Backgrounds removed via flood-fill algorithm, sprites resized to 32x32 grid.
  Files replaced:
  - assets/sprites/items/honey_bucket.png (32x32) -- new bucket with visible honey
  - assets/sprites/interiors/extractor.png (32x32) -- new extractor drum icon
  - assets/sprites/objects/honey_spinner.png (128x96) -- larger extractor for world view
  Protected files verified: GDD 603,634 bytes, DevPlan 139,437 bytes.

[2026-03-30 CDT] REDESIGN: Scraping minigame -- rectangular brush replaces full-height column strip
  Based on reference video, the de-capping brush is now a rectangular scraper
  (11 columns x 7 rows) that follows the cursor, instead of a full-height
  vertical strip that cleared entire columns in one pass. Player must now
  paint across the whole frame with multiple passes, like a real uncapping knife.
  Changes in scraping_minigame.gd:
  - BRUSH_HALF replaced by BRUSH_HALF_X=5 and BRUSH_HALF_Y=3
  - _try_scrape() iterates only cells within the rectangle, not all rows
  - _update_brush() positions rectangle at cursor (col,row) not just col
  - Brush visual (blade + edges + handle) tracks both X and Y position
  Protected files verified: GDD 603,634 bytes, DevPlan 139,437 bytes.

[2026-03-30 CDT] FIX: Frame data discrepancy + honey jar pickup
  1. FRAME DATA DISCREPANCY: De-capping minigame now uses actual hive cell data
     instead of a hardcoded 85% capping distribution. When a super is removed from
     the hive, its frame cell arrays are stored in GameData.harvested_super_frames.
     Both honey_house.gd and harvest_yard.gd read this stored data via
     ScrapeFrame.fill_from_hive_data() so the de-capping view matches inspection.
     Files changed: GameData.gd (new harvested_super_frames var),
     ScrapeFrame.gd (new fill_from_hive_data method), hive_management.gd (store
     frame data on remove), player.gd (store frame data on marked-super remove),
     honey_house.gd (use stored data), harvest_yard.gd (use stored data + pass
     ScrapeFrame to scraping overlay).
  2. HONEY JAR STACKING: Max stack for ITEM_HONEY_JAR changed from 99 to 20
     in player.gd get_max_stack().
  3. JAR COLLECTION FIX: Players can now collect filled jars from the bottling
     table even when the bucket still has honey. If the active item is not an
     empty jar and there are filled jars on the table, pressing E collects them.
     Bucket stays on table until both jars are collected AND bucket is empty.
  Protected files verified: GDD 603,634 bytes, DevPlan 139,015 bytes.

[2026-03-30 CDT] LOGO: Approved game logo propagated to Five Cats / beekeeperPro project files
  - Copied assets/ui/logo/game_logo.png to beekeeperPro/assets/game_logo.png
  - Updated beekeeperPro/Smoke_and_honey.html Slide 8 ("Logo concepts") to display the
    approved logo image (assets/game_logo.png) in place of the three SVG concept sketches.
    Added composition/typography/source summary cards below the logo display.
  - FiveCatsStudios_BusinessPlan.html: no game logo section needed (references studio cat logo only).
  - beekeeperPro/BeeKeeper_Pro_DevPlan.html: no logo section found; no update needed.
  Protected files verified: SmokeAndHoney GDD 603,634 bytes, DevPlan 104,468 bytes.
  beekeeperPro GDD 541,708 bytes, DevPlan 65,903 bytes.

[2026-03-30 CDT] FIX: Canning table bucket placement (honey_house.gd)
  Three targeted fixes to the bucket-to-table workflow:
  1. State desync recovery: each frame, if _player_carrying_bucket is false but
     the player has ITEM_HONEY_BUCKET in inventory (and honey exists in the bucket),
     _player_carrying_bucket is automatically re-synced to true. Prevents stale
     state after scene transitions or any other unexpected resets.
  2. Larger CANNING proximity when carrying: threshold expands from 87px to ~186px
     so the player can trigger [E] Place bucket on table from anywhere in the aisle,
     not just the left side of the room near the table.
  3. Removed Phase 1 auto-pickup from CANNING station action: pressing E at the
     canning table without a bucket now shows a directional hint ("Pick up the honey
     bucket to the right of the spinner first!") instead of silently picking up
     and then confusing the player with "Carry it to the canning table!" status.
     The BUCKET station remains the only place to physically pick up the bucket.
  Protected files verified: GDD 585,523 bytes, DevPlan 102,508 bytes.

[2026-03-30 CDT] PHASE 2 POLISH: 10-item feature/fix pass -- z-layering, dev buttons, decapping visuals, organic flowers, bucket carry, jarring workflow.
  1. FLOWERS vs EQUIPMENT (z-index layering): Removed all exclusion-zone/protected-rect logic from
     dandelion_spawner.gd, wildflower_scatter.gd, and flower_lifecycle_manager.gd. Equipment now
     overlaps flowers via z_index: flowers z=1, equipment z=3+. Harvest yard pallets bumped to z=3.
  2. DEV BUTTONS IN INSPECTION MODE: Fixed InspectionOverlay.gd consuming the G key before
     GameData could see it. Root cause: _unhandled_key_input() called set_input_as_handled()
     unconditionally after the match block. Fix: added explicit KEY_G case that calls
     GameData.toggle_dev_labels(), allowing +Day/+Month buttons to show/hide in-overlay.
  3. WHITE CAPS IN DECAPPING: ScrapeFrame.fill_for_harvest() was placing ~7.5% of cells in
     S_DRAWN_EMPTY (pale wax, avg RGB [191,175,122]) which appeared white among amber honey.
     Fixed: _pick_state() now returns only S_PREMIUM_HONEY (capped) or S_CURING_HONEY (uncapped).
     No drawn-empty cells in harvest-ready frames.
  4. ORGANIC FLOWER CLUSTERS: Replaced uniform 6x6 grid sampling in flower_lifecycle_manager.gd
     with cluster-seed distance-falloff algorithm. Each species uses CLUSTER_PARAMS (avg_cluster
     size and radius). Dandelion/clover biased 55% toward world edges. Core tiles always placed,
     fringe tiles fade to ~30% probability at cluster edge. Produces natural-looking patches.
  5. SPINNER SPRITE: honey_spinner.png loaded from assets/sprites/objects/ in honey_house.gd
     spinner station setup (was placeholder/missing).
  6. BUCKET CARRY VISUAL: player.gd extended with _carried_bucket_sprite (Sprite2D loaded from
     assets/sprites/objects/honey_bucket.png). Shown/hidden based on ITEM_HONEY_BUCKET count.
     Floats in front of player at facing_direction * 10 + (4,4) offset, z_index=3.
  7-9. JARRING WORKFLOW (honey_house.gd CANNING station, three phases):
     Phase 1 (no bucket): E auto-picks up bucket if honey available.
     Phase 2 (carrying bucket): E places bucket on canning table, consumes ITEM_HONEY_BUCKET
       from player inventory, shows bucket sprite on table.
     Phase 3 (bucket on table): each E press fills one jar (consumes ITEM_JAR, shows jar sprite
       on table). Jars line up left-to-right in rows of 4. When honey exhausted or no jars left,
       E collects all jars at once (adds ITEM_HONEY_JAR * count to player inventory, frees
       sprites, resets state). Prompts updated per phase.
  10. SELL IN TOWN: Already functional via existing sell_screen.gd -- ITEM_HONEY_JAR registered
      and priced. No code changes needed.
  ASCII compliance verified on all 8 modified .gd files. Protected files verified:
  GDD 569,013 bytes, DevPlan 102,508 bytes.

[2026-03-30 CDT] AUDIO: Replaced seasonal music tracks with new versions from research/music. Old tracks archived to assets/audio/seasonalMusic/old/ and assets/audio/main_theme/old/. New tracks (1_Quickening.mp3 through 8_Kindlemonth.mp3) now live in assets/audio/seasonalMusic/. Main theme (mainTheme.mp3) placed in assets/audio/main_theme/ -- reserved for future use (title screen, etc.). MusicManager.gd MONTH_TRACKS array updated to reference new file paths. Stale .import files moved to old/ dirs -- Godot will regenerate imports on next open. No script logic changed; month-based looping and crossfade already fully functional. Protected files verified: GDD 566,687 bytes, DevPlan 101,899 bytes.

[2026-03-30 CDT] LOGO APPROVED: Official Smoke & Honey game logo selected. Nathan approved logo from the "smoke from spout + ornate text" round (research/art/logos/Almost_There.html). Design: bee smoker anchored LEFT (squat silver cylinder, flat leather board bellows with copper rivets, smoke from side spout nozzle), ornate amber-orange calligraphic script "Smoke & Honey" RIGHT with cream highlights, swash flourish, amber-brown background. (1) GDD: New section 10.0 "Game Logo & Brand Mark" added before section 10.1, documenting full design spec, color palette, composition rules, and usage guidelines. Nav link added. (2) DevPlan: "Game Logo -- APPROVED" row added to Art & Video Capture table; capsule/banner art note updated to reference approved logo as source. Art & Video Capture nav status updated to in-progress. (3) Almost_There.html: Green "LOGO APPROVED" banner added at top with file path reference. (4) assets/ui/logo/ directory created -- place approved logo PNG there as game_logo.png. Protected files verified: GDD 569,013 bytes, DevPlan 102,508 bytes.

[2026-03-30 CDT] LOGO: Almost There report updated -- smoke from spout + ornate text round. Generated 8 new logo variations (32 images total) in Leonardo AI (Lucid Origin, 1200x1200, Fast). Two corrections from Nathan's feedback: (1) smoke now emits from the side spout nozzle on the cylinder body, NOT from the top tube opening; (2) text style returned to ornate amber-orange calligraphic/vintage script with swash flourishes and golden glow. Flat-panel bellows anatomy preserved (squat wide silver barrel, rectangular leather board bellows, dome lid). Gen IDs: VAR01=0ee47d7e, VAR02=2ce0d394, VAR03=70e33243, VAR04=8e43dfd1, VAR05=5519b944, VAR06=b2429146, VAR07=356318be, VAR08=5c30bfbb. Updated research/art/logos/Almost_There.html. Protected files verified: GDD 566,687 bytes, DevPlan 101,899 bytes.

[2026-03-30 CDT] FIX: Honey bucket pickup and flower exclusion zones. (1) BUCKET PICKUP: Player could not pick up the honey bucket to carry it to the canning table. Root cause: the CANNING station's larger proximity threshold (87px) always dominated the BUCKET station threshold (62px), so pressing E always triggered the canning action instead of bucket pickup. Fixed honey_house.gd _action_canning(): on first E press when not carrying but honey available, auto-pick up the bucket (same as walking to the bucket tile). Also updated _get_station_prompt() for CANNING to show "[E] Pick up bucket" instead of the old "Pick up bucket first" message. (2) FLOWER EXCLUSION ZONES: Dandelions and wildflowers were spawning on top of buildings, hive, and harvest yard equipment. Fixed all three flower systems: dandelion_spawner.gd now has EXCLUSION_RECTS constant and _is_excluded() check that skips spawn tiles inside buildings/equipment zones. wildflower_scatter.gd has the same exclusion check and aborts spawning individual flower sprites that land inside exclusion rects. flower_lifecycle_manager.gd's _build_no_spawn_mask() now calls new _mark_world_rect_no_spawn() helper to add HarvestYard (420,240,460x330) and Hive (248,298,110x110) to the no-spawn mask alongside existing building footprints. Protected files verified: GDD 566,687 bytes, DevPlan 101,899 bytes.

[2026-03-30 CDT] VISUAL: Scraping minigame upgraded with hex honeycomb cells and custom cursor. (1) New scraping_hex_grid.gd Node2D child draws pointy-top hexagonal cells using draw_colored_polygon() matching the inspection overlay look. Dark wax background fills gaps between amber hex cells. Uncapped cells show bright honey amber with a small highlight sparkle. (2) scraping_minigame.gd rewritten: removed 336-ColorRect grid, replaced with HexGridScript.new() child node. Hit detection updated for hex column stagger (odd columns offset 3px down). (3) Custom mouse cursor: uncapping_fork_cursor.png (32x64, yellow handle, metal tines) set via Input.set_custom_mouse_cursor() on minigame open, restored to default on complete or ESC. Hotspot at tine tips (16, 60). Protected files verified: GDD 566,687 bytes, DevPlan 101,899 bytes.

[2026-03-30 CDT] FEATURE: Bucket grip item gates honey carry step to bottling. New physical carry loop: after extraction completes a bucket sits at the extraction yard. Player must equip Bucket Grip tool, interact with bucket (E) to pick it up as ITEM_HONEY_BUCKET, then carry it to the Bottling Table (bucket consumed on placement). New items: ITEM_BUCKET_GRIP and ITEM_HONEY_BUCKET in GameData.gd. Starting chest includes 1x Bucket Grip. Sprite icons bucket_grip.png and honey_bucket.png (32x32) added with .import files. hud.gd and chest_storage.gd updated with display names, tint colors, and sprite entries. Protected files verified: GDD 566,687 bytes, DevPlan 101,899 bytes.

[2026-03-30 CDT] MARKETING: KS live story updated with new copy. (1) Anti-AI-art intro added to "A Letter From the Beekeeper" section: new opening paragraphs lead with headlines about studios replacing art teams with AI before pivoting to "This is not that kind of game." (2) "About the Art" section replaced with expanded "On AI Art -- and Why This Campaign Exists" section: direct statement that every current art asset will be deleted, 47% budget figure for human pixel artist prominently featured, "Your pledge is a vote: the artist over the algorithm." (3) Character creation language already in place from prior session. (4) Saved via CKEditor API -- story tab confirmed saved and preview shows updated nav with new section title. (5) Hero image KS_hero_apiary_golden_hour.jpg (1600x896, Lucid Origin) downloaded to Downloads folder -- KS S3 upload endpoint returning 503 (server-side issue), manual upload required: go to Basics tab, upload from Downloads folder. Protected files verified: GDD 566,687 bytes, DevPlan 101,899 bytes.

[2026-03-30 CDT] MARKETING: KS campaign overhauled with anti-AI-art-replacement angle. (1) Kickstarter_Campaign_Page.md: Intro rewritten to lead with AI art crisis in gaming, added direct statement that all current art is AI placeholder and campaign exists to replace it with human work. Art section fully rewritten -- positions pledge as vote for artist over algorithm. Video script [1:20-1:45] updated to name AI art controversy directly. Closing ask rewritten: "In an industry choosing the algorithm, we're choosing the artist." Risks section updated: "AI placeholder art" replaces softer "placeholder art" language. (2) KS profile bio updated and saved: new bio is direct/human/no AI tells ("Real beekeeper. Solo developer. Grundy Center, Iowa..."). Privacy setting unchecked so bio now shows publicly. (3) Leonardo AI generated 4 hero images (1600x896, Lucid Origin) showing Iowa apiary at golden hour with two beekeepers and pickup truck -- pending Nathan approval before upload to KS. Protected files verified: GDD 566,687 bytes, DevPlan 101,899 bytes.

[2026-03-30 CDT] DESIGN CHANGE: Player character is now player-created, not fixed as "Eli." Updated across all three documents. (1) GDD: Section heading updated to "The Player Character & Character Creation." Added full Character Creation System section with customization table (name, appearance, pronouns, backstory tag) and design principles. Added design note clarifying "Eli" throughout remaining quest/narrative text is default-name shorthand. Updated gender-specific "He is defined" line to neutral language. (2) Kickstarter_Campaign_Page.md: Replaced all "Eli" player character references with player-created character language throughout (What Is Smoke and Honey, story sections, stretch goals). (3) Live Kickstarter story: Both "a genuine story about a kid finding his footing" and "You play as Eli, a twelve-year-old kid..." updated to player-created character language via CKEditor API and saved. Protected files verified: GDD 566,687 bytes, DevPlan 95,020 bytes.

[2026-03-30 CDT] DEVPLAN UPDATE: Character Creator system added to Phase 3 based on GDD S21 (Player Character & Character Creation). (1) Sidebar nav: "Character Creator" sub-link added under Phase 3. (2) Phase 3 build order updated: creator scene and pronoun token pass are now steps 1-2, before quest system work. (3) Phase 3 exit criteria updated: character creator, name injection, and pronoun-clean dialogue all required gates. (4) New task section 3.0-3.0e: PlayerData singleton (name/pronouns/backstory/appearance fields), character creator scene (new game flow, live portrait preview), layered portrait sprite system (title card / journal / cutscene -- NOT gameplay, always beekeeper gear), name injection ({player_name} token in DialogueUI + Knowledge Log + market nameplate), pronoun token pass ({they}/{them}/{their} in all NPC strings -- existing Bob dialogue needs retrofit), backstory tag (journal intro + 1-2 Bob lines, flavor only). (5) Current state banner updated: notes fixed sprite is correct for gameplay, character creator is Phase 3 TODO, pronoun tokens not yet in dialogue. (6) MVP Checklist updated: tasks 3.0-3.0d added, market nameplate row notes player_name wire-up during Phase 2 build. (7) Phase 3 art callout updated: character creator portrait art flagged as HIGH PRIORITY / blocks 3.0b. Protected files verified: GDD 566,687 bytes, DevPlan 101,899 bytes.

[2026-03-30 CDT] DOCS UPDATE: Business & Funding section fully restructured in DevPlan. (1) CLAUDE.md updated with permanent rule: GDD is game-only -- no LLC/business/funding content ever. (2) GDD confirmed clean of LLC content (Section 11 Monetization is game design only -- distribution model, pricing philosophy -- not formation details). (3) DevPlan Business & Funding replaced with: Master Priority List (14 items, game + LLC interleaved in dependency order), LLC Formation Status, Art & Video Capture Plan (OBS + DaVinci Resolve workflow, 4 asset types with specs), Platforms & Launch (itch.io, Steam, GOG/Epic/Mobile stretch), Kickstarter Campaign Plan (gate requirements, reward tiers, fees, promotion strategy), Grants & External Funding (Iowa Arts Council, Indie Fund, NEA), Overhead & Revenue, Release Timeline. Sidebar nav updated with 6 Business sub-section links. Protected files verified: GDD 563,588 bytes (unchanged), DevPlan 95,020 bytes.

[2026-03-30 CDT] DEVPLAN UPDATE: Major DevPlan revision incorporating new systems and business formation. (1) Current State: scripts count updated 60->61 (MusicManager autoload added March 28), verified callout updated to March 30. (2) Simulation verification logged: honey production now confirmed working (LBS_PER_NU fix, comb drawing fix). (3) Phase 5 audio milestone: MusicManager marked DONE with 8-track seasonal system detail. (4) Phase 2 development principle updated: honey simulation verified, ~80 lbs harvestable per season confirmed, harvest pipeline now has validated pre-condition. (5) NEW Business & Funding section added: LLC formation status (EIN 41-5191825 confirmed), Certificate of Organization pending filing, corrected grant eligibility (Iowa Greenlight Grant = film only NOT eligible, Epic MegaGrants = Unreal only NOT eligible), active grant targets (Indie Fund Q4 2026, Iowa Arts Council Sept 2026, NEA Arts in Digital Media annual), revenue projections, monthly overhead, release timeline, Steam strategy. (6) Sidebar nav updated with Business & Funding link. Protected files verified: GDD 563,588 bytes, DevPlan 76,170 bytes.

[2026-03-30 CDT] BUSINESS: EIN confirmed for Five Cats Studios LLC. IRS Notice CP575G received March 30, 2026. EIN: 41-5191825. Name control: FIVE. Certificate of Organization prepared but not yet filed with Iowa SOS. Next action: file at sos.iowa.gov ($50) to complete LLC formation, then open business bank account. Operating Agreement already drafted.

[2026-03-30 CDT] SIMULATION FIX: Honey supers never filling -- 4 bottlenecks found and fixed. (1) LBS_PER_NU was 0.0005 (10x too low). At peak summer NU ~800/day, colony produced only 0.15 lbs/day gross -- less than the 0.5 lbs/day consumption. Net production was NEGATIVE, making it physically impossible to fill supers. Fixed: LBS_PER_NU = 0.005. Now at peak: 400 NU -> 2.0 lbs/day gross, ~1.5 net after consumption. Over 80 productive days = ~120 lbs gross, ~80 lbs harvestable (enough for 2 supers). (2) Super comb drawing rate was 0.30 per house bee with 3000 cell/day cap -- far too slow. A 2450-cell super frame side would take days to draw even at max rate. Real bees draw a full super in 1-2 weeks during peak flow. Fixed: base rate 0.80 per house bee, cap raised to 8000 cells/day. (3) Super comb drawing required honey_stores >= 2.0 lbs and brood box >= 80% drawn. Both thresholds were too conservative -- lowered to 1.0 lbs stores and 60% brood drawn. store_mult scaling improved: starts at 0.50 at 1 lb (was 0.30 at 2 lbs). (4) Season rankings forced to S-rank as dev default for tuning. Random roll commented out with TODO to restore once production is balanced. Protected files verified: GDD 558,285 bytes, DevPlan 68,801 bytes.

[2026-03-30 CDT] DEV: +Day/+Month buttons in frame inspection overlay. When dev mode is active (G key), two small buttons appear in the bottom of the stats sidebar during hive inspection. +Day advances one simulation day and live-refreshes the frame view so you can watch comb drawing, egg laying, nectar deposits, bee bread, honey curing, and capping happen cell by cell. +Month runs 28 days at 0.05s intervals with per-day frame refresh -- you can watch the entire colony develop in real time. Buttons toggle with dev mode (G key) and show countdown during month advance. Same simulation logic as the HUD dev buttons (advance_day on all hives, flowers, TimeManager, energy restore). Protected files verified: GDD 558,285 bytes, DevPlan 68,801 bytes.

[2026-03-30 CDT] FIX: Foundation cells rendering as light tan instead of black. The 00_empty_foundation.png sprite in the cell atlas was a light beige hex (same as drawn comb) instead of the dark plastic backing it should be. Regenerated the sprite as a near-black hex (RGB 18,15,13 matching LOD palette 0.07,0.06,0.05) with subtle dark outline (30,26,22) and faint center highlight (25,22,18) to show hex structure without looking like a void. Rebuilt cell_atlas.png (390x20, 15 states). Foundation now appears black in both honeycomb and LOD render modes, and only turns light as bees draw wax comb (state 1: S_DRAWN_EMPTY). Protected files verified: GDD 558,285 bytes, DevPlan 68,801 bytes.

[2026-03-30 CDT] FIX: Dev +Day/+Month buttons were giant and overlapping (v3). Root cause: Control parent inside CanvasLayer gets auto-expanded to fill viewport regardless of anchors, and Button children inherit that sizing via default theme minimum sizes. Complete rewrite: replaced Control container with bare Node (no layout behavior), created _create_dev_button() helper that builds each Button with all 5 stylebox states overridden (normal/hover/pressed/disabled/focus) using zero-margin StyleBoxFlats, explicit anchor offsets for absolute positioning (anchor_left/top/right/bottom = 0, offsets define exact pixel rect). Buttons are 52x14px each, positioned at y=20 (Day) and y=37 (Month) just below energy bar. Visibility toggle updated to use individual button refs. Protected files verified: GDD 558,285 bytes, DevPlan 68,801 bytes.

[2026-03-30 CDT] TREE RELOCATION + SUPER BOX PLACEMENT. Two changes: (1) TREES: Relocated 5 trees that were blocking the hive (300,350) and harvest yard (480,350) area. WildPlumFence1/2 moved further west (-100,400) and (-140,480). CottonwoodWind moved to far north-east (680,-60). BasswoodSouth moved to far south-east (820,450) well clear of harvest yard stations. ElmDead moved to south-west (-80,520) away from hive. SilverMaple, AppleTree, Sycamore, and Willows unchanged (already clear). (2) HARVEST YARD: Scraped pallet now requires player to select and place an empty Super Box before scraped frames can be deposited. New flow: scrape frames at super pallet -> walk to scraped pallet with Super Box equipped -> press E to place box -> scraped frames fill the box as you scrape -> at 10/10 take to extractor. Added _super_box_on_scraped state flag, visual brown box drawn on scraped pallet when placed, prompt text guides player through workflow, scraping warns if no box placed. Super box consumed from inventory on placement. Protected files verified: GDD 558,285 bytes, DevPlan 68,801 bytes.

[2026-03-29 CDT] NPC Sprite Evaluation Report v2. Generated all 18 NPC characters (12 named + 6 pedestrians) in both front-facing and top-down views via Leonardo AI (Lucid Origin, 1024x1024, Dynamic, Fast, 4 options each). Total: 37 generation sets (144+ images). Characters: Uncle Bob, Frank Fischbach, Dr. Ellen Harwick, Darlene Kowalski, Walt Harmon, Kacey Harmon, Silas Crenshaw, Carl Tanner, June, Rose, Lloyd Petersen, Terri Vogel, Pedestrians A-F. Prompts based on weighted family feedback (Nathan 30%, Daniel 25%, Aadyn 25%, Kari 20%). Built interactive HTML evaluation report (NPC_Sprite_Evaluation_Report_v2.html) with dark theme, clickable A/B/C/D selection per character per view, lightbox zoom, progress tracker, and JSON export. Saved to research/art/. Protected files verified: GDD 558,285 bytes, DevPlan 68,801 bytes.

[2026-03-29 CDT] MOVEMENT REWORK: Bee overlay now feels organic instead of robotic. Completely rewrote movement system in BeeOverlay.gd to give bees realistic, lifelike behavior with multiple simultaneous improvements: (1) SPEED VARIATION: Bees now accelerate smoothly at start (0.5-1.0 mult), sustain full speed mid-path (0.95-1.1 mult), and decelerate approaching targets (0.4-0.7 mult). Added 3% per-frame burst chance for brief 0.15-0.3s speed rushes (1.8-2.5x mult) creating urgency. Queens have longer acceleration phase (15% of path) and larger deceleration zone (25px). (2) MICRO-WOBBLE PATH DEVIATION: Bees no longer walk in perfectly straight lines. Each bee oscillates 0.3-0.8px perpendicular to heading (3-5 Hz frequency) using per-bee wobble_phase and wobble_amp. Creates natural, bug-like lateral shimmy without changing target. (3) IDLE BEHAVIOR VARIATION: 15% antenna-clean (extends idle 0.3-0.6s for fidgeting), 10% 360-scan (rapid direction changes during idle), 75% normal. Fidget_factor multiplies idle duration per bee (0.5-1.5 range), making some bees hyper and others calm. (4) ATTENDANT ORBIT IMPROVEMENTS: Per-attendant drift speed (0.05-0.12 rad/sec) instead of constant 0.08. 8% chance/sec to break orbit briefly, moving 5-10px away then returning. Orbit radius wobbles 16-20px per frame (instead of fixed 18px). (5) QUEEN MOVEMENT POLISH: Queens take more time to accelerate, sometimes pause mid-step (2-5% chance/sec for 0.3-0.5s pause), and have larger deceleration zone (25px vs 15px). All new fields added to bee dicts at spawn with proper initialization. No sprite, rendering, hit-test, or density model code touched. All ASCII-verified, no := with ternary/as. Protected files verified: GDD 558,285 bytes, DevPlan 68,801 bytes.

[2026-03-29 CDT] FIX: Bee bread/nectar invisible in hive inspection. Three rendering and simulation bugs found and fixed: (1) RENDERING: FrameRenderer.STATE_COUNT was 14 but there are 15 cell states (0-14). S_BEE_BREAD (state 14) was clamped to S_VACATED (13) in both LOD and honeycomb render modes, making bee bread cells completely invisible. Fixed: STATE_COUNT=15, added bee bread color to LOD_PALETTE (warm amber-orange). (2) CELL ATLAS: Atlas was 364x20 px (14 sprites). Created 14_bee_bread.png sprite (warm golden-orange hex cell with lighter center highlight) and rebuilt cell_atlas.png to 390x20 px (15 sprites). (3) INIT BUG: init_as_overwintered() set pollen_stores=1.5 but never created any S_BEE_BREAD cells in the frames. NurseSystem reads bee bread from actual cell counts, so on first tick the weight was immediately zeroed with no backing cells. Fixed: now seeds ~25 bee bread cells in drawn frames 3-6, just outside the brood radius (realistic pollen ring placement). pollen_stores is now derived from actual seeded cells. Package bee init also corrected (pollen_stores=0.0 since all frames are foundation). All ASCII-verified. Protected files verified: GDD 558,285 bytes, DevPlan 68,801 bytes.

[2026-03-29 CDT] Super honey fill visual feedback on overworld hive. Added per-super visual data pipeline so the overworld hive sprites visually communicate honey production state. (1) NEW: HiveSimulation.get_super_visual_data() -- scans all super boxes and returns fill_pct, capping_pct, and drawn_pct per super. (2) MODIFIED: SnapshotWriter now includes "super_visuals" array in the per-tick snapshot dict. (3) NEW: hive.gd super fill tinting system -- each super sprite gets a graduated tint from grey (empty/undrawn) through pale yellow (nectar arriving) to warm gold (honey curing) to rich amber (mostly full) to deep amber (fully capped and harvest-ready). Tints update every tick and layer on top of the existing health tinting. _rebuild_sprite_stack() now tracks which child sprites correspond to which super data index. (4) MODIFIED: stat label now shows per-super fill% and capping% when supers are present (e.g. "S1:45%f/20%c"). All ASCII-verified. Protected files verified: GDD 558,285 bytes, DevPlan 68,801 bytes.

[2026-03-29 CDT] UI fixes: button overlap + inspector nectar/beebread. (1) FIX: Dev +Day/+Month buttons overlapping the Tab info panel -- both were positioned at y=18. Moved dev button panel to y=84, below the info panel area (energy bar + info panel spans y=3 to y=80). (2) FRAME INSPECTOR: Added nectar, curing honey, and bee bread cell counts to all inspection tiers. Tier 3 (qualitative): shows "Flow on/Some nectar/Trickle/No nectar" for incoming nectar and "Good pollen/Some pollen/Low pollen/No pollen" for bee bread. Tier 4 (approximate): shows nectar (fresh+curing combined) and bee bread with qualitative labels (Heavy/Some/Light/None and Good/Some/Low/None). Tier 5 (master): shows exact percentages for nectar, curing, honey, and bee bread. Dev mode: shows raw counts and percentages for all cell types including Nectar, Curing, and Bread rows with color-coded labels (green-yellow for nectar, amber for curing, orange-brown for bee bread). Increased stats label count from 18 to 21 to accommodate the 3 extra rows. All ASCII-verified. Protected files verified: GDD 558,285 bytes, DevPlan 68,801 bytes.

[2026-03-29 CDT] Hive frame logo concepts PDF. Generated 4 new logo variations based on Nathan's uploaded "Smoke and Honey" design, replacing the bee globe element with a rectangular wooden beehive frame filled with golden capped honeycomb. Prompt-only generation (no Content Reference -- browser extension permission issue). All 4 variations at 1440x1440 via Leonardo AI (Lucid Origin, Dynamic, Fast). Compiled into 6-page PDF presentation: title page, 2x2 grid overview, and 4 full-size individual pages. Saved to research/design/Smoke_and_Honey_HiveFrame_Logos.pdf (2.3 MB). Protected files verified: GDD 558,285 bytes, DevPlan 68,801 bytes.

[2026-03-29 CDT] FIX: Honey cells not being deposited despite positive forecast. Three bugs found and fixed: (1) _sync_honey_to_frames() only counted existing honey cells on outer brood frames (indices 4-9 in queen order), but deposits also went to inner frames via the fallback path -- this caused the count to underestimate existing cells, leading to over-deposit followed by negative delta removal next tick. Fixed: now counts ALL brood box frames. (2) _overflow_honey_to_supers() was double-depositing into supers on top of _sync_honey_to_frames()'s own super overflow, pushing cell count above what honey_stores warranted and causing oscillating add/remove cycles. Fixed: disabled redundant overflow function since _sync already handles super overflow. (3) Added diagnostic prints for NU allocation decision (stores-first threshold) and honey sync (target vs existing vs deposited) to help debug future issues. Protected files verified: GDD 558,285 bytes, DevPlan 68,801 bytes.

[2026-03-29 CDT] NU ALLOCATION: Stores-first, then wax, then storage. Replaced blind 50/50 NU split with realistic bee behavior: (1) Daily consumption subtracted from honey_stores. (2) If stores < 14 days of consumption: ALL NU goes to honey stores (survival mode, no wax). (3) Once 2-week stores are secure: NU goes to wax first (capped at 50%), remainder to storage. In fall with queen slowing down, goldenrod/aster flow fills winter stores before any wax drawing. Forecast function updated to match. Protected files verified: GDD 558,285 bytes, DevPlan 68,801 bytes.

[2026-03-29 CDT] FARMSTEAD TREES: Added 10 SeasonalTree nodes to home_property providing real forage from Day 1. Iowa farmstead layout: 2x willow (creek-side, month 0, nectar=30 pollen=50), 1x silver maple (yard shade, month 0, nectar=8 pollen=20), 2x wild plum (fenceline, months 0-1, nectar=24 pollen=20), 1x cottonwood (windbreak, months 0-1, pollen=12), 1x apple tree (farmhouse, month 1, nectar=20 pollen=15), 1x basswood/linden (south, months 2-3, nectar=60 pollen=15), 1x elm dead (atmosphere, no forage), 1x sycamore (near buildings, months 0-1, pollen=8). Trees use existing SeasonalTree system -- auto-join "trees" group, queried by HiveSimulation._get_zone_nu/pu(). Removed ambient forage floor hack from HiveSimulation (was temporary workaround). Honey curing pipeline verified: S_NECTAR (3d) -> S_CURING_HONEY (5d) -> S_CAPPED_HONEY (7d) -> S_PREMIUM_HONEY, processed on both frame sides via CellStateTransition. Protected files verified: GDD 558,285 bytes, DevPlan 68,801 bytes.

[2026-03-29 CDT] SIMULATION FIX: Wax drawing and honey storage not working. Root cause: zero forage for first 27 days of game. No flowers bloom until day 28 (dandelions start day 21 + 7 days to reach PHASE_MATURE), and home_property has no trees in "trees" group. With zone_nu=0, nu_for_wax=0 (no comb drawing) and nu_for_honey=0 (no honey accumulation). Colony burned through 4 lbs starting stores with no income. Three fixes applied: (1) AMBIENT FORAGE FLOOR in _get_zone_nu() and _get_zone_pu() -- adds baseline NU/PU per month representing unmodeled natural forage (early tree buds, weeds, road flowers). Month 0: 80 NU/60 PU, Month 1: 120/80, Summer: 60/40, Fall: 30/20, 20/10. Real colonies forage on willows/maples from day 1. (2) HONEY DEPOSIT FALLBACK -- _sync_honey_to_frames() now falls back to depositing nectar on inner brood frames (order_idx 3,2,1,0) when outer honey frames are all foundation. Real bees store honey in corners of brood frames too. (3) ENHANCED DEBUG OUTPUT -- 7-day diagnostic now shows NU_in breakdown (total, wax split, honey split), brood box honey cell count, nectar cell count, and all previous stats split across two lines for readability. Protected files verified: GDD 558,285 bytes, DevPlan 68,801 bytes.

[2026-03-29 CDT] Fix: dev mode cell info tooltip in inspection overlay. The mouse-following cell info tooltip (shows coords, state, age, comb type, 3D distance in dev mode) was not appearing when G-key toggled dev mode mid-inspection. Root cause: _apply_tier_visibility() updated stats sidebar but not the tooltip panel. If player was level 1 (tier 1), tooltip started hidden and _on_grid_mouse_entered wouldn't re-fire for an already-hovering mouse. Fix: _apply_tier_visibility() now explicitly sets _tooltip_panel.visible based on tier >= 2. Protected files verified: GDD 558,285 bytes, DevPlan 68,801 bytes.

[2026-03-29 CDT] Feedback logos v2 -- corrected smokers and ampersand. Regenerated all 4 community feedback logo sets with two fixes: (1) replaced "and" with "&" in all title text ("Smoke & Honey"), (2) all smokers now explicitly described as traditional beekeeping bellows hive smokers (cylindrical metal canister with cone nozzle and hinged leather bellows). Same 4 categories: Nathan's 2A Iteration, RogoGal's Oval Sign Mashup, RogoGal's Beekeeping Co. Badge, swife's Simple Smoker & Jar. 16 new images total via Leonardo AI (Lucid Origin, 1440x1440). Updated Smoke_and_Honey_Feedback_Logos.pdf with square 1:1 image layout. Protected files verified: GDD 558,285 bytes, DevPlan 68,801 bytes.

[2026-03-29 CDT] Starting inventory trimmed. Player now starts with only hive tool, gloves, queen excluder (x1), and supers (x5) in personal inventory. All other items (hive stands, deep bodies, frames, lids, package bees, seeds, beehives, deep boxes, jars, comb scraper) moved to the storage chest with consolidated quantities. Protected files verified: GDD 558,285 bytes, DevPlan 68,801 bytes.

[2026-03-29 CDT] Dev mode: +Month button (v3 -- crash fix + layout fix). Two bugs fixed: (1) CRASH: called nonexistent NotificationManager.show_toast() -- changed to correct API NotificationManager.notify(). (2) CRASH: create_timer(0.0) fired too fast for signal handlers -- replaced with proper Timer node (0.05s interval = ~1.4s for 28 days) giving each day a real engine frame for weather, flowers, hive ticks, notifications to process. (3) OVERLAP: legacy NextDayButton scene node ("Test Button") was being toggled visible alongside custom buttons -- now kept hidden. Buttons stacked vertically (+Day on top, +Month below). Button shows countdown ("27...", "26...", etc.) and disables during advance. Protected files verified: GDD 558,285 bytes, DevPlan 68,801 bytes.

[2026-03-29 CDT] Community feedback logo iterations PDF. Generated 4 new logo sets based on Discord feedback (Daniel, Abby, Aadyn, RogoGal, swife): (1) Nathan's 2A Iteration -- smoker added behind logo in bottom-left, smoke wisp removed from bee; (2) RogoGal's Oval Sign Mashup -- 1C+1B combined oval wooden sign with detailed smoker/jar icons; (3) RogoGal's Beekeeping Co. Badge -- 3A variant with "Beekeeping Co." on bottom arc; (4) swife's Simple Smoker & Jar -- clean minimalist smoker and honey jar design. All generated via Leonardo AI (Lucid Origin, 1440x1440, 4 images each = 16 total). Compiled into Smoke_and_Honey_Feedback_Logos.pdf (5-page presentation: title + 4 category pages). Protected files verified: GDD 558,285 bytes, DevPlan 68,801 bytes.

[2026-03-29 CDT] Mouse drag-and-drop for Storage Chest. Added click-and-drag item transfer to the chest storage UI (chest_storage.gd). Left-click a slot to pick up the full stack; right-click picks up 1 item. While holding, a floating icon follows the mouse showing item sprite and count. Click a destination slot to drop: empty slot = place, same item = merge stacks, different item = swap (full stack only). Right-click while holding drops 1 item into the target slot. ESC or clicking outside cancels drag and returns items to source. Closing the chest also safely returns any held items. Mouse hover auto-syncs the keyboard cursor position so both input methods stay in sync. Existing keyboard controls (WASD + E + Q) preserved unchanged. Updated hint bar text. All ASCII-verified. Protected files verified: GDD 558,285 bytes, DevPlan 68,801 bytes.

[2026-03-29 CDT] MAJOR: Outdoor Harvest Yard with 3 minigames. Implemented the complete Level 1 manual honey processing system as an outdoor yard on the home property. (1) NEW: scripts/world/harvest_yard.gd -- Main controller for 5 outdoor stations (Super Pallet, Scraping Station, Scraped Frame Pallet, Honey Extractor, Bottling Table + white honey bucket). Placeholder colored-rect visuals for each station. Manages full pipeline state: frames on pallet, scraped frames, honey in bucket, jars on table. Stations use proximity detection with dynamic prompt labels. Player interacts via E key (integrated into player.gd action priority system as Priority 0). (2) NEW: scripts/ui/scraping_minigame.gd -- Interactive de-capping overlay. 24x14 cell grid per frame side. Click+drag scraper (3-cell brush width) across honeycomb. 95% uncap chance per cell normally, drops to 80% when side is 90%+ done. Both sides required. Auto-flips after Side A complete. (3) NEW: scripts/ui/extractor_minigame.gd -- Tap-E gauge overlay. Vertical speed gauge decays at 1.8/sec, each E tap adds 0.15. Must keep gauge above 60% red threshold line to accumulate extraction progress (0.055/sec). ~20 seconds sustained effort to complete. Visual feedback: green gauge = progressing, red gauge = tap faster. (4) NEW: scripts/ui/bottling_minigame.gd -- Jar filling overlay. Press E to toggle honey gate open/closed. Honey fills jar visually from bottom up (0.6 fill/sec). Auto-stops when full, places jar on table. Jars stack in columns of 10 on visual table display. 40 jars max per session. Unused jars returned to inventory on ESC. (5) MODIFIED: scripts/core/player.gd -- Added harvest yard station check as Priority 0 in _perform_action(). Added ITEM_COMB_SCRAPER to starting chest inventory. (6) MODIFIED: scripts/world/home_property.gd -- Spawns HarvestYard node at (480, 350) in World. Scene bounds expanded from 940 to 1640 width. Right exit pushed +600px right (offset parameter) to make room for harvest yard. Harvest Yard registered as map POI. (7) MODIFIED: Smoke_and_Honey_GDD.html -- Updated Section 6.6 Manual Harvest Equipment (Tier 0) to document all 5 stations, 3 minigame mechanics, station layout, equipment items, and updated processing flow. All scripts ASCII-verified, no := with ternary/as patterns. Protected files verified: GDD 558,285 bytes, DevPlan 68,801 bytes.

[2026-03-29 CDT] HUD cleanup -- minimal UI by default. (1) Hid the top bar (season/date/time/weather) by default for a cleaner gameplay view. (2) Added honey-themed energy bar in upper-left corner, always visible -- 52x10px bar with golden amber fill that shifts from bright golden (full) to deep amber (mid) to dark burnt honey (low), decorative honey drip at fill edge, honeycomb-style border, highlight stripe for depth. (3) Added Tab-toggle info panel (140x62px overlay below energy bar) showing: season icon + month + day, time + weather, money + honey count, energy %, level + title + XP bar, and key hints. Panel appears/hides on Tab press. All signal handlers (energy, money, time, weather, day advance) now also refresh the info panel when visible. (4) Tab key hint shown below energy bar so players discover the toggle. The hotbar (inventory toolbar) remains always visible at screen bottom. All ASCII-verified. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-29 CDT] Logo design phase v2. Regenerated all 4 logo concept categories using Leonardo AI (Lucid Origin model, 1440x1440, 4 images each = 16 total). Categories: (1) The Homestead Label -- vintage oval honey jar label with smoker/jar flanking title; (2) Smoke & Drip -- whimsical text formed by smoke curls and honey drips with bee ampersand; (3) The Woodburned Badge -- circular woodcut stamp/badge with rope border detail; (4) The Shelf -- cozy windowsill scene with smoker and honey jar. Updated logo_options.html with all new generation URLs and v2 subtitle. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-29 CDT] Dev mode global toggle + music persists through pauses. (1) GameData.gd: changed G-key handler from _unhandled_input to _input so it fires before any scene's input handlers. Dev mode now toggles from any screen (inspection overlay, shop, chest, pause menu, etc.). (2) InspectionOverlay.gd: connects to GameData.dev_labels_toggled signal on open. New _on_dev_toggled() handler recalculates inspection tier and refreshes stats/frame display live when dev mode is toggled mid-inspection. (3) MusicManager.gd: set process_mode = PROCESS_MODE_ALWAYS so background music continues playing when the scene tree is paused (chest, shop, sell screen, pause menu). Previously music would stop when any overlay paused the tree. All ASCII-verified. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-29 CDT] Super frame inspection display fix. Honey super frames (70x35) now display at their correct proportions in the inspection overlay instead of being stretched to fill the deep frame (70x50) cell area, which left a large black gap at the bottom. (1) FrameRenderer.gd: render_honeycomb() now dynamically sizes the canvas based on actual frame.grid_rows instead of hardcoded FRAME_ROWS=50. Added honeycomb_px_w() and honeycomb_px_h() static helpers for callers. Canvas is recreated when switching between deep/super frame types. (2) InspectionOverlay.gd: added _resize_for_frame_type(is_super) which rescales the entire Langstroth frame display (top bar, bottom bar, side bars, foundation, wires, cell rect, bee overlay, harvest overlay) proportionally for 35-row super frames and centers the shorter frame vertically within the GRID_H space. Called from _refresh_frame() when box type changes. Mouse-to-cell tooltip mapping and _viewport_to_canvas() now use dynamic frame dimensions instead of hardcoded constants. All ASCII-verified. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-29 CDT] Honey production rebalance -- 40-60 lbs/year target. Reduced NU-to-honey conversion from 0.001 to 0.0005 lbs per Nectar Unit. Added named constant LBS_PER_NU (replaces magic number in tick() and forecast_tomorrow()). Sweep tested across 10 conversion rates: 0.0005 produces ~50 lbs harvestable super honey for an S-rank Carniolan queen in a good year with one brood body, squarely in Nathan's 40-60 lb target. Colony still builds up properly, fills 1-2 supers, and retains enough brood box stores for winter. All ASCII-verified. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-29 CDT] Storage chest now shows actual item sprites. Replaced solid-color placeholder rectangles in chest_storage.gd with real item sprite icons. Each slot now contains a TextureRect child that displays the item's 32x32 sprite scaled to the 14x14 slot via STRETCH_KEEP_ASPECT_CENTERED. Texture loading mirrors hud.gd's _load_item_textures() -- maps 27 GameData item constants to assets/sprites/items/ PNGs. Falls back to the original color-tint for any item without a matching sprite file. Both the 50-slot chest grid and the 10-slot player inventory row use the same sprite system. All ASCII-verified. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-29 CDT] Dev mode hive inspection forecast. Added "TOMORROW" forecast section to the dev mode inspection stats sidebar. When inspecting a hive with dev labels on (G key), the right-side stats panel now shows 5 additional rows below the existing 12: a divider header, then predicted +Wax cells (new comb drawn from foundation), +Bread (net bee bread cells stored/consumed), +Honey (net honey cells deposited), and +Eggs (queen laying estimate). Forecasts use expected-value averages of the same formulas as tick() -- zone NU/PU, forager collection, 50/50 NU split, nurse feeding priority, queen laying modifiers -- without random rolls or state mutation. (1) HiveSimulation.gd: added forecast_tomorrow() function (~80 lines) returning {wax_cells, bee_bread_cells, honey_cells, eggs_laid}. (2) InspectionOverlay.gd: expanded stats label count from 12 to 18 (extra rows use 8px spacing to fit in panel), _build_dev_rows() now calls forecast_tomorrow() and appends color-coded forecast rows (green for positive, red for negative bee bread, muted for zero). All ASCII-verified. No := with ternary/as. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-28 CDT] Carniolan S-rank, delayed bloom, barrel feeder, tree rebalance. (1) ALL DEFAULT QUEENS changed to Carniolan S-rank: HiveSimulation default queen dict, init_as_overwintered (now respects p_grade param), init_as_package (default Carniolan), home_property starter hive, hive.gd defaults, all fallbacks in BeeSwarmCloud/SnapshotWriter/BeeOverlay. (2) DANDELION BLOOM DELAYED from day 1 to day 21-28 (quality-dependent: EXCEPTIONAL=21, POOR=28). Extends to day 64 (Wide-Clover day 8). Updated flower_lifecycle_manager, dandelion_spawner (quality-based timing + day-based despawn), ForageManager month range. (3) BARREL FEEDER: new placeable item, 100 NU/day for 10 days, $25. barrel_feeder.gd world script, player placement/pickup, zone NU injection in HiveSimulation._get_zone_nu(). (4) TREE FORAGE REBALANCE: boosted NU/PU to sustain S-rank queen -- willow 15/25, maple 8/20, plum 12/10, apple 20/15, cherry 18/15, pear 18/15, basswood 60/15, cottonwood 0/12, sycamore 0/8. Month 0 trees: 35 NU + 55 PU. Month 1 trees: 68 NU + 52 PU. (5) TYPE FIX: var idx := to var idx: int = in HiveSimulation lines 841/959/1254/1431. (6) LBS_PER_FULL_SUPER: 3.5->4.0 (40 lbs per 10-frame super). (7) S-rank queen laying: 1500-2000 eggs/day (comments updated in QueenBehavior grade table). All ASCII-verified. Protected files: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-28 CDT] Level 1 Harvest Loop + Bug Fixes. HARVEST LOOP: Implemented complete honey extraction pipeline in Honey House. (1) GameData.gd: added ITEM_COMB_SCRAPER and ITEM_HONEY_BUCKET constants. (2) item_registry.json: added comb_scraper ($12 tool) and honey_bucket (material). (3) InspectionOverlay.gd: added frame harvest marking system -- H key marks individual frames, Shift+H marks entire super, fermentation warning for <60% capped frames, gold overlay + capping % display, updated footer. (4) honey_house.gd (NEW): 5-station pipeline controller -- Super Prep (break into 10 frames, return empty super box), Frame Holder (staging), Uncapping Station (1 energy/frame, beeswax collection), Honey Spinner (button-mash E for 20s, 10 frames batch), Canning Table (1 jar + 1 lb honey = honey jar). Dynamic station labels, proximity prompts, progress bar for spinner. (5) honey_house.tscn: rewired from generic_interior.gd to honey_house.gd, room expanded 12x10. BUG FIXES: (A) Hive height not updating when supers added -- _rebuild_sprite_stack() used queue_free() which doesn't remove children immediately; changed to remove_child()+free(). (B) No bees visible outside hive -- _ready() was missing _ensure_swarm_cloud() call for COMPLETE+colony_installed hives. (C) Cannot inspect hive -- replaced silent print() calls with NotificationManager.notify() so player sees feedback ("Select Hive Tool", "Colony establishing", "Too tired"). (D) hive_management.gd _on_add_super() consumed ITEM_SUPER_BOX before checking try_add_super() return value; now refunds item if super fails to add. All files ASCII-verified. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-28 CDT] Honey selling system. Created sell_screen.gd + sell_screen.tscn: simple confirm dialog for selling honey jars. Player sees buyer name, price per jar, current jar count, and adjusts quantity with A/D before confirming with E. Two selling locations: (1) Tanner's Feed & Supply -- "Sell Honey to Carl" button added to shop UI, Carl buys at $8/jar any day; (2) Saturday Market in Cedar Bend -- Frank Fischbach buys at $10/jar on Saturdays (spring-fall). Added proximity-based "[E] Sell Honey" hint near Frank at market. Sales award XP (2 per $10 earned) and +1 Community Standing per transaction. Sales logged to expense_log as "Honey Sale" with negative amount (income). All scripts ASCII-verified, no := with ternary/as patterns. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-28 21:30:00 CDT] MAJOR: Complete NU/PU discrete economy rewrite. Replaced float-based forage_pool system with Nathan's 9-rule discrete unit economy. (1) ForagerSystem.gd: completely rewritten -- each forager collects 2-6 NU and 1-2 PU per day (discrete integers), zone resources divided by colony count, daily variance 80-120%, congestion penalty only at state 3 (25%). (2) NurseSystem.gd: completely rewritten -- 2 actions per nurse per night: Priority 1 feeds brood with PU (incoming first, then bee bread reserves), Priority 2 stores excess PU as bee bread. Returns brood_fed, brood_stunted, pu_stored, capping_delay, rjelly_surplus. (3) CellStateTransition.gd: added S_BEE_BREAD (state 14), STATE_COUNT bumped to 15. Bee bread cells store up to 3 PU using cell_age field. (4) HiveSimulation.gd tick() completely restructured: zone NU/PU queried from FlowerLifecycleManager + trees, ForagerSystem collects discrete NU/PU, NurseSystem runs 2-action night, NU split 50/50 between wax production and honey storage, _draw_comb_with_nu() converts NU to comb (3 cells per NU), overflow honey to supers when brood box full, super comb drawn once brood >80% complete. New helpers: _get_zone_nu(), _get_zone_pu(), _store_bee_bread(), _consume_bee_bread(), _count_bee_bread_cells(), _draw_comb_with_nu(), _overflow_honey_to_supers(). (5) Queen excluder implemented: has_excluder flag checked in _queen_lay() -- queen cannot lay in supers when excluder installed, without excluder she can but prefers brood box (distance penalty). (6) All starting queens forced to S-rank (grade "S") on init_as_package, init_as_overwintered, and defaults. Package colony honey_stores raised 2.0->4.0, pollen_stores 0.3->0.5. Removed dead _draw_comb() function (replaced by _draw_comb_with_nu()). All files ASCII-verified. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-28 20:45:00 CDT] MAJOR: Honey supers now functional + colony buildup tuning. Three critical bugs in HiveSimulation.gd prevented supers from ever filling: (1) CellStateTransition only processed brood box frames -- super frames never aged (nectar never cured/capped). Fixed: tick() now iterates ALL boxes, but only counts emerged workers/drones from brood box. (2) _sync_honey_to_frames() only deposited honey into brood box. Fixed: rewrote to overflow into super frames once brood box honey frames fill up, matching real bee behavior. Extracted helpers: _count_honey_cells_in_frame(), _deposit_nectar_in_frame(), _remove_honey_from_frame(). (3) Comb drawing only operated on boxes[0]. Fixed: added _draw_super_comb() that draws comb in super boxes once brood box is >80% drawn, at 60% of brood rate. Also tuned package colony early-game: raised starting honey_stores from 2.0 to 4.0 lbs (simulates standard beekeeper syrup feeding), raised pollen_stores from 0.3 to 0.5, boosted comb drawing store_mult floor from 0.20 to 0.50 at low stores (real package colonies fed syrup draw comb aggressively). Added weekly diagnostic print to console showing population, honey stores, comb %, super honey cells. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-28 20:22:00 CDT] Fixed dev mode G-key double-toggle bug. Pressing G appeared to do nothing because both player.gd (_input) and GameData.gd (_unhandled_input) were each calling toggle_dev_labels(), causing dev mode to flip ON then immediately back OFF on the same keypress. Fix: removed KEY_G handler from player.gd _input() since GameData handles it globally via _unhandled_input(). Player now reacts to the dev_labels_toggled signal instead (for grid overlay sync). Dev mode +Day button and midnight auto-advance should now work correctly for rapid day testing. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-29 01:00:00 CDT] KARPATHY CONSTANTS PORTED TO GDSCRIPT. All 8 research phases pass validation -- ported research-validated constants into game scripts. Changes: (1) PopulationCohortManager.gd: summer mortality rates updated to S-tier validated values (nurse 0.005->0.0023, house 0.008->0.0038, forager 0.055->0.037); winter mortality rates increased for realistic cluster dynamics (nurse/house 0.003->0.018, forager 0.004->0.030); (2) TimeManager.gd: Quickening season factor 0.65->0.55 (validated by Phase 2+8); (3) QueenBehavior.gd: complete rewrite -- grade system now S/A/B/C/D/F with S=1.00 baseline (was A+/A/B/C/D with B=1.00), added queen_age_multiplier (peaks year 2, declines after), congestion_laying_modifier, varroa_laying_modifier; (4) ForagerSystem.gd: congestion penalty now only applies to FULLY_CONGESTED (state 3) at 25% (was 40% on states 2+3); (5) HiveSimulation.gd: _grade_modifier and _species_seasonal_modifier now delegate to QueenBehavior static functions, _queen_lay() now applies age curve + varroa + congestion modifiers (Phase 4+8 validated), queen base laying_rate 1500->2000 (grade modifier scales it: B=0.70x2000=1400 effective); (6) CellStateTransition.gd: DAYS_CURING_TO_CAPPED 4->5 (Phase 1 validated); (7) ForageManager.gd: PLANT_DATA expanded with Phase 6 Iowa native species (black_locust, prairie_clover), bloom windows aligned with research day-ranges; (8) WeatherManager.gd: Foggy forage multiplier 0.5->0.4 (Phase 6). HiveHealthCalculator.gd and CongestionDetector.gd already matched research -- no changes needed. All files verified ASCII-only. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-28 23:30:00 CDT] Hive buzzing ambient sound. Added proximity-based bee buzz audio to hives: generated a 3-second looping bee hum WAV (195 Hz fundamental with 5 harmonics, pitch warble, and amplitude modulation for organic feel) at assets/audio/sfx/bee_buzz_loop.wav. Modified scripts/world/hive.gd: added AudioStreamPlayer routed to SFX bus with custom exponential volume curve -- silent beyond ~10 tiles (320px), ramps up sharply via cubic falloff (pow(t, 3.0)) as player approaches, full volume at 16px. Buzz only plays when colony_installed is true; starts automatically via activate_simulation() and all colony placement paths (place_as_complete, place_as_overwintered, install_colony). Loop mode set to LOOP_FORWARD on the AudioStreamWAV with finished signal fallback. Volume updated every frame in _process() alongside existing prompt-label distance check (shared player cache). Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-28 23:15:00 CDT] Pause menu with audio settings. Rewrote scripts/ui/PauseMenu.gd: ESC or P now properly toggles the menu open AND closed (previously only closed). New buttons: Resume, Save, Settings, Quit Game. Settings sub-panel has three sliders -- Music volume, Effects (SFX) volume, and Master volume -- all controlling Godot AudioServer bus volumes on a logarithmic dB curve (0-100% mapped to -80dB to 0dB). PauseMenu auto-creates "Music" and "SFX" audio buses at runtime if they don't exist (no default_bus_layout.tres needed). Routes MusicManager's AudioStreamPlayers to the "Music" bus. Quit Game calls get_tree().quit(). process_mode = PROCESS_MODE_ALWAYS so tweens and sliders work while paused. Warm GDD amber palette preserved. MusicManager.gd updated to route to "Music" bus when available. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-28 23:00:00 CDT] Fixed game crash on launch (second attempt). Actual root cause: scenes/home_property.tscn was truncated/corrupted -- 150,553 bytes ending mid-word instead of the full 154,264 bytes. Godot could not parse the incomplete scene file and exited immediately. Restored from git history (previous good commit). Also added research/.gdignore so Godot stops scanning the research/ directory (which contained 89 orphaned .import files from queenFinder R&D with no matching source PNGs). Cleared uid_cache.bin and global_script_class_cache.cfg again for clean rebuild. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-28 22:30:00 CDT] Fixed game crash on launch (window opens then immediately closes). Root cause: orphaned resource files left over from earlier refactors were causing Godot's startup scanner to fail silently. Removed 4 orphaned files: (1) assets/sprites/npc/uncle_bob_portrait.png.import -- referenced a deleted PNG with no cached .ctex, (2) scripts/managers/TimeManager.gd.uid -- pointed to old pre-autoloads location, (3) scripts/world/calders_bluff.gd.uid -- script no longer exists, (4) scripts/world/test_environment.gd.uid -- script no longer exists. Cleared stale Godot caches: .godot/uid_cache.bin and .godot/global_script_class_cache.cfg (both will regenerate cleanly on next launch). Removed empty scripts/managers/ directory. Full static analysis performed: all 10 autoload scripts verified, all .tscn ext_resource paths confirmed, all preload() targets exist, no non-ASCII characters in any .gd file, no := with ternary/as patterns, no class_name cycles, no zero-byte files. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-28 21:00:00 CDT] MusicManager autoload -- seasonal background music. Created scripts/autoloads/MusicManager.gd: plays one MP3 track per month (8 tracks total, mapped to month index 0-7), loops each track on finish, crossfades over 2 seconds when TimeManager.month_changed fires. Tracks: 1_Quickening_theme, 2_Greening_Theme, 3_Wide-Clover_Theme, 4_High-Sun_Theme, 5_Full-Earth_theme, 6_Reaping_Theme, 7_Deepcold_Theme, 8_Kindlemonth_Theme (all in assets/audio/seasonalMusic/). Uses two AudioStreamPlayers for smooth crossfading between months. Default volume -6dB. Registered as autoload in project.godot after SceneManager. Music starts immediately on game launch matching the current month. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-28 20:30:00 CDT] Karpathy Research Machine v5 -- smart diagnostics and thermal monitoring. Major improvements to main.py research loop: (1) FAILURE DIAGNOSTICS: when find/replace fails, the loop now diagnoses WHY (hallucinated code, whitespace mismatch, partial match) and prints ">> WHY:" with the specific reason, plus ">> NEXT STRATEGY:" explaining what the loop will try differently; (2) LOCKED CONSTANT ENFORCEMENT: before applying any edit, checks if AI is trying to modify science-locked constants (EGG_DURATION, LARVA_DURATION, etc) and REJECTS the change with a clear message, telling the AI to find other parameters instead; (3) FAILURE FEEDBACK LOOP: failure reasons are fed back into the next Ollama prompt via last_failure_context so the AI learns from mistakes (e.g., "your FIND text didn't match -- read the actual code more carefully"); (4) THERMAL MONITORING: reads CPU temp via WMI and GPU temp via nvidia-smi before each attempt, pauses 30s if temps exceed 90C CPU or 88C GPU, gentle 3s pacing between all attempts to prevent overheating during long overnight runs; (5) EDGE CASE FIX: phases where all test lines pass but script exits non-zero (import error, etc) are now detected and treated as locked instead of entering an infinite research loop on 0 failures. Prompt improvements: locked constants section is now more prominent with explicit "Any attempt to modify these will be REJECTED automatically", FIND block instructions emphasize "copy PRECISELY from the code above". Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-28 19:00:00 CDT] Karpathy Research Machine v4 -- autoresearch methodology. Rebuilt main.py to follow Karpathy's autoresearch pattern (github.com/karpathy/autoresearch): (1) NEVER STOPS -- all loops run indefinitely until tests pass or Ctrl+C, no max iteration caps; (2) results.tsv structured experiment log (timestamp, phase, attempt, pass_count, total, status, target_file, description) for reviewing overnight experiment history; (3) accept/reject/revert cycle with single metric (test pass count); (4) backups/ folder preserves every file version before modification. Removed git commit integration per user preference -- all tracking is local via results.tsv + backups. Architecture mirrors autoresearch: prepare=CONTEXT.md (fixed), train=phase scripts (AI-modified), program=Ollama prompts (built by main.py), metric=test pass count. Bottom-up progression: Phase 1->2->...->7 each perfected then locked, Phase 8 integration fixes SOURCE phases not full_sim.py. CLI: default=full pipeline, --skip-validate, --status, --dry-run, --model, --start-at N. Current status: P1/P4/P5/P6/P7 all pass, P2 (8/11), P3 (11/13), P8 (1/15). Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-28 18:30:00 CDT] Karpathy Research Machine -- main.py entry point. Created main.py and main.ps1 in research/karpathy/ as the single entry point for the entire research pipeline. Pipeline: (1) validates all 7 phases pass their own tests, (2) runs Phase 8 integration (15 tests), (3) launches automated research loop if any integration tests fail. Modes: default=full pipeline, --skip-validate=straight to loop, --status=show test results, --dry-run=show AI suggestions only, --model=choose Ollama model. Phase validation correctly catches Phase 2 (3/4) and Phase 3 (3/4) known minor failures. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-28 18:15:00 CDT] Karpathy Research Machine v3 -- fully automated research loop. Rebuilt research_loop.py as a proper Karpathy automated research loop: runs Phase 8 integration -> identifies earliest failing phase -> sends FULL phase source code + science context + failure data to Ollama -> AI analyzes the math/formulas/biology and proposes multi-line code changes -> applies changes -> validates the phase's own tests still pass -> re-runs Phase 8 to measure improvement -> reverts if regression -> loops until all 15 pass or Ctrl+C. Features: multi-line find/replace editing (not just single constants), indent-aware fuzzy matching, per-phase attempt history (max 8 attempts before moving to next phase), automatic revert on regression, backup of every file before edit (backups/ folder), research_log.txt tracking all iterations. run.ps1 modes: default=start loop, status=show test results, test=raw full_sim, dry=show AI suggestions without applying. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-28 17:45:00 CDT] Fixed all 8 run.ps1 scripts closing immediately on double-click. Added Read-Host "Press Enter to close..." at the end of every run.ps1 (phases 1-8) so the PowerShell window stays open and you can read the test results. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-28 17:35:00 CDT] Phase 8 Karpathy research loop files. Added CONTEXT.md (complete reference with all 30+ tunable constants, validation targets, known calibration issues, and tuning strategy) and research_loop.py (structured tuning reports with --baseline/--compare/--constants/--diff modes for AI-assisted iterative research). Updated run.ps1 with 6 run modes (default, loop, baseline, compare, constants, diff) and AI workflow instructions. Updated README.txt with full research loop workflow. Updated CONTEXT.md with actual test run data: colony starts at 24k adults but crashes to 3.2k by Wide-Clover instead of growing to 40-65k. Root cause: queen laying rate near-zero in early months due to multiplicative season*forage*stress dampening. Peak forage is 0.79 (adequate) but comes too late (High-Sun) while Quickening forage is only 0.025. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-28 17:00:00 CDT] Karpathy Research Machine -- PowerShell workflow. Added run.ps1 launcher scripts and README.txt instruction files to all 8 phase folders (research/karpathy/phase1-8). Each run.ps1: colored output, dependency checks (Phase 8 verifies all 7 prior phases exist), pass/fail messaging with next-step commands. Each README.txt: what the phase validates, how to run, specific stop criteria (test counts and targets), advancement command, what carries forward. Phase 8 README includes calibration strategy guide for the 1/15 integration test status. Updated karpathyResearch.html "Running the Scripts" section to full PowerShell workflow guide with per-phase stop/advance instructions and a "Run All Phases Sequentially" PowerShell snippet. Fixed truncated HTML closing tags. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-28 16:05:00 CDT] Karpathy Incremental Research Machine v2. Major overhaul of karpathy research approach -- rebuilt as 8-phase Python simulation system in research/karpathy/ directory. Each phase is a standalone runnable Python script that validates one biological subsystem against real-world science, with Phase N importing and building on Phase N-1. PHASES: (1) brood_sim.py -- CellStateTransition (21-day brood cycle, 7/7 PASS), (2) population_sim.py -- PopulationCohortManager (nurse/house/forager lifecycle, 3/4 PASS), (3) foraging_sim.py -- ForagerSystem+NectarProcessor (5:1 nectar-to-honey, harvest targets, 3/4 PASS), (4) queen_comb_sim.py -- QueenBehavior+NurseSystem (S-F grade system, age curve, comb drawing, 6/6 PASS), (5) disease_sim.py -- Varroa/AFB/EFB dynamics (doubling time, treatments, 6/6 PASS), (6) environment_sim.py -- WeatherManager+flower_lifecycle_manager (Iowa climate, 7 native species, 8/8 PASS), (7) colony_behavior_sim.py -- CongestionDetector+HiveHealthCalculator (health 0-100, swarm impulse, 7/7 PASS), (8) full_sim.py -- HiveSimulation full 10-step pipeline (1/15 PASS, TUNING NEEDED). Phase 8 integration reveals: subsystems are mathematically correct individually but calibration constants between systems compound -- environment-based forage yields much lower than simple seasonal mock. This is exactly what the Karpathy method is designed to expose. Updated karpathyResearch.html in project root with full test results, science references, directory structure, GDScript mapping table. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.

[2026-03-29 04:30:00 CDT] Tree and flower art rebalance. Normalized all 10 tree species to consistent, ratio-appropriate dimensions per species (all 4 seasonal variants now match within each species). Target sizes scaled relative to player (~32x60px), farmhouse (179x220px), and 32x32 tile grid: small trees (apple 96x128, cherry 100x130, pear 88x140, wild_plum 96x112), medium (silver_maple 112x128, basswood_linden 112x144), large (willow 112x160, sycamore 112x148, cottonwood 108x170), dead elm 96x160. Fixed major size inconsistencies: cottonwood summer was 249px tall (taller than farmhouse), elm dead 231px tall, pear winter only 69px wide vs 137 for summer (68px variance), basswood had 43px height variance between seasons. All sprites now bottom-center aligned on their canvas with NEAREST-neighbor scaling. Placed all 10 tree species as SeasonalTree instances in home_property.tscn: Willow(-160,120), SilverMaple(140,-10), Apple(60,300), Cherry(-100,220), Pear(480,300), WildPlum(-60,420), Basswood(580,80), Cottonwood(620,220), Sycamore(540,400), ElmDead(280,480). Fruit trees (apple, cherry, pear) have bloom_start/end set for spring blooming. Updated scene bounds in home_property.gd from Rect2(-100,-110,750,550) to Rect2(-240,-130,940,700) to encompass all tree positions. Flower sprites verified correct at 16x16 on 16x16 grid -- no changes needed. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.
[2026-03-28 CDT] Phase 2 Weather System (GDD S6.9, DevPlan Task 2.5). Full Iowa-based weather generation + visual effects. NEW FILES: (1) WeatherManager.gd (autoload) -- realistic weather generator calibrated to NOAA 1991-2020 Des Moines/Ames normals. 8 weather states: Sunny, Overcast, Rainy, Windy, Cold, HeatWave, Drought, Foggy. Season-weighted probability tables per game month (Quickening-Kindlemonth). Temperature model: monthly high/low ranges with triangle-distribution daily variance (+/-8F Iowa volatility). Multi-day drought tracking, spring rain forcing, auto-cold-snap on sub-50F rolls. Foraging multipliers per GDD: Sunny 1.0x, Overcast 0.7x, Rainy/Cold 0.0x, Windy 0.4x, etc. Inspection gating: Rain/Cold block hive opening; Windy/Drought increase sting chance. Save/load serialization via collect_save_data()/apply_save_data(). (2) weather_overlay.gd (CanvasLayer) -- scene tinting via CanvasModulate + semi-transparent haze ColorRect. Each weather state has unique tint color (rain=blue-gray, heat=warm amber, fog=pale gray, etc.) and overlay alpha. Smooth 2-second lerp transitions between weather states. (3) weather_particles.gd (Node2D) -- procedural rain/snow particle system using _draw() (no GPU particles, fits pixel art aesthetic). Rain: 120 drops, diagonal lines with wind drift, muted blue-gray colors, depth variation via shade parameter. Snow: 80 flakes, sinusoidal wobble drift, pixel rectangles. COLLISION SYSTEM: auto-scans scene tree for tree/bush/grass/roof nodes and registers them as occluders with catch rates (trees 60%, bushes 40%, grass 20%, roofs 90%). Raindrops hitting occluder rects have a chance to stop short and produce splash effects at canopy level. Splashes: expanding horizontal line, 0.15s lifetime, capped at 60 for performance. Rain becomes snow visually in winter months (month index >= 6). (4) weather_setup.gd (static helper, class_name WeatherSetup) -- one-call setup for exterior scenes. INTEGRATIONS: TimeManager.start_new_day() now triggers WeatherManager.roll_daily_weather(). HiveSimulation Step 4 (ForagerSystem) now multiplies season_factor by WeatherManager.get_forage_multiplier(). SaveManager serializes/deserializes weather state (current_weather, temp, wind, humidity, drought tracking). HUD top bar shows weather description with color-coded text. Dev mode panel expanded: shows current weather state + temperature, plus top-3 probability forecast for next day. WeatherSetup.add_to_scene() called from all 7 exterior scene scripts (home_property, county_road, cedar_bend, community_garden, harmon_farm, timber_creek, fairgrounds). Catastrophic events (tornadoes, severe blizzards) intentionally excluded per design. Art note: no new art assets needed -- system uses procedural drawing + color layers only, consistent with Leonardo AI pipeline requirements. All GDScript files verified ASCII-only, no := with ternary/as patterns. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.
[2026-03-29 03:30:00 CDT] Leonardo AI tree sprites -- all 9 remaining species. Generated seasonal sprite sheets for Silver Maple, Apple Tree, Cherry Tree, Pear Tree, Wild Plum, Basswood/Linden, Cottonwood, Sycamore, and Elm (dead/weathered) via Leonardo AI (Lucid Origin model, 1376x768, 16:9, Dynamic style). For each species: generated 3 results, selected the one most cohesive with existing willow art style (warm muted earthy palette, clean pixel art, good seasonal differentiation). Processing pipeline: beige background removal via color-distance thresholding, ground shadow cleanup via trunk-width detection, split into 4 seasonal PNGs (spring/summer/fall/winter), NEAREST-neighbor resize to ~40%. All 36 sprites (9 species x 4 seasons) copied to assets/sprites/environment/trees/ with naming matching seasonal_tree.gd tree_type keys (apple_tree, cherry_tree, pear_tree, basswood_linden, etc). Sycamore and elm_dead are new additions (previously had no sprites at all). All other species replace tiny 1-2KB placeholder sprites from Mar 26 with full Leonardo AI art (4-36KB each). ~100 Leonardo tokens used across all generations. Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.
[2026-03-29 02:00:00 CDT] Development Plan updated: Tasks 2.1-2.4 (Sleep/Day Advance step) marked DONE. Updated task descriptions with implementation details (bed interaction, house interior, nap bench, auto-save wiring). Exit criteria checkbox checked for "Player can sleep to advance the day and restore energy". Build order item 1 struck through as complete. Summary table row updated (2.1 -> [x]). Current state banner updated to reflect sleep/nap/auto-save completion. Bottom guidance text updated: next task is 2.5 (weather). Protected files verified: GDD 555,381 bytes, DevPlan 68,801 bytes.
[2026-03-29 01:30:00 CDT] Nap bench on home property (GDD S5.3/S5.4). Added outdoor bench near the farmhouse at (80, 20) that lets the player take a mid-day nap. Walk near the bench and press [E] to nap -- screen fades to black, time advances 2 in-game hours, 50% of max energy is restored, then fades back in. Does NOT advance to the next day, does NOT trigger a save (unlike sleeping in bed). Uses existing bench.png sprite from assets/sprites/world/props/. New script: scripts/world/nap_bench.gd -- self-contained Node2D with Sprite2D (runtime-loaded), StaticBody2D collision (prevents walk-through), Area2D interaction zone (48px radius), "[E] Nap" prompt label, and tween-based fade animation. NapBench node added to home_property.tscn under World. Registered as map POI ("Bench", light blue) in home_property.gd. Midnight clamp: if nap would push current_hour past 24.0, clamps to 23.5 to avoid triggering midnight logic mid-nap. Protected files verified: GDD 555,381 bytes, DevPlan 67,772 bytes.
[2026-03-29 01:00:00 CDT] Auto-save on bed sleep. SaveManager.save_game() now fires automatically when the player accepts the daily summary after sleeping in the bed -- both from house_interior.gd (interior bed interaction) and hud.gd (HUD summary overlay). Save captures end-of-day state BEFORE advancing to the next day. Dev mode advance-day button (G-key panel) intentionally does NOT save, so rapid testing never overwrites the player's real save. Console logs distinguish "[House] Game saved (bed sleep)" / "[HUD] Game saved (bed sleep)" from dev-mode advances. Protected files verified: GDD 555,381 bytes, DevPlan 67,772 bytes.
[2026-03-29 00:15:00 CDT] House interior bugfixes: (1) Player now spawns at the door (bottom of room) instead of the top. (2) Bed [E] Sleep now shows the full daily summary overlay (Day Complete panel with stats, date, "Begin Day N" button) -- previously skipped straight to day advance because the HUD CanvasLayer only existed in home_property.tscn. Built self-contained summary overlay in house_interior.gd matching the HUD version's style. (3) Fixed house exit re-entry loop -- player return position offset 40px south of the door zone so the player doesn't immediately walk back into the building trigger. (4) Moved dev mode G-key handler from player.gd _input to GameData.gd _unhandled_input so it works globally in every scene (player.gd still has the binding as fallback).
[2026-03-28 23:45:00 CDT] Tree forage system: SeasonalTree now produces NU and PU. Added TREE_FORAGE_DATA const with GDD-accurate nectar/pollen values for all 10 tree species (willow 2N/6P, silver_maple 1N/5P, wild_plum 2N/3P, apple/cherry/pear 3N/4P, basswood_linden 14N/4P, cottonwood 0N/3P, sycamore 0N/2P, elm_dead 0N/0P). Each SeasonalTree instance: exports nectar_nu, pollen_pu, forage_month_start, forage_month_end (auto-populated from TREE_FORAGE_DATA by tree_type); adds itself to "trees" group; exposes get_forage_contribution(month) and is_in_bloom() API. ForageManager updated: replaced hardcoded monthly willow/linden bonuses with _get_tree_forage() which queries all nodes in "trees" group and sums their per-month contributions. Tree NU divided by 10 to match flower tile density scale. Replaced willow sprite with Result 3 from original Leonardo generation (cascading curtain style). PU (pollen) allows queen to lay eggs; NU (nectar) allows wax and honey production -- trees now properly participate in both systems.
[2026-03-28 23:30:00 CDT] House interior overhaul: sprite-based room with bed interaction. Replaced procedural _draw() colored-rectangle rendering with Leonardo AI-generated pixel art background (house_interior_bg.png, 480x320). Room enlarged from 10x10 to 15x10 tiles (320x320 -> 480x320px). Background loaded as Sprite2D at runtime (bypasses import pipeline). Added bed interaction zone (Area2D, cols 11-13 rows 3-5) -- "[E] Sleep" prompt appears when player walks near the bed, pressing E triggers daily summary / day advance. Updated collision walls for new 15x10 dimensions. Door exit repositioned to column 7 (center of 15-tile width). Player spawn adjusted to (240, 80) for new room center. Scene file updated with new player position. Interior art includes: wood plank floor, brick fireplace, arched window, kitchen counter, table with chairs, potted plant, burgundy rug, bed with patchwork quilt, coat hooks, honey shelf, framed painting -- all baked into single background sprite. Door opening indicator painted into background at bottom center.

[2026-03-28 22:00:00 CDT] Development Plan Full Audit &amp; Reset. Reexamined GDD against actual codebase state (60 scripts, 29 scenes). Reset all Phase 2+ status markers based on what actually compiles and runs in-game -- not stubs or skeletons. Key changes: (1) Phase 2 status reset from "~20% partial" to "0% functional" -- GameData wallet/energy/XP fields exist (Phase 1 infrastructure) but no sleep action, no weather, no harvest, no market, no smoke/stings are implemented. (2) Converted vague task groupings into numbered, sequential build-order checklists (30 concrete tasks in Phase 2, numbered 2.1-2.30). (3) Added explicit dependency chains and "why this order" rationale. (4) Updated Current State banner to reflect actual counts (60 scripts, 29 scenes, 42 flower sprites). (5) Reordered Phase 2 build sequence: sleep/day-advance first (foundation for testing multi-day systems), then weather (gates foraging), then equipment shop wiring (enables smoker/suit purchase), then smoke/stings (inspection risk), then harvest pipeline, then market, then standing. (6) All Phase 3-4 tasks numbered for trackability (3.1-3.20, 4.1-4.26). (7) Removed misleading PARTIAL/STUB status from features that only have variable declarations or empty frameworks -- everything is either DONE or TODO. Protected files verified: DevPlan 67,772 bytes, GDD 555,381 bytes.

[2026-03-28 18:00:00 CDT] GDD Major Update: Honey House restoration system, Carpenter NPC, manual harvest equipment. Added new NPC Silas Crenshaw (Carpenter) -- semi-retired carpenter who built Uncle Bob's original Honey House decades ago. 4-quest chain (Q1: The Old Honey House, Q2: Gathering Materials, Q3: Raising the Roof, Q4: The Upgrade) spans Year 1-2 and gates the Honey House restoration. Added 3-tier extraction progression: Tier 0 (Manual Equipment) uses comb scraper ($12), hand spinner ($45), and bottling kit ($18) purchased from catalogue -- 1 frame at a time, 3 energy/frame, +1.5% moisture penalty; Tier 1 (Restored Honey House) via Silas Q1-Q3 mid-summer Y1 -- 2 frames, 2 energy/frame, no penalty; Tier 2 (Upgraded Honey House) via Silas Q4 Year 2 -- 4 frames, 1 energy/frame, -0.8% moisture curing bonus. Honey House starts as dilapidated structure visible on home property from game start (broken windows, sagging roof, rusted equipment visible inside). Transforms over 7 in-game days during Silas quest chain with daily visual progress. Updated sections: 1.3 Starting Conditions (manual equipment + dilapidated Honey House), 6.6 Harvest System (extraction facilities table + manual equipment subsection), 7.1 Progression (Level 3 description), 7.2 Apiary Structures (4 Honey House states), 8.4 NPC Quest Chains (Silas chain + interconnections + economy totals: 38 quests, 3850 XP), 9 NPCs (Silas entry), 13.1 Home Property (dilapidated Honey House asset).

[2026-03-28 13:00:00 CDT] Willow tree Leonardo AI sprite overhaul + SeasonalTree system with collision and y-sort layering. Generated 4 seasonal willow tree sprites via Leonardo AI (Lucid Origin model, 1376x768, 16:9, Dynamic style, 3/4 isometric-adjacent perspective). Processing: background removal, ground/shadow cleanup, split into 4 PNGs with transparent backgrounds, resized to 40% for proper game scale (~130x160px). Created SeasonalTree system: scripts/world/seasonal_tree.gd (class_name SeasonalTree, extends Node2D) with child Sprite2D + child StaticBody2D. Node position = trunk base; sprite draws upward via offset; StaticBody2D collision rect (16x12 default, configurable via exports) blocks player at trunk. Works with y_sort_enabled World node -- player walks behind canopy when above, in front when below. Supports @export tree_type, flip, bloom_start/bloom_end, trunk_collision_width/height. Scene: scenes/world/seasonal_tree.tscn.
[2026-03-27 18:51:00 CDT] Fixed game load failure caused by flower sprite integration. Root causes: (1) .import files generated by integrate.py used wrong [params] format -- Godot 4.6 expects roughness/src_normal (not src_normal_path), process/channel_remap/* fields, compress/uastc_level, compress/rdo_quality_loss that were missing or misnamed. (2) fix_imports.py unnecessarily modified 7 non-flower .import files (clover_backup, clover_plant, dandelion_tile, goldenrod_backup, wildflower_tile, willow). (3) Stale uid_cache.bin had old UID-to-path mappings conflicting with new flower UIDs. Fix: restored non-flower .import files from git, regenerated all 42 flower .import files with correct Godot 4.6 param format, cleared .godot/imported/ cache + uid_cache.bin + global_script_class_cache.cfg for clean reimport. All 5 scene-referenced UIDs verified matching.
[2026-03-27 17:10:00 CST] Leonardo AI flower sprite overhaul. Replaced all programmatic v3 flower sprites with Leonardo AI-generated pixel art for all 7 core GDD species (dandelion, clover, bergamot/bee balm, coneflower/echinacea, sunflower, goldenrod, aster) x 5 lifecycle stages (seed, sprout, growing, mature, withered) = 42 sprites. Generated via Leonardo AI (Lucid Origin model, 1024x1024, Dynamic style) with species-specific lifecycle prompts emphasizing 3/4 top-down RPG perspective, Stardew Valley style, warm muted earthy palette, and clean outlines. Processing pipeline: background removal via color distance thresholding, smart sprite detection with equal-column fallback splitting, NEAREST-neighbor resize to game dimensions (32x32 mature, 16x16 others), bottom-aligned on transparent canvas. All 5 critical scene-referenced UIDs preserved (aster, clover, coneflower, dandelion, goldenrod). Cleared Godot import cache. 7 Leonardo generations used (~175 tokens, 24,377 remaining).
[2026-03-27 16:00:00 CST] Flower sprite fidelity overhaul v3. Regenerated all 9 flower species (aster, bergamot, clover, coneflower, dandelion, goldenrod, lavender, phacelia, sunflower) x 5 lifecycle stages (seed, sprout, growing, mature, withered) = 54 sprites total. New sprites use hue-shifted 5-color ramps per species, species-specific petal anatomy (star petals for aster, drooping petals + raised cone for coneflower, tubular spikes for bergamot, puffy florets for clover, ray florets for dandelion, cascading plume for goldenrod, whorled spike for lavender, scorpioid cyme for phacelia, large disc + spiral seed center for sunflower), proper stem shading with cylindrical lighting, detailed leaves with midrib veining, elliptical ground shadows, and subtle outline hints. Mature flowers are 32x32, lifecycle stages are 16x16. Generator script saved as gen_flowers_v3.py. Previous sprites were flat-colored circles on stick stems with 4-5 colors each; new sprites have 15-25 colors per species with realistic botanical structure. Cleared Godot import cache for all 54 affected textures. Reverted custom tileset swap (grass/dirt) back to Cainos Village Pack -- Leonardo AI textures were photorealistic (11K colors) and clashed with the pixel art style (28 colors).
[2026-03-27 15:00:00 CST] Cleanup and tileset integration. Removed 7 building-entry InteractHint Label nodes from 3 scene files (home_property.tscn, cedar_bend.tscn, harmon_farm.tscn) -- leftover from the old E-key proximity system now replaced by collision-based door entry. Preserved NPC dialogue hints (RoseWaitress, JunePostmaster, CarlTanner, FrankNPC) and interactive prop hints (BulletinBoard, Truck, Mailbox). Swapped grass and dirt TileMap atlas textures from old Cainos Village Pack paths (assets/sprites/paid/...) to new custom Leonardo AI tilesets (assets/sprites/tilesets/TX Tileset Grass.png, TX Tileset Dirt.png) in both home_property.tscn and main.tscn. Both new tilesets match the old dimensions exactly (grass 512x512, dirt 256x128) so all painted tile coordinates remain valid. UIDs updated to match new import files.
[2026-03-27 14:00:00 CST] Honey House sprite replaced with closed-door version (Leonardo AI, 3/4 top-down perspective, dark wood barn with iron-hinged double doors, "HONEY HOUSE" sign). Building entry system refactored from E-key proximity to collision-based door entry across both home_property.gd and cedar_bend.gd. Area2D door zones are now created programmatically at each building's door position; player walks into the door to trigger scene transition. Removed INTERACT_RADIUS, _update_interact_hints(), _input(KEY_E), _try_enter_building() in favor of body_entered signal on DoorZone Area2D nodes. InteractHint labels no longer used. Also generated custom ground tilesets via Leonardo AI: seamless grass texture (512x512 atlas, 64 unique 32x32 tiles), dirt path texture (256x128 atlas, 30 unique tiles), and grass-to-dirt transition terrain atlas (128x192, 24 edge/corner blend tiles). All placed in assets/sprites/tilesets/.
[2026-03-27 13:00:00 CST] Standardized all exterior bee sprites to 6x6px. Previously breed forager sprites and bee_particle.png were 8x8 while bee_forager.png was 6x6. Regenerated all 5 breed foragers (italian, buckfast, carniolan, caucasian, russian) and bee_particle.png at 6x6 to match. Cleared Godot import cache.
[2026-03-27 12:00:00 CST] Leonardo AI building sprites - Phase 1. Generated 5 high-detail pixel art building sprites via Leonardo AI (Lucid Origin model) matching the Extension Office quality benchmark: Player's Farmhouse (white clapboard, porch, chimney), Honey House/Shed (weathered red-brown, sliding barn door, beekeeping tools), Post Office (red brick, American flag, USPS mailbox), Grange Hall (white wood, cupola bell tower, double doors), Extension Office (tan brick, bay window, garden bed). All sprites had backgrounds removed, cropped to content, and resized to match existing game scale (180-230px tall). Replaced programmatically-generated building sprites in assets/sprites/buildings/. Added Farmhouse and Honey House exterior sprites to home_property.tscn with E-key building entry triggers (InteractHint labels, 100px radius proximity detection). Updated home_property.gd with building interaction system mirroring cedar_bend.gd pattern. Farmhouse enters house_interior, Honey House enters honey_house interior. Both registered as map POIs.
[2026-03-27 08:00:00 CST] Regenerated breed forager sprites with colors sampled directly from queen-finder worker spritesheets. Previous sprites were too saturated/cartoony and looked identical in-game. New sprites use actual pixel-sampled body, band, head, and wing colors: Italian body=(84,59,31) band=(130,105,65) warm golden-brown; Buckfast body=(65,44,23) band=(114,91,58) medium dark brown; Carniolan body=(53,47,39) band=(94,84,71) cool gray-brown; Caucasian body=(57,51,45) band=(99,91,81) warm gray; Russian body=(40,30,22) band=(75,62,48) very dark. Wings use semi-transparent breed-matched tones. Cleared Godot import cache for all 5 sprites.
[2026-03-27 07:00:00 CST] Breed-specific bee sprites and package bee species. Generated 5 tiny 8x8 top-down forager sprites (assets/sprites/fx/bee_forager_{breed}.png) with colors matching queen-finder spritesheets. BeeSwarmCloud now reads queen_species from the snapshot and swaps all particle + forager textures to match the colony's breed. Falls back to generic bee_particle.png if breed sprite missing. init_as_package() now accepts a p_species parameter (default "Russian") instead of picking randomly -- lets the player see breed differences between their overwintered Carniolan hive and new package installations.
[2026-03-27 06:00:00 CST] Forager flight system added to BeeSwarmCloud. Scripted Sprite2D forager bees now fly from the hive to nearby flowers, pause 1.5-3.5s to "gather", then fly back and rest briefly before repeating. Purely cosmetic -- actual resource math stays in ForagerSystem.gd. Finds flowers via "flowers" group nodes and flower_lifecycle_manager mature tiles (phase 3) within 250px radius. Falls back to random positions if no flowers found. Forager count scales with snapshot forager_count (0-10000 mapped to 0-12 visual bees). Each bee has per-instance speed jitter (+/-20px/s around 70px/s base) and perpendicular flight wobble for organic look. State machine: TO_FLOWER -> GATHERING -> TO_HIVE -> RESTING with staggered start timers. Reverted particle texture from black dot back to bee_particle.png. Kept all three visual layers: entrance GPUParticles2D buzz + overhead cloud + scripted forager flights.
[2026-03-27 05:00:00 CST] Bugfix: hive collision and bee swarm cloud. (1) StaticBody2D collision now dynamically resizes with sprite stack height -- previously stayed at the 24x16 scene default, letting the player walk into the hive sprite. _update_collision_for_height() now updates both Area2D (interaction) and StaticBody2D (physics). Body collision covers 60% of visual height with 28px width. (2) Bee swarm cloud was never visible because emitters started with emitting=false and only updated on day-advance tick -- on Day 1 no tick fires. Added _update_swarm_cloud() call in _ensure_swarm_cloud() to kick-start particles from the initial snapshot immediately. (3) Bumped minimum particle counts (entrance 2->4, overhead 1->2) and max counts (entrance 40->50, overhead 25->30) for better visibility at game scale.
[2026-03-27 04:00:00 CST] Bee swarm cloud -- visual hive activity indicator. New BeeSwarmCloud.gd (scripts/world/) creates two GPUParticles2D emitters per hive: an entrance swarm near the landing board and an overhead cloud above the hive. Driven by snapshot data each tick -- composite "activity" score (population 35%, forager count 30%, health 35%) controls particle count (2-80 bees), emission spread, and lifetime. Separate "agitation" score (congestion 35%, mite load 25%, AFB 20%, health-stress 20%) controls speed, turbulence intensity, and color tint (golden-calm to dark-brown-stressed). When agitation exceeds 0.5, particle counts boost further for a dense swarming look. Entrance emitter uses box emission with horizontal outward flight + gentle gravity pullback. Overhead emitter drifts upward with stronger turbulence for organic circling. Both smoothly lerp toward target values (2.0/sec) for fluid transitions. Uses existing bee_particle.png sprite. Integrated into hive.gd: cloud spawns on activate_simulation(), repositions on sprite stack rebuild, updates from snapshot on every tick. Aligns with GDD S3.5 design philosophy -- no numbers shown, player learns to read visual bee behavior as health indicator.
[2026-03-27 03:00:00 CST] Art quality overhaul v6 -- Block-level fidelity pass targeting Cainos Village Pack quality per 32x32 block. Key rendering improvements: ROOF TILES: 40% larger tiles with 5-6 color shades per tile (was 2-3), 2px overlap shadows (was 1px), deeper edge shadows, visible ridge highlight band. STONE WALLS: larger stones with 3px mortar gaps (was 1px), beveled/rounded per-stone shading using edge-distance calculation (center bright, edges dark), 2px highlight on top/left and 2px shadow on bottom/right, wider per-stone color variation (warm/cool shifts), occasional chip/crack detail. WOOD WALLS: wider planks with 2px gap shadows, visible grain via sine-wave pattern, per-plank barrel-curve shading (center brighter), concentric-ring knot details (0-2 per plank), per-plank color variation with warm/cool shifts. BRICK WALLS: wider mortar (2-3px), beveled per-brick shading, dramatic per-brick color variation (warm/cool/darker shifts), highlight top edge + shadow bottom edge per brick. WINDOWS: thicker 3px frame, diagonal glass reflection streak, 2px mullion cross bars, inner shadow for recessed look, 3px window sill ledge, louver detail on shutters. DOORS: deeper recessed panels with proper shadow/highlight (top/left dark, bottom/right light), 2px doorknob with highlight, wider steps. ALL ELEMENTS: outline colors derived from local material hue (not uniform brown), deeper ambient occlusion (5px depth, 55 alpha), larger ground shadow (7px spread). Generator saved to tools/gen_buildings_v6.py. All 15 building sprites regenerated.
[2026-03-27 02:00:00 CST] Bugfix: TileSet atlas errors and empty image errors on launch. Root cause: 5 flower forage textures (dandelion_mature, clover_mature, aster_mature, coneflower_mature, goldenrod_mature) were 16x16 pixels but home_property.tscn TileSetAtlasSource definitions expected 32x32 texture_region_size. This caused "Cannot create tile" and "TileSetAtlasSource atlas has no tile at (0,0)" errors for all 5 flower atlas sources, plus "Image width specified (0 pixels)" / "create_from_image: image is empty" errors from Godot internally trying to slice subregions from undersized textures. Fix: upscaled all 5 flower textures from 16x16 to 32x32 using nearest-neighbor interpolation (preserves pixel art look). Cleared Godot import cache (.ctex/.md5 and .import files) for the 5 affected textures. Fixed coneflower comment in home_property.tscn that incorrectly said "16x16". All 51 debug log errors resolved.
[2026-03-27 01:00:00 CST] Art quality overhaul v5 -- Multi-layer compositing with professional pixel art techniques. BUILDINGS (v5): Complete rewrite using individual overlapping roof tiles (not flat gradients), per-stone/per-brick rendering with mortar joints and edge highlighting, wood plank grain texture, ambient occlusion at all surface contacts, hue-shifted shadows (cool blue-purple) and warm highlights, detailed windows with glass reflections/mullions/sills/optional shutters, doors with recessed panels/knobs/steps, stone foundations, horizontal trim boards at material junctions, decorative bushes at building bases, awnings with scalloped edges, building signs, chimneys with individual stone rendering, ground drop shadows, anti-aliased warm brown outlines (72,37,16). Color density: 10K-19K unique colors per building (reference asset pack: 5K). 15 buildings total. ENVIRONMENT (v5): Trees with layered leaf cluster rendering (10+ overlapping clusters per oak tree) and cylindrical trunk shading, props with 3/4 top-down cylindrical shading (barrels, hydrants), lampposts with light glow, benches with wood grain, flower planters with individual flowers, tiles with blade/pebble/crack detail. NPCs (v5): 3/4 top-down perspective matching player sprite, hue-shifted skin/hair/clothing ramps, individual character details (hats, overalls, aprons, shutters), 460-560 unique colors per character (up from 20-27). SCENE: Adjusted cedar_bend.tscn building positions for new sprite heights. Generators saved to tools/.
[2026-03-26 21:00:00 CST] Art quality overhaul v3 -- dramatic fidelity increase targeting Stardew Valley/Harvest Moon quality. BUILDINGS: Complete rebuild of all 15 building sprites using pixel art techniques: hue-shifted color ramps (shadows shift cooler/purple, highlights shift warmer/yellow), individual roof shingles with overlap patterns, real wood plank textures with grain/knots/wear marks, brick walls with individual bricks and mortar and color variation, detailed windows with panes/curtains/reflections/flower boxes, doors with recessed panels/hardware/kick plates/steps, stone foundations, architectural trim/molding, porches with railing balusters, sign boards. New sizes slightly larger: Diner 280x200, Feed Supply 240x185, Grange 260x195, Farmhouse 240x190. NPCs: Rebuilt all 17 character sprites with hue-shifted skin/hair/clothing ramps (5 levels each), eyebrows, eye whites, cheek blush, clothing wrinkle detail, proper boot shading, hair strand groupings. ENVIRONMENT: Created 13 new environmental sprites: 4 ground tiles (grass/dirt/road/sidewalk with pixel texture), 2 trees (oak 96x140 with layered canopy and bark detail, pine 56x110), 7 props (lamppost, bench, mailbox, barrel, crate, fire hydrant, flower planter). SCENE: Updated cedar_bend.tscn with grass areas above/below street, curb edges, 5 oak trees, 4 lampposts, 2 benches, flower planters, mailbox, fire hydrant, barrels and crates for environmental richness.
[2026-03-26 20:00:00 CST] Art direction overhaul Phase 2: Regenerated all NPC and pedestrian sprites using the master muted palette (52 colors, art_direction.py). 11 named NPCs (uncle_bob, frank_fischbach, carl_tanner, dr_harwick, rose_waitress, june_postmaster, walt_harmon, kacey_harmon, darlene_kowalski, lloyd_petersen, terri_vogel) and 6 pedestrians (a-f) now use cohesive warm-brown outlines, muted clothing tones, directional lighting, dithered shading, and proper pixel art detail (hats, glasses, facial hair, aprons, vests, suspenders, collars, boots). Characters are visually distinct through unique silhouettes, color combos from palette families (wood, blue, green, red, gold, cream, brick), and accessories. Generator script saved as /tmp/gen_npcs_v2.py with reusable draw functions (draw_head, draw_torso, draw_arms, draw_legs, draw_boots, draw_facial_hair, add_glasses). All sprites 64x80 matching v1 dimensions. This completes the full art refresh: buildings (Phase 1) + characters (Phase 2) now share the same muted Midwest aesthetic.
[2026-03-26 19:00:00 CST] Art scale overhaul: Fixed player/NPC/building proportions. Player character is ~32x59px within 120x120 frames -- buildings were only 48-72px tall (same height as player). Regenerated all 15 building sprites at 2.5-3x size with significantly more detail (brick textures, window interiors, architectural features, signage, porches, shutters). Crossroads Diner: 96x72 -> 240x160, Feed Supply: 80x64 -> 200x150, Post Office: 64x56 -> 180x140, Grange Hall: 80x64 -> 220x160, Farmhouse: 80x64 -> 200x160, plus all farm/fairground buildings. Regenerated all 11 NPC standing sprites from 16x32 to 64x80 with proper body proportions, clothing detail, and personality (hats, overalls, aprons, hair styles). Uncle Bob: 124x124 -> 64x80 (matching player frame ratio). All 6 pedestrians regenerated at matching scale with diverse appearances. Updated cedar_bend.tscn layout: wider road (40px), buildings spaced across -560 to +520 range, player walks at y=70. Updated fairgrounds.tscn and harmon_farm.tscn for new building sizes. Updated ExitHelper edge positions from 330 to 700 and spawn positions accordingly. Updated all exterior scene bounds from Rect2(-400,-160,800,320) to Rect2(-800,-300,1600,600). Updated interact radius from 60 to 120. Updated pedestrian waypoints and spawn positions. Updated NPC label font sizes and offsets for new sprite scale.
[2026-03-26 16:00:00 CST] Added overwintered colony initialization as the default starting hive. New init_as_overwintered() in HiveSimulation.gd and place_as_overwintered() in hive.gd create a realistic Carniolan B-rated colony that has successfully overwintered and is entering spring (Day 1 of Quickening). Configuration: 4 center frames of drawn comb (indices 3-6, 6 outer = foundation), small brood nest in all stages via 3D ellipsoid (radius 0.30, ~800-1200 brood cells), 8000 winter bees (2500 nurse / 3500 house / 2000 forager / 0 drone), 12 lbs honey stores (with capped honey cells seeded on flanking frames 3 and 6), 1.5 lbs pollen, 35 mites, no diseases, 1-year-old queen with no laying delay. Fresh games now auto-spawn this hive at (300, 350) near the player start via _spawn_starter_hive() in home_property.gd -- no placement from inventory needed. Player.gd _action_place_hive() also calls place_as_overwintered() so any additional ITEM_BEEHIVE placements use the same benchmark. Old nuc init (init_as_nuc / place_as_complete) preserved for backward compatibility. This colony serves as the benchmark for tracking and validating hive development through the simulation.
[2026-03-26 17:00:00 CST] Made map overlay fully static: bounds are now fixed per scene (set once in _register_map_markers, never change). Only the player dot moves. Exit arrows rendered at their actual world-edge positions on the map using EXIT_WORLD_POS lookup. Destination labels placed next to arrows ("to County Road", etc). Tightened home_property bounds to Rect2(-100,-110,750,550) so content spreads across the full map area. Added Hive as a registered POI. Set fixed Rect2(-400,-160,800,320) bounds for all 5 other outdoor scenes to match ExitHelper edge positions.
[2026-03-26 16:45:00 CST] Fixed map scale and layout: map now auto-calculates bounds from player position + all registered POIs (finds bounding box, adds 30% padding). Removed broken auto-scan that was creating duplicate/overlapping labels. Exit arrows now draw inside the map area (orange stripe + ">>" + "to Destination" label) so they are always visible. POI labels positioned above markers to avoid overlap. Footer always shows navigation hint.
[2026-03-26 16:15:00 CST] Fixed map overlay visibility: all markers and labels were too small to see. Increased POI markers from 5px to 8px with dark outlines, labels from font 4 to font 6, exit arrows from font 7 to font 8 with "to [Destination]" labels at font 6. Added orange highlight stripes along exit edges. Added "YOU" label on player dot (increased to 8px bright green). Added auto-scan fallback that crawls the scene tree for notable Node2D children when no POIs are registered. Tightened home_property bounds from full wall extent (2667x1635) to playable area (1000x600). Fixed POI registration to use global_position instead of position.
[2026-03-26 15:30:00 CST] Redesigned map overlay (M key) from county-wide overview to scene-level minimap. Now shows: player position (green dot tracking real-time), points of interest in current scene (buildings, NPCs, objects), and exit arrows with destination labels at screen edges. Each scene registers its POIs and exits with SceneManager on load. Added SceneManager API: register_scene_poi(), register_scene_exit(), set_scene_bounds(), clear_scene_markers(). Updated all 12 scene scripts (7 outdoor + 5 interior) to register their map markers. Dynamic bounds support handles both the large home_property scene and small interior rooms.
[2026-03-26 15:00:00 CST] Renamed TestEnvironment to home_property (scene + script). Updated all references in project.godot (main_scene), SceneManager, MainMenu, county_road, generic_interior, house_interior, honey_house, wildflower_scatter, dandelion_spawner, and SaveManager. Game now starts at res://scenes/home_property.tscn. Old TestEnvironment files removed.
[2026-03-26 14:00:00 CST] World Build: First-pass playable world with walking navigation. Generated 157 sprites via PIL (ground tiles, buildings, trees, props, wildlife, crops, NPCs, UI). Created 4 new outdoor scenes (harmon_farm, timber_creek, community_garden, fairgrounds) and 3 new interiors (grange_interior, harmon_barn_interior, honey_house) using generated sprites. Built walk-to exit system (ExitHelper class) with Area2D edge triggers -- player walks to screen edges to transition between connected locations. World topology: Home <-> County Road <-> Cedar Bend <-> Community Garden, County Road <-> Harmon Farm, County Road <-> Timber Creek, Cedar Bend <-> Fairgrounds. Converted map overlay to display-only (M key shows parchment map with "You Are Here" marker, no click-to-travel). Added SceneManager autoload for global M-key handling and zone name display. Added player instances to all scenes missing them (county_road, cedar_bend, diner/feed_supply/post_office interiors). Added previous_scene and current_scene_id to TimeManager for navigation tracking. New files: scripts/autoloads/SceneManager.gd, scripts/world/exit_helper.gd, scripts/world/generic_interior.gd, scripts/world/zone_exit.gd, scripts/world/harmon_farm.gd, scripts/world/timber_creek.gd, scripts/world/community_garden.gd, scripts/world/fairgrounds.gd, 7 new .tscn files.
[2026-03-26 10:30:00 CST] Research: Updated Location & Map Build Report with comprehensive sprite/tile requirements for all 18 locations. Each scene section now includes a "Required Sprites & Tiles" table listing every asset needed (ground tiles, buildings, props, NPCs, trees, seasonal overlays, particles, wildlife). Added new Section 7 "Master Sprite Inventory" with categorized summary: 137 unique sprites total, 48 exist (35%), 88 needed (64%). Report doubled in size (~69KB to ~140KB). No game files modified.
[2026-03-26 10:00:00 CST] Research: Location & Map Build Report generated (research/design/Location_Map_Report.html). Comprehensive audit of all 18 scene maps required by GDD Section 13. Catalogs 7 existing partial scenes vs 11 not-yet-built. Includes: gap analysis, Stardew Valley and Harvest Moon genre comparison, ASCII layout suggestions for every map, world connection diagram, travel flow chart, recommended 7-phase build order. Report uses GDD visual style (amber/honey HTML theme). No game files modified.
[2026-03-25 20:45:00 CST] Fix: corrected bee sprite facing direction (second attempt). The base sprite in generate_nurse_bee.py draws the head at cx-11.5 (LEFT of center), meaning row 0 actually faces West, not East as labeled. All 8 rows were offset by 4 positions (180 degrees in X). Previous Y-negate fix was wrong -- the actual fix is adding 4 (mod 8) to the direction index in _angle_to_dir(), and undoing that offset in _dir_to_vector(). Actual row mapping: 0=W, 1=NW, 2=N, 3=NE, 4=E, 5=SE, 6=S, 7=SW.
[2026-03-25 20:15:00 CST] Fix: removed dead Phase 1 queen sighting code from InspectionOverlay.gd that referenced deleted constants (QUEEN_SIGHT_BASE, QUEEN_SIGHT_MAX, QUEEN_SIGHT_XP), causing parse error on project load. The Phase 1 functions (_check_queen_sighting, _show_queen_notification, _get_queen_frame_idx) were leftover from before Phase 2 replaced them.
[2026-03-25 20:00:00 CST] Queen Finder Phase 2 implementation -- replaced probability-based queen sighting with interactive visual search minigame. New files: scripts/ui/BeeOverlay.gd (bee entity management, movement state machine, sprite compositing, click detection, spatial hash soft repulsion), assets/sprites/bees/ (10 breed spritesheets: 5 breeds x worker/queen, 420x336 each, 8 directions, 7 anim frames). Modified files: scripts/ui/InspectionOverlay.gd (integrated BeeOverlay rendering layer, added mouse click handling for queen identification, Phase 2 queen notification with tier-scaled messages, added _bee_rect TextureRect for bee sprite overlay, updated footer hints), scripts/simulation/CellStateTransition.gd (added count_brood_side() for per-side brood counting), scripts/core/HiveSimulation.gd (added HiveBox.get_frame_brood_share() for density model). System design: At inspection start, rolls difficulty rank (Easy/Medium/Hard, flat 33/33/34) and queen visibility (80% spawn chance). Difficulty controls three parameters: density multiplier (0.33/0.67/1.0), minimum bee spacing (28/17/12 px), and cluster tightness (0.28/0.20/0.16 Gaussian spread). Worker bees animate with walk/idle/turn state machine, queen moves slower with longer pauses, attendant bees orbit the queen in a retinue circle. XP scales with difficulty: Easy +10, Medium +15, Hard +25. No penalty for wrong clicks. Queen anatomy tells: longer abdomen, shorter wings, fewer fuzz dots, wider leg splay.
[2026-03-25 18:30:00 CST] Project rebrand and repository migration: Moved entire project from beekeeperPro/ to SmokeAndHoney/ git repository (GitHub: qLu-jML/SmokeAndHoney). Renamed BeeKeeper_Pro_DevPlan.html to Smoke_and_Honey_DevPlan.html. Updated all project-name references in code files (research/hive_sim_harness.py, research/queenFinder/generate_nurse_bee.py), config files (.claude/settings.local.json), and documentation (research/PIXELLAB_STYLE_GUIDE.md, changelog.txt). Preserved all NPC character references ("The Beekeeper") and gameplay skill names ("Beekeeper", "Master Beekeeper") as-is. Reorganized project root -- moved all non-game files into research/ subdirectories: design/ (quest design, story, proposals), art/ (pixel art guide, style guide, art assets spreadsheet, example sprites), reports/ (daily and morning reports), archive/ (old GDD versions, deprecated dev plan, test screenshots), reference/ (Karpathy research, workflow docs), temp/ (scratch space). Updated CLAUDE.md with new protected file names, directory structure conventions, and git workflow rules. Project root now contains only game-essential files: project.godot, assets/, scenes/, scripts/, resources/, tools/, and project docs.
[2026-03-25 13:30:00 CST] Eight design decisions applied to Queen Finder sub-GDD (_Queen_Finder_GDD.html): (1) Attendant bees scale inversely with difficulty -- Easy x1.5 (more attendants = visual hint), Medium x1.0, Hard x0.5 (sparse court). New ATTENDANT_DIFF_MULT_* constants. (2) Movement speeds locked across all difficulty ranks -- speed is NOT a difficulty lever; explicit pitch block added to S8. (3) Supers always show typical density using Medium-equivalent parameters; queen entity never spawns on super frames. (4) Expanded Phase 2 Plans (S11) with "How the Systems Interact -- Complete Picture" section: 4 numbered tables covering inspection start (5 steps), per frame flip (3 steps), per game tick (3 steps), and on player click (3 outcomes). (5) 80% queen spawn chance rolled once at inspection start -- binary decision, queen entity + attendants exist or don't. Rewrote trigger conditions and sighting probability sections; removed per-frame probability roll. (6) Toast notification only fires when player clicks queen; removed difficulty rank toast at inspection start from all output tables and lifecycle. (7) Temperament directly scales visible bee count via density modifier column (Calm x1.0, Normal x1.15, Defensive x1.4, Congested x1.25, Post-smoke x0.85); stacks multiplicatively with difficulty mult. Updated formula to 8-step pipeline including temperament_density_mult. (8) Challenge floor -- MIN_VISIBLE=8 + tight spacing (12px) + tight clustering (0.16) on Hard ensures no inspection is trivially simple; bias hook forces Easy on nurse_count < 1000. Constants cross-reference updated with QUEEN_SPAWN_CHANCE, ATTENDANT_DIFF_MULT_*, and TEMPERAMENT_DENSITY_* values. Sidebar nav updated with new subsection links.
[2026-03-25 12:45:00 CST] Fixed gloves and hive management: (1) Added ITEM_GLOVES to hud.gd ITEM_SPRITE_MAP so gloves icon renders in toolbar. (2) Fixed hive prompt text -- gloves, deep_body, deep_box, and super_box prompts now show BEFORE colony-install check, so "[E] Hive Management" appears when holding gloves on any completed hive. (3) Fixed hive_management.gd to accept either ITEM_DEEP_BODY or ITEM_DEEP_BOX for adding deeps (both in button state check and consume action). (4) Fixed super max in prompt from 4 to 10. (5) Added debug prints to gloves/hive management flow for troubleshooting. Restructured COMPLETE prompt priority: gloves/boxes first, then colony status, then inspection.
[2026-03-25 12:30:00 CST] Expanded inspection difficulty rank system from single count multiplier to three-parameter model matching generate_frame_sim.py. Each difficulty rank now controls: (1) density multiplier (0.33/0.67/1.0), (2) minimum bee spacing (28/17/12 px), (3) cluster tightness (0.28/0.20/0.16 Gaussian spread). Updated 6 sections of Queen Finder sub-GDD: (S2) difficulty rank table expanded with spacing and clustering columns plus mockup reference; new Roll Probabilities subsection with concrete 33/33/34 default formula and 5 documented-but-deferred bias hooks (player level, knowledge tier, temperament, congestion, colony size) with specific percentage shifts and [5%,80%] clamp rule. (S8) collision/density section rewritten with difficulty-dependent MIN_BEE_DIST and CLUSTER_SPREAD constants; soft repulsion now takes min_bee_dist parameter; added paragraph explaining Gaussian target selection. (S8) edge cases expanded from 5 to 8 rows covering: Easy+tiny colony (floor makes ranks identical, force Easy), Easy+booming (intentionally relaxing), Hard+small colony (tight spacing creates challenge despite low count), super frames (multiplier applies to forager_count), colony collapse (MIN_VISIBLE floor disabled). (S8) pitch block rewritten to explain why three levers are needed over count alone. (S10) XP constants split into per-rank values (10/15/25). (S12) lifecycle updated to store min_bee_dist and cluster_spread alongside mult; constants cross-reference expanded with 6 new spacing/clustering constants and 3 XP-per-rank constants. Sidebar nav updated with Roll Probabilities link.
[2026-03-25 12:15:00 CST] Multiple changes: (1) Resized hive_deep.png and hive_deep_empty.png from 24x18 to 24x24 -- deeps now visually much taller than supers (24x14), with 12px of solid wall vs 6px. Same 11px open-top anatomy preserved. (2) Spring dandelion rush: bloom_start moved from day 5 to day 1, peak_start from 12 to 3, nu_nectar bumped from 2 to 3. Dandelions now fast-start (30% spawn MATURE, 30% GROWING, 20% SPROUT, 20% SEED) representing overwintered rosettes popping immediately. (3) Quickening season_factor raised from 0.55 to 0.65 for stronger spring nectar flow. (4) Redrawn gloves.png -- now shows a pair of cream leather beekeeping gloves with fingers, gauntlet cuffs, and stitching detail. Old sprite was just a colored box. (5) Redrawn super_box.png -- off-white painted box with golden honey visible from above, handle grooves, and honey drip detail. Cleared Godot import caches for all changed sprites.
[2026-03-25 11:45:00 CST] Redrew hive_lid.png from 28x13 to 30x16. New lid is a proper telescoping cover: flat silver metal top (rows 0-7), dark band (row 8), wooden inner rim walls extending down (rows 9-15) that visually wrap over the top of the box below. 3px overhang on each side beyond the 24px box body. Deleted stale Godot import cache (.ctex/.md5) so the new PNG is picked up on next launch.
[2026-03-25 11:30:00 CST] Rewrote sprite stacking overlap to use actual sprite anatomy. Analyzed each PNG row-by-row: deep has 11px open interior top (rows 0-10) and 7px solid wall bottom (rows 11-17); super has 8px open top (rows 0-7) and 6px solid wall (rows 8-13). Overlap is now based on the open-top height of the piece BELOW: 11px when stacking on a deep, 8px when stacking on a super, 0px on stand/base/excluder. Previous 4px overlap was far too small, leaving frame tops visible.
[2026-03-25 11:15:00 CST] Retuned wax production base coefficient from 0.39 to 0.49. At B-grade conditions across the board (B queen grade_mod=1.0, Italian species_mod=1.0, B month s_factor=0.65, 4000 house bees, 9lb stores, 2500 foragers, 75 health), wax draw rate is now ~1264 cells/day vs queen laying ~975 eggs/day = ~30% ahead. Previous tuning was only ~3% ahead at B conditions.
[2026-03-25 11:00:00 CST] Fixed hive sprite stacking: boxes now overlap the one below by 4px so the solid bottom of each component covers the open frame-tops of the box beneath. Old code had 1px gaps between all pieces, leaving frame tops exposed. New overlap constants: OVERLAP_BOX=4 (deep/super over deep/super), OVERLAP_LID=4 (lid over top box), OVERLAP_EXCL=1 (piece above thin excluder wire). Stand and base still sit flush (0px overlap). Each layer's overlap is determined by what texture it is and what's below it.
[2026-03-25 10:45:00 CST] Fixed box management not working: (1) All box actions (deep, super, excluder, gloves) now fire BEFORE colony-install and lockout checks in player.gd -- they work on any completed hive regardless of colony status. (2) Accept both ITEM_DEEP_BODY and ITEM_DEEP_BOX for adding a second deep (player starts with deep_body in hotbar but old code only checked deep_box). (3) Removed colony_installed requirement from try_add_super() and try_add_excluder() in hive.gd. (4) Removed duplicate excluder/super handlers that were stranded after the lockout gate.
[2026-03-25 10:30:00 CST] Added inspection difficulty rank system to Queen Finder sub-GDD. When player starts an inspection, a difficulty is rolled (Easy/Medium/Hard) which applies a density multiplier to visible bee counts: Easy = 0.33 (1/3), Medium = 0.67 (2/3), Hard = 1.0 (full). Updated 8 sections: (S2) new Inspection Difficulty Rank subsection with multiplier table; sighting probability XP now scales by rank (Easy +10, Medium +15, Hard +25). (S7) snapshot pipeline adds queen.temperament as difficulty bias input. (S8) density formula updated to 7-step pipeline with difficulty_mult as Step 4; worked examples table shows all 3 rank columns. (S10) queen-found feedback XP scales with rank. (S12) InspectionOverlay changes updated with rank roll; new Gamestate Interactions subsection maps all inputs (knowledge tier, temperament, congestion, player level, nurse count) and outputs (stored mult, XP scaling, HUD toast) with full lifecycle; constants cross-reference updated with 3 DIFFICULTY_MULT constants. Sidebar nav updated.
[2026-03-25 10:15:00 CST] Three changes: (1) Removed winter warning system entirely -- no more red/orange glow on hives for low stores. Deleted _update_winter_warning(), winter tint constants, pulse animation, and notification triggers from hive.gd. Health tint still works for colony health. (2) Fixed adding second deep body -- gloves and deep box actions now work on any completed hive regardless of colony status. Restructured player.gd E-key chain so gloves/deep-box are checked before colony-install gating. Removed colony_installed requirement from hive.gd try_add_deep(). (3) Added 5 honey supers (super_box) to starting player inventory.
[2026-03-25 10:00:00 CST] Separated dev HUD grade display into two distinct labels: "Month: X" shows the per-month nectar grade from season_factor (S/A/B/C/D/F), "Season: Y" shows the flower_lifecycle_manager's random seasonal quality roll. Each has its own color coding. Panel height increased from 82 to 92px to fit both rows. Replaced combined _get_season_grade() with _get_month_grade() and _get_season_roll() helpers.
[2026-03-25 09:30:00 CST] Completed Queen Finder sub-GDD audit and systems impact update. (1) Updated S2 difficulty scaling table -- bee counts now reference the 1:100 nurse-to-visible ratio with worked examples instead of static mockup numbers (e.g., "8-19 visible bees via 1:100 ratio" instead of "30-40 bees"). Added note clarifying that mockup images use fixed counts for illustration while in-game counts are always derived from the ratio formula. (2) Updated S9 performance budget -- all estimates now explicitly reference MAX_VISIBLE=120 as the worst-case ceiling from the density model constants, with note that typical colonies produce 15-55 visible bees. (3) Added new Section 12: Systems Impact -- Required Modifications. Lists every existing .gd file needing changes (InspectionOverlay.gd, FrameRenderer.gd, HiveSimulation.gd, CellStateTransition.gd, QueenBehavior.gd, GameData.gd) with specific required changes and new constants. Lists new files needed (BeeOverlay.gd, bee_spritesheet_atlas.png, bee_atlas.tres). Includes 8-step recommended implementation order with dependencies. Ends with a full constants cross-reference table (density model, movement, interaction, attendant circle). (4) Updated sidebar navigation with Section 12 links. (5) Updated file tree annotations to mark sim images as "(mockup)".
[2026-03-25 09:00:00 CST] Updated Queen Finder density model from arbitrary VISUAL_SCALE (0.012) to concrete 1:100 nurse-to-visible ratio. Added real hive population reference table (nuc 3500 nurses through booming 15000+), per-frame proportional distribution via brood share, random jitter of +/-2-3 bees after ratio calculation, worked examples showing 9-56 visible bees across colony states, and edge case handling (zero brood frames, queenless hives, colony collapse, super frames using forager_count). Floor of 8 bees minimum, ceiling of 120 for performance.
[2026-03-25 08:30:00 CST] Expanded _Queen_Finder_GDD.html from 7 sections to 11. Added 4 new major sections: (S7) System Hook Points -- full data flow trace from player opens inspection through queen sighting, queen frame assignment algorithm via QUEEN_FRAME_ORDER, 3D ellipsoid brood model with distance formula, hex coordinate system mapping (26px col/15px row/13px odd shift), snapshot pipeline fields consumed by queen finder. (S8) Bee Movement Algorithm -- BeeEntity data structure, worker 3-state machine (WALKING/IDLE/TURNING), queen movement differences (half speed, longer idles, straighter lines), attendant circle retinue system (orbit radius 18px, slow drift at 0.08 rad/sec, grade-scaled 0-12 attendants), all speed/timing constants, temperament/congestion modifiers, target selection algorithms for workers (60% nearby/30% center-biased/10% random) and queens (60% continue/25% adjacent brood/15% slight turn), no-pathfinding direct lerp movement, soft repulsion via spatial hash, density model scaling nurse population to 15-120 visible sprites. (S9) Rendering Pipeline -- 3-layer stack (cell grid/bee overlay/UI), sprite compositing via blend_rect, animation timing tables, performance budget (~4.3ms for 120 bees), adaptive LOD fallback. (S10) Player Interaction -- click detection with 18px hit radius, reverse Y-order testing, feedback states (gold highlight for queen, red flash for wrong bee, no penalties), knowledge tier gating for post-sighting info display.
[2026-03-25 07:00:00 CST] Created _Queen_Finder_GDD.html sub-GDD in research/queenFinder/. Full design document covering the Find the Queen mechanic: design intent, probability model (60% base +1%/level, 80% cap, +15 XP), visual tells (abdomen length, wing proportion, fuzz, leg splay), breed difficulty ranking (Italian easiest to Russian hardest), sprite asset spec (60x42 cell, 5x superscale, 8 directions, 7 frames), all 12 simulation mockups documented by tier, InspectionOverlay.gd integration details, XP reward flow, and Phase 2 roadmap (rendered queen, attendant circle, movement patterns, marking system). Renamed sim files to sequential numbered format (01-24) grouped easy/medium/hard with challenge+answer pairs. Underscore prefix ensures GDD sorts first in directory.
[2026-03-25 06:00:00 CST] Queen Finder asset overhaul: (1) Dimmed wing colors significantly across all 5 breed palettes -- wing_fill dropped from ~140 to ~90 RGB, wing_edge from ~120 to ~76, wing_vein from ~105 to ~62 for much more subdued, realistic appearance. (2) Fixed queen abdomen cutoff -- expanded cell size from 56x40 to 60x42, shifted bee center left, reduced queen abdomen rx from 15.5 to 14.5 so tip stays well inside cell boundary during rotation. (3) Added queen_species_comparison.png -- 8x closeup of all 5 queen breeds side-by-side with worker counterpart below for visual reference. (4) Expanded find-the-queen simulations from 6 to 12 images: added 2 easier (Caucasian Sparse 30 bees, Italian Honey 38 bees), 2 harder (Buckfast Dense 82, Italian Crowded 78), 2 very hard (Carniolan Swarm 110, Russian Overflow 115). (5) Reduced sim frame from 960x540 to 800x450 and preview scale from 5x to 3x to keep all images within pixel limits. All PNGs use optimize=True compression. (6) Deleted entire delete/ directory (old player sprites, uncle bob duplicates). (7) Removed stale .import files from queenFinder directory.
[2026-03-25 01:30:00 CST] Regenerated all 20 item sprites at 32x32 (was 16x16) to match existing art fidelity. All sprites in assets/sprites/items/ are now consistently 32x32. Higher resolution allows for better detail: outlines, shading, hex patterns on honeycomb, label text on bottles, wood grain, wire mesh on queen cage, etc. Removed stale .import files so Godot reimports fresh on next launch. Fixed size mismatch that caused right-side hotbar slots to render incorrectly.
[2026-03-25 01:15:00 CST] Generated 20 new item sprites and hooked them into the hotbar. Items now display their actual icon in hotbar slots instead of flat color fills. Added TextureRect icon layer to each slot in _build_hotbar(), new _load_item_textures() preloads all 25 item-to-sprite mappings at HUD startup, update_player_inventory() shows the sprite when available (dark slot background behind it) with color fallback for unmapped items. New sprites: raw_honey, pollen, seeds, frames, super_box, beehive, hive_stand, deep_body, hive_lid, treatment_oxalic, treatment_formic, syrup_feeder, queen_cage, deep_box, queen_excluder, full_super, jar, honey_bulk, fermented_honey, chest. Existing sprites reused: beeswax, hive_tool, honey_jar_standard, package_bees, sugar_syrup.
[2026-03-25 00:50:00 CST] Fixed magenta atlas crash: changed FrameRenderer._ensure_atlas() to load the PNG directly via Image.load_from_file() using ProjectSettings.globalize_path(), bypassing Godot's import cache entirely. This means external edits to cell_atlas.png are picked up immediately without reimport. Restored the .import file that was accidentally deleted. The old load() path went through the resource system which requires a valid .ctex cache file.
[2026-03-25 00:40:00 CST] Fixed nuc hive inspection lockout: added _is_nuc_hive flag to hive.gd. Nuc hives (ITEM_BEEHIVE) now explicitly bypass the 7-day establishment lockout in can_inspect() — inspectable from the moment they're placed. colony_install_day set to 0 (epoch) as belt-and-suspenders. Package Bees colonies still have the full lockout.
[2026-03-25 00:35:00 CST] Completed hives (ITEM_BEEHIVE) now spawn with an active nuc colony: center 5 frames have drawn comb and brood (3D ellipsoid dome pattern), outer 5 frames are blank foundation for bees to draw out. Colony is pre-established, simulation ticks immediately. place_as_complete() now sets colony_installed=true, calls simulation.init_as_nuc(), and activates ticking.
[2026-03-25 00:30:00 CST] Fixed Godot texture cache preventing dark foundation from appearing in-game. Deleted stale .ctex cache and .import file for cell_atlas.png — Godot was serving the old bright atlas from its import cache, ignoring the updated PNG. Godot will reimport fresh on next launch. Foundation fill is now 8% brightness of drawn comb — unmistakable dark grey vs white contrast.
[2026-03-25 00:20:00 CST] Dev mode cell tooltip now shows wax status and 3D distance. Hovering a cell in dev mode shows: "(col,row) StateName age N [WAX/NO WAX] d3d=0.XX". [WAX] = cell has drawn comb (any state except foundation). [NO WAX] = bare foundation. d3d = normalized 3D distance from hive center (useful for verifying ellipsoid build pattern).
[2026-03-25 00:15:00 CST] Darkened foundation cells dramatically in cell_atlas.png: fill now (18,16,14) nearly black, outlines (75,70,62) medium-dark grey — unmistakable contrast against drawn comb (218,205,156). LOD palette state 0 updated to (0.07,0.06,0.05). Honeycomb background fill darkened to (0.05,0.04,0.03). Foundation cells are now clearly dark grey with visible hex pattern; drawn comb is bright by comparison. Draw rate verified: ~2.8% after 7 days at moderate forage — appropriately slow ramp.
[2026-03-25 00:00:00 CST] Fixed critical bug: HiveSimulation._ready() was auto-registering with HiveManager and seeding nuc brood for ALL hives, including empty ones waiting for Package Bees. This caused empty hives to tick daily (drawing comb, aging brood) before the player installed bees — by inspection time all foundation was drawn. Fix: _ready() now only creates empty foundation boxes without registering or seeding. New init_as_nuc() method handles nuc initialization + registration. init_as_package() now also registers with HiveManager. Simulation only ticks after explicit colony initialization.
[2026-03-24 23:45:00 CST] Rewrote comb drawing and queen laying to use 3D ellipsoid geometry. Both systems now model a true dome-shaped brood nest expanding from the 3D center of the hive (frame 4.5, column 35, row 15 — upper-center). New _cell_3d_dist() computes normalized distance from this point using configurable ellipsoid radii (RZ=5 frames, RX=38 cols, RY=42 rows). Comb drawing collects all foundation cells, sorts by 3D distance, and draws closest first — middle frames fill before outer frames, center of each frame before edges, creating the natural dome real bees build. Queen laying uses same 3D sort: she fills cells nearest the hive center first, only within the ellipsoid boundary (dist<=1.0) and only in walled-in drawn cells. Nuc _seed_initial_brood() also converted to 3D ellipsoid (BROOD_RADIUS=0.55) so starting brood forms a proper dome across frames 2-6. Removed old 2D ELLIPSE_RX/RY_RATIO constants.
[2026-03-24 23:30:00 CST] Three fixes: (1) Greyscale foundation atlas — increased contrast on state 0 hex outlines (105,100,90 vs old 65,62,58) so cell structure is clearly visible against dark fill (38,35,32), updated LOD palette to match. (2) Comb building now starts day 1 — lowered honey threshold from 3.0 lbs to 1.0 lbs absolute minimum. Package bees (starting with 2.0 lbs) now draw comb immediately at slow startup rate (~15-30% speed), ramping up as stores grow. Three-tier system: 1-3 lbs (slow), 3-8 lbs (moderate), 8+ lbs (full). (3) Dandelions now sprout day 1 of spring — removed bloom delay system (was day 12-28), spawn immediately when Quickening starts. Quality roll still affects density/NU, not timing.
[2026-03-24 23:15:00 CST] Implemented _cell_walled_in() hex adjacency check in HiveSimulation.gd: queen can only lay eggs in drawn-empty cells where all 6 hex neighbours (pointy-top offset grid, odd rows shifted right) are also drawn comb (not foundation). Edge cells always return false. Completes the queen laying constraint — prevents queen from laying on the frontier of drawn comb, requiring a complete "wall" of wax around each cell before it's eligible for eggs. Also: greyscale black plastic foundation visual applied to cell_atlas.png state 0, LOD palette updated, honeycomb background darkened. Wax production now costs 0.0004 lbs honey per cell drawn, with store thresholds (stops below 3 lbs, reduced rate 3-8 lbs).
[2026-03-24 23:00:00 CST] Comb drawing now scales with nectar availability (forage level). _draw_comb() receives forage parameter from tick(); base rate from house bees is multiplied by forage_mult (0.05 at dearth → 1.0 at full flow). Good months (S/A rank) = fast comb building (~800-1200 cells/day); poor months (D/F) = near stall (~50-200 cells/day). This directly limits queen's laying area since she can only lay in drawn comb. Added season grade display to dev mode UI panel: "Grade: S/A/B/C/D/F" label color-coded (gold/green/neutral/orange/red-orange/red), reads from FlowerLifecycleManager.get_current_rank(). Dev panel expanded from 72px to 82px to fit new label. Grade refreshes in _refresh_dev_widget().
[2026-03-24 22:45:00 CST] Implemented realistic package bee colony establishment. Package colonies now differ fundamentally from nuc-style initialization: all 10 frames start as empty foundation (no drawn comb), ~8000 loose bees (smaller than nuc), minimal stores (2 lbs honey, 0.3 pollen), very low mite load (15). Queen has randomized species/grade and a 4-6 day laying_delay before she starts laying (simulating cage release + acclimation). New _draw_comb() method in HiveSimulation.gd progressively converts foundation to drawn comb center-out (~960 cells/day). Added 7-day inspection lockout: colony_install_day tracks when bees were installed, can_inspect() returns false for 7 days, player.gd enforces lockout, prompt shows "Establishing... Xd" countdown. tick() now decrements laying_delay each day and skips _queen_lay() until delay expires. After lockout, player can inspect and discover queen quality for the first time.
[2026-03-24 22:30:00 CST] Added Package Bees item (ITEM_PACKAGE_BEES) and colony installation mechanic. New item added to GameData constants, item_registry.json (with generated sprite), player starting inventory (5x), HUD hotbar (golden slot color, short "Bees", long "Package Bees"). Added colony_installed flag to hive.gd — structurally complete hives now start EMPTY with no simulation. Player must select Package Bees and press E near an empty complete hive to "[E] Install Colony", which consumes 1 Package Bees, sets colony_installed=true, and starts the simulation. Each hive is its own independent unit with unique frame/cell data. Prompts are context-sensitive: empty hive shows "Needs Package Bees" or "[E] Install Colony"; colonized hive shows "Equip Hive Tool" or "[E] Inspect Hive". place_as_complete() and try_add_lid() no longer auto-start simulation.
[2026-03-24 22:20:00 CST] Made hive prompt labels context-sensitive based on player's held item. Complete hives show "[E] Inspect Hive" when hive tool is selected, "Equip Hive Tool" otherwise. Building-phase hives show actionable "[E] Add..." only when the correct item is held, otherwise show "Needs..." hint. Renamed _get_active_item_name to public get_active_item_name so hive.gd can read the player's held item. Prompt now refreshes every frame while player is in range.
[2026-03-24 22:15:00 CST] Fixed selected-item label not updating on slot change: set_active_slot() now triggers a full inventory refresh so the lower-left label updates in real time when scrolling or pressing number keys.
[2026-03-24 22:10:00 CST] Re-added selected item name label, now positioned in the empty lower-left corner of the hotbar bar (left-aligned, amber accent text, font size 5). Shows the long name of the currently selected inventory item.
[2026-03-24 22:00:00 CST] Added Hive Tool as inventory item: new ITEM_HIVE_TOOL constant in GameData.gd, added to player starting inventory (1x), item_registry.json, and HUD hotbar (slot color, short/long name). Hive inspection now requires Hive Tool to be selected in hotbar — prompt updated to "[Hive Tool + E] Inspect". Removed entire bottom UI bar (honey/money/energy/level/XP/key hints) — only inventory hotbar remains at screen bottom. Removed active item name label above hotbar and per-slot short name labels — player identifies items visually by slot color only. Hotbar repositioned to sit flush at viewport bottom.
[2026-03-24 21:25:00 CST] Fixed two persistent HUD bugs in scripts/ui/hud.gd: (1) NextDayButton now only visible in dev mode (G key) by syncing its visibility with dev_labels_visible in _on_dev_toggled(), (2) hotbar inventory slots now populate correctly by calling update_player_inventory() from _refresh_all(). Also moved _hide_legacy_nodes() to end of _ready() to ensure all signals are connected before hiding, preventing subtle visibility state issues.
[2026-03-20 22:29:50 CST] Implemented a dynamically generated Inventory UI inside `scripts/ui/hud.gd` that shows a 10x2 grid when toggled with the "O" key. The UI elements were explicitly rescaled to match the engine's 16x16 pixel art tiles and properly fit within the 320x180 screen resolution with zero border separation.
[2026-03-20 22:37:00 CST] Rewrote the targeter tile coordinates logic in scripts/core/player.gd to use strict integer tile snapping from the player's current physical map cell, matching classical farm sim momentum and resolving offset inaccuracies.
[2026-03-20 22:40:00 CST] Fixed a bug where the Inventory UI was invisible on screen by replacing implicit CanvasLayer anchor presets in hud.gd with explicit Vector2 size and centering coordinates.
[2026-03-24 09:00:00 CST] Wired 10-script simulation pipeline in HiveSimulation.gd: tick() now calls NurseSystem, CellStateTransition, PopulationCohortManager, ForagerSystem, CongestionDetector, HiveHealthCalculator, and SnapshotWriter in sequence (Phase 1 — "Quarter Loop It's Alive" complete).
[2026-03-24 09:15:00 CST] Cedar Bend rename complete: created cedar_bend.gd and cedar_bend.tscn, updated map_overlay.gd to reference new scene, and deprecated calders_bluff.gd.
[2026-03-24 09:30:00 CST] Added Uncle Bob NPC stub at scripts/npc/uncle_bob.gd: 6 rotating tutorial hints, +2 XP per talk interaction, auto-dismissing speech bubble, wired into player.gd E-key interaction chain.
[2026-03-24 09:45:00 CST] Implemented queen sighting mechanic in InspectionOverlay.gd: 60% base chance + 1% per level (capped at 80%), awards +15 XP on success, displays animated "👑 Queen confirmed!" notification.
[2026-03-24 10:00:00 CST] Synced TASKS.md: marked Phases 2 and 3 complete (work was already done in code) and updated phase status to reflect current project state.
[2026-03-24 10:30:00 CST] Built Crossroads Diner interior (diner_interior.tscn + diner_interior.gd): full meal system with 5 menu items, time-gated availability, Uncle Bob Tuesdays special, Darlene Friday mornings special (Phase 4 — World & Navigation).
[2026-03-24 10:45:00 CST] Added 6 new diner sprites: rose_waitress.png, chalkboard_menu.png, pie_case.png, diner_stool.png, diner_floor.png, weather_icons.png.
[2026-03-24 11:00:00 CST] Built Feed & Supply interior (feed_supply_interior.tscn + feed_supply_interior.gd): 11 purchasable items with seasonal availability, dynamic bulletin board displaying Grange notices.
[2026-03-24 11:15:00 CST] Built Post Office interior (post_office_interior.tscn + post_office_interior.gd): June the postmaster NPC, package collection UI, spring-only "LIVE BEES" delivery mechanic, new pigeonholes.png sprite.
[2026-03-24 11:30:00 CST] Extended GameData.gd with new fields and helpers: meals_eaten dict, coffee_until_hour, xp_buff_until_day, pending_deliveries array, and associated accessor methods.
[2026-03-24 12:00:00 CST] Expanded TestEnvironment.tscn from 21 to 72 nodes: Uncle Bob placed at (380, 140), 5 hive spot markers, 4 garden beds (clover, phacelia, lavender, sunflower), 14 wildflower patches, DandelionSpawner node.
[2026-03-24 12:15:00 CST] Added 10 new pixel art sprites for test environment: lavender.png, sunflower.png, aster.png, phacelia.png, clover_plant.png, willow.png, wildflower_tile.png, dandelion_tile.png, hive_spot_marker.png, garden_bed.png.
[2026-03-24 12:30:00 CST] Implemented dandelion spring mechanic (dandelion_spawner.gd, 279 lines): annual bloom quality roll (POOR/AVERAGE/GOOD/EXCEPTIONAL), per-quality density ranges, timing shifts, edge tile bias, mow API, and signals.
[2026-03-24 12:45:00 CST] Updated ForageManager.gd to integrate dandelion nectar unit (NU) values from DandelionSpawner signals.
[2026-03-24 13:00:00 CST] Added GDD §14.8.4 documenting the full dandelion spring mechanic: bloom quality tiers, density logic, timing windows, and mow API contract.
[2026-03-24 14:00:00 CST] Code efficiency audit — HiveSimulation.gd tick(): collapsed 7 passes to 2, reducing cell reads per tick from ~245,000 to ~70,000 (~3.5× faster).
[2026-03-24 14:10:00 CST] Code efficiency audit — SnapshotWriter.gd: refactored to reuse pre-computed counts, eliminating all extra cell reads in the hot path.
[2026-03-24 14:20:00 CST] Code efficiency audit — hud.gd: removed redundant _process() override and added string cache to avoid per-frame allocations.
[2026-03-24 14:30:00 CST] Code efficiency audit — grid_overlay.gd: guarded queue_redraw() behind a visibility check so it only fires when the grid overlay is visible.
[2026-03-24 14:40:00 CST] Code efficiency audit — hive.gd: cached player node reference at scene entry to replace per-frame get_tree group traversal.
[2026-03-24 14:50:00 CST] Code efficiency audit — player.gd: removed debug print spam and fixed add_item() triggering a double HUD refresh on every pickup.
[2026-03-24 15:00:00 CST] Code efficiency audit — QuestManager.gd: changed completed_quests storage from Array (O(n) lookup) to Dictionary (O(1) lookup).
[2026-03-24 16:00:00 CST] Full project refactoring: consolidated all .gd scripts under scripts/ hierarchy (autoloads/, core/, simulation/, ui/, npc/, world/), updated all .tscn scene references to match new paths, removed deprecated calders_bluff files.
[2026-03-24 16:15:00 CST] Fixed NotificationManager.gd: changed 8 internal show() calls to notify() (CanvasLayer has no show() method).
[2026-03-24 16:20:00 CST] Fixed player.gd: added _is_ui_blocking() guard preventing WASD movement while inspection overlay or shop is open.
[2026-03-24 16:25:00 CST] Fixed InspectionOverlay.gd: replaced broken Engine.has_singleton() check with get_tree().root.get_node_or_null() pattern.
[2026-03-24 16:30:00 CST] Fixed project.godot: changed TimeManager autoload from fragile UID reference to explicit filepath.
[2026-03-24 17:00:00 CST] Science fixes applied to simulation pipeline: realistic starting populations (24k bees), exponential varroa growth model, corrected nurse:larva ratios, winter consumption ×3.5, nectar→honey 0.20 factor.
[2026-03-24 17:30:00 CST] Updated CongestionDetector.gd: 4-arg evaluate() returning dict with swarm_prep flag, science-based thresholds (honey 0.62, brood 0.65, swarm 0.78).
[2026-03-24 17:35:00 CST] Updated NurseSystem.gd: added capping_delay return (0–2 days when understaffed), corrected IDEAL_NURSE_RATIO to 1.2.
[2026-03-24 18:00:00 CST] Fixed 5 parse errors blocking game launch: removed class_name from DialogueUI.gd (conflicted with autoload name), fixed ambiguous 'not node is Node2D' syntax in player.gd, cleared stale Godot caches (.godot/global_script_class_cache.cfg and uid_cache.bin) left over from file reorganization.
[2026-03-24 19:00:00 CST] Fixed grass tile coverage in TestEnvironment.tscn: replaced 1099 edge/transition grass tiles (atlas 1,0) with solid grass tiles (atlas 12-15,0), filled 464 empty cells with solid grass. Base layer now has 100% coverage (4770/4770 cells) for seamless ground.
[2026-03-24 19:30:00 CST] Fixed Variant type inference errors across project: player.gd min()→mini() (lines 77, 86), HiveSimulation.gd randi() explicit int type (line 247), CellStateTransition.gd min()→mini() (line 275), shop_screen.gd 4× min/max→mini/maxi (lines 61-67), hud.gd max()→maxi() (line 332).
[2026-03-24 19:35:00 CST] Fixed MainMenu.gd: removed invalid mouse_filter on Node2D (line 69), changed load_game()→load_from_disk() to match SaveManager API (line 267), fixed save file path from user://save.dat to user://smokeandmoney_save.json (line 301).
[2026-03-24 19:40:00 CST] Fixed PauseMenu.gd: removed incorrect Engine.has_singleton() call, simplified to get_tree().root.has_node() pattern (line 258).
[2026-03-24 19:45:00 CST] Fixed map_overlay.gd: resized panel from 480×320 to 280×150 to fit 320×180 viewport, adjusted pin positions and confirm dialog layout.
[2026-03-24 19:50:00 CST] Cleared stale Godot caches (global_script_class_cache.cfg, uid_cache.bin) to prevent phantom errors from previous file reorganization.
[2026-03-24 20:00:00 CST] Root directory cleanup: moved research files (hive_sim_harness.py, hive_sim_research_notes.md, GDD_Gap_Workbook.docx, PIXELLAB_STYLE_GUIDE.md) to research/, removed Antigravity/, backups/, TASKS.md, and stale lock file.
[2026-03-24 20:10:00 CST] Fixed dandelion_spawner.gd line 178: explicit int() casts for Dictionary array element access to resolve Variant type inference error.
[2026-03-24 20:15:00 CST] Fixed abs()→absi()/absf() across project: player.gd (lines 256, 343-344, 370-371), grid_overlay.gd (line 82) — all Variant-returning abs() calls replaced with typed alternatives.
[2026-03-24 20:30:00 CST] Full project audit: scanned all 82 .gd files for type inference errors, verified all 226 resource references (scripts, textures, scenes), confirmed zero remaining compilation errors.
[2026-03-24 20:35:00 CST] Fixed flower.gd: is_seeding flag now resets to false outside the fall window (days 112-167), preventing permanent seeding state after first fall season.
[2026-03-24 20:40:00 CST] Added bounds safety checks to InspectionOverlay.gd: _refresh_frame() and _refresh_stats() now guard against empty boxes/frames arrays before access.
[2026-03-24 21:00:00 CST] UI visual identity redesign — Langstroth Frame Aesthetic: regenerated all 38 UI sprite assets in assets/sprites/ui/ with new Langstroth hive frame theme (wooden borders, honeycomb cell fill bars, wax-cream interiors, warm amber palette). Replaces previous generic wood/amber style with a fully themed, coherent visual language.
[2026-03-24 21:05:00 CST] Generated 6 new UI composite preview images in assets/sprites/ui/previews/ (HUD, toasts, dialogue, speech bubble, pause menu, main menu) all rendered in the new Langstroth Frame theme at 860x620.
[2026-03-24 21:10:00 CST] Updated Smoke_and_Honey_GDD.html: added §10.1.4 Langstroth Frame UI Design System (palette tokens, component specifications, button states, fill bar animation spec, font treatment, PixelLab prompt block for UI elements). Updated §10.1 UI Philosophy and §17 UI Systems intro to reference new theme.
[2026-03-24 21:15:00 CST] Updated research/PIXELLAB_STYLE_GUIDE.md: added full UI Asset Direction section with Langstroth Frame palette table, master prompt block, per-element prompt templates (panel, button, fill bar, toast, dialogue, speech bubble, HUD bar), and UI style don'ts.
[2026-03-24 22:00:00 CST] NUCLEAR REWRITE: scripts/ui/hud.gd completely rewritten from scratch to fix script-not-loading bug. Original file (with Unicode box-drawing characters, em-dashes, and emoji in comments) was not executing in Godot 4.6 despite passing gdtoolkit validation. New file is pure ASCII, tab-indented, with identical functionality: top bar (season/month/day/time), bottom bar (honey/money/energy/XP), 10-slot hotbar, dev-mode panels (G key toggle), daily summary overlay. Also fixes: NextDayButton now hidden on startup (only visible in dev mode), G key toggle now properly exits dev mode (signal connection was never established when old script failed to load). Backup saved as hud.gd.broken_backup2.
[2026-03-24 23:00:00 CST] Fixed NectarProcessor.gd: replaced hardcoded CellStateTransition.FRAME_SIZE with frame.grid_size for variable-size frame support (deep 3500 vs super 2450 cells). Made LBS_PER_CELL frame-aware via new static lbs_per_cell(frame) helper. Completes the variable-grid-size refactor across all simulation files.
[2026-03-24 23:15:00 CST] Implemented Storage Chest system (GDD harvest prerequisites): Created chest.gd world object (50-slot persistent storage, placeable by player, proximity prompt), chest_storage.gd modal UI overlay (10x5 chest grid + player hotbar row, WASD navigation, E/Shift+E transfer, Q to switch focus), chest sprite (24x18 pixel art). Added ITEM_CHEST constant to GameData.gd, item_registry.json entry, hud.gd color/name mappings. Integrated chest interaction into player.gd (E to open nearby chest, place from inventory). Placed pre-built StorageChest in TestEnvironment.tscn at (200, 220) near player start.
[2026-03-24 23:30:00 CST] BUGFIX: Invisible hives -- hive.gd was using preload() for 5 new modular sprites (hive_base, hive_deep, hive_super, hive_excluder, hive_lid) that had no Godot .import files. Replaced with runtime Image.load_from_file() loading via static _load_tex() helper. Added null-texture guards in _rebuild_sprite_stack() to fall back to legacy sprite if files are missing. Cleaned 2 em-dash Unicode chars from hive.gd comments.
[2026-03-24 23:35:00 CST] BUGFIX: Chest storage UI not opening -- chest.gd and chest_storage.gd contained 2100+ Unicode characters (em-dashes, box-drawing chars, multiplication signs) that prevented Godot 4.6 from executing the scripts. Rewrote both files in pure ASCII. Same root cause as the earlier hud.gd nuclear rewrite.
[2026-03-24 23:50:00 CST] BUGFIX (round 2): Hive invisible + chest won't open. Root causes: (1) hive.gd had a const Dictionary (const _TEX_PATHS := {}) and static func that may cause parse failure in Godot 4.6 -- replaced with instance method _load_runtime_tex() and inline string paths. (2) player.gd used preload() for chest.gd -- if chest.gd had any issue, player.gd itself would fail to load, breaking ALL player interactions. Changed to runtime load(). (3) Created hive_stand.png sprite (24x8) for the bare stand build phase. (4) Updated _rebuild_sprite_stack() to show proper build-phase visuals: stand-only for STAND_PLACED, stand+deep for BODY_ADDED/FRAMES_PARTIAL, full modular stack for COMPLETE. (5) Cleared all Godot caches.
[2026-03-25 01:15:00 CST] BUGFIX (round 3 -- project-wide): Cleaned ALL non-ASCII characters from every .gd file in the project (42 files, 27,032 characters replaced). player.gd alone had 1,171 non-ASCII chars in comment headers (box-drawing lines, em-dashes, arrows, multiplication signs, section signs) which was silently preventing the ENTIRE script from executing in Godot 4.6 -- this is why NO interactions worked (chest, hive, merchant, all broken). Created .import files for all new sprites and chest_storage.tscn scene wrapper in earlier rounds. Added ASCII-only coding rule to CLAUDE.md to prevent recurrence. Cleared Godot caches.
[2026-03-25 01:30:00 CST] BUGFIX (round 4 -- type inference): Fixed 90+ ":= ternary" and ":= as Type" parse errors across 23 files. Godot 4.6 cannot infer types from ternary expressions (var x := a if cond else b) or as-casts (var x := node as Sprite2D). Replaced all with explicit type annotations (var x: Type = ...). Added ":= prohibition" rule to CLAUDE.md to prevent recurrence.
[2026-03-25 02:00:00 CST] Rewrote hive modular sprite system for isometric stacking. New sprites: hive_stand (24x10 wooden platform with legs), hive_deep_empty (24x16 white-painted box with dark interior), hive_deep (24x16 with frame tops and wax visible), hive_super (24x12 shorter honey super), hive_excluder (24x3 metal grid), hive_lid (24x8 metal-topped telescoping cover), hive_base (24x6 bottom board with entrance). Stacking logic changed from top-down to bottom-up with configurable overlap per layer (3-4px) so each component is visible. Each build phase shows appropriate layers: STAND_PLACED=stand only, BODY_ADDED=stand+empty deep, FRAMES_PARTIAL=stand+deep with frames, COMPLETE=full stack. Z-index per layer ensures proper draw order. Also improved chest.png sprite (20x16 wooden chest with metal hardware).
[2026-03-25 02:30:00 CST] BUGFIX: Parse errors in cedar_bend.gd (get_node_or_null := without type), diner_interior.gd (duplicate var "hour" in same function scope), feed_supply_interior.gd (duplicate var "d" in same function scope x2). All three were editor-load errors from Godot 4.6 strict scoping rules.
[2026-03-25 02:30:00 CST] Redrawn all hive sprites with proper top-down isometric perspective. Previous sprites showed only front face (flat look); new sprites show prominent top surface with shorter front edge, matching the game's overhead camera angle. Top of each box visible: dark interior for empty boxes, alternating frame bars and wax for boxes with frames. Deleted stale .import files so Godot reimports fresh. Fixed _load_runtime_tex() to use Image.new().load() instead of Image.load_from_file() to avoid 0-pixel errors.
[2026-03-25 02:30:00 CST] Implemented box rotation (R key near hive with hive tool): moves bottom deep body to top of deep section. Real beekeeping technique -- queen prefers climbing up, so putting a mostly-empty bottom box on top gives her new laying space and encourages brood expansion. Added rotate_deep_bodies() to HiveSimulation.gd, try_rotate_deeps()/can_rotate_deeps() to hive.gd, _action_rotate_boxes() to player.gd.
[2026-03-25 02:30:00 CST] Implemented multi-box inspection in InspectionOverlay.gd. Player can now navigate between boxes with W/S keys. All boxes (deeps and supers) are inspectable with full 10-frame, sides A/B access. Header shows current box type and number (e.g. "Deep 1", "Super 2"). Progress tracking spans all boxes -- "examined" counter counts across every frame side in every box. Footer updated to show all controls: [A/D] Frame, [F] Flip, [W/S] Box, [ESC] Close.
[2026-03-25 03:00:00 CST] Redrawn all 7 hive component sprites (attempt 3) to match RPG 3/4 camera angle. Previous attempt was too top-down; new sprites match the overworld_hive.png reference with dominant front face and small visible top edge. Sizes: stand 24x8, base 24x4, deep_empty 24x14, deep 24x14, super 24x10, excluder 24x2, lid 24x7. Lid is fully opaque to cover frames beneath it.
[2026-03-25 03:00:00 CST] BUGFIX: hive.gd _rebuild_sprite_stack() referenced undefined variable "top_y" for label positioning -- changed to "y_cursor". Also added missing _rebuild_sprite_stack() calls in try_add_body() and try_add_frames() so build-phase visual updates are immediate (stand -> empty deep -> frames -> complete). Changed sprite stacking from overlap-based to gap-based (1px gap between components, lid sits flush with 0 gap). Deleted stale .import files and cleared Godot caches.
[2026-03-25 03:30:00 CST] Redrawn all 7 hive sprites (attempt 4) with true 45-degree overhead angle. Previous versions were too front-heavy (~85% front); new sprites are ~60% top, ~35% front. You now look DOWN into empty boxes (dark interior visible), see frame bars and wax from above in filled boxes, and the metal lid surface is prominent. New sizes: stand 24x10, base 24x5, deep_empty 24x18, deep 24x18, super 24x14, excluder 24x3, lid 24x10. Also added base layer to BODY_ADDED and FRAMES_PARTIAL build states so lid no longer causes a visual jump.
[2026-03-25 04:00:00 CST] Redrawn hive_stand.png (24x12, 6px wood top + 3px front + 3px legs) and hive_lid.png (24x12, 6px metal top + 3px overhang + 3px inner wood/rim, fully opaque every pixel). Fixed diner_interior.gd duplicate _process function. Converted all := to explicit types in cedar_bend.gd (10), diner_interior.gd (9), feed_supply_interior.gd (25). Cleared all Godot caches.
[2026-03-25 05:00:00 CST] Implemented Gloves item system: Added ITEM_GLOVES constant to GameData.gd, generated gloves.png sprite (32x32 beekeeping gloves), added item_registry.json entry, added hud.gd slot color/short/long name mappings. Created HiveManagementUI overlay (scripts/ui/hive_management.gd + scenes/ui/hive_management.tscn) -- when Gloves active and E pressed near colonized hive, opens management panel with: Add Deep Body (max 2, permanently locked), Add Honey Super (max 10), Add Queen Excluder, Rotate Deeps (bottom to top). Each action consumes items from player inventory. Wired gloves check into player.gd E-key interaction chain (priority above box management actions). Updated HiveSimulation.gd add_super() max from 4 to 10.
[2026-03-25 05:00:00 CST] Updated starting inventory: Player now starts with only 1 hive stand, 2 deep bodies, 4 supers, 1 package bees, 1 gloves, 1 hive tool. All overflow items (4 extra stands, 3 extra deeps, 50 frames, 5 lids, 5 beehives, 5 seeds, 4 extra package bees, 5 queen excluders, 1 extra super, 3 deep boxes, 20 jars) are auto-stocked into the pre-placed storage chest on scene load via deferred _stock_starting_chest() call.
[2026-03-25 05:30:00 CST] Updated starting inventory: Player now gets 1 hive stand, 2 deep bodies, 20 frames, 1 lid, 1 package bees, 1 hive tool. Gloves moved to storage chest along with all other overflow items (4 extra stands, 3 extra deeps, 30 frames, 4 lids, 5 beehives, 5 seeds, 4 extra bees, 5 excluders, 5 supers, 3 deep boxes, 20 jars).
[2026-03-25 05:30:00 CST] Redrawn all 7 hive component sprites in clean off-white painted Langstroth style matching overworld_hive.png aesthetic. Dark pixel-art outlines, off-white painted body surfaces, dark interior visible from above in empty boxes, golden wax/frame bars in filled boxes, silver metal lid top. Crisp clean edges replacing previous brown/muddy palette. Sizes unchanged: stand 24x12, base 24x5, deep_empty 24x18, deep 24x18, super 24x14, excluder 24x3, lid 28x13. Cleared stale .import files and Godot caches.
[2026-03-25 06:00:00 CST] BUGFIX: Game freeze on day advance -- hive.gd line 486 accessed non-existent property TimeManager.current_month (no such property exists). Changed to TimeManager.current_month_index() which is the correct method. This was crashing _update_winter_warning() every day tick.
[2026-03-25 06:00:00 CST] BUGFIX: Hive sprite "0 pixels" loading errors -- improved _load_runtime_tex() with 3-tier fallback: (1) load() resource loader, (2) Image.load_from_file() static method, (3) Image.new().load() instance method. Also handles case where load() returns an Image instead of Texture2D. Added abs_path to error message for debugging.
[2026-03-25 06:30:00 CST] BUGFIX: Honey/wax not visually progressing on frames during inspection. Root cause: _sync_honey_to_frames() was clearing ALL honey/nectar/curing cells from outer frames every single tick and re-stamping them as S_CAPPED_HONEY from the global honey_stores float. This destroyed the natural nectar -> curing -> capped -> premium visual progression that CellStateTransition manages. Replaced with incremental sync: new honey deposits as S_NECTAR (so you see the yellow nectar -> amber curing -> capped progression), and consumption only removes capped/premium cells. The cell state machine now drives the visual lifecycle naturally.
[2026-03-25 06:30:00 CST] BUGFIX: Hive glowing red during colony establishment period. The _update_winter_warning() was checking honey_stores on all colonized hives -- but new package colonies start with only 2 lbs, triggering critical (level 3) red pulse. Now skips the winter warning entirely during the 7-day establishment lockout period and when no colony is installed.
[2026-03-25 18:45:00 CST] MAJOR: Full project rebrand from "BeeKeeper Pro" to "Smoke & Honey". (1) Renamed all code comments in GDScript files from "BeeKeeper Pro" to "Smoke & Honey". (2) Updated save file path from user://beekeeperpro_save.json to user://smoke_and_honey_save.json in SaveManager.gd, MainMenu.gd, test_environment.gd, and GDD. (3) Updated main menu title display string to "Smoke & Honey". (4) Updated project.godot config/name to "Smoke and Honey". (5) Renamed BeeKeeper_Pro_Art_Assets.xlsx to Smoke_and_Honey_Art_Assets.xlsx. (6) Updated all documentation: PIXELLAB_STYLE_GUIDE.md, hive_sim_research_notes.md, GDD_FlowerUpdate_Proposal.md, Harvest_System_Design.md, item_registry.json. (7) Updated BeeKeeper_Pro_DevPlan.html internal text (title, headings) to "Smoke & Honey". (8) Updated morning report and daily sync report branding. (9) Updated workflow.html branding. (10) Created fresh git repository, removing all old history. Remote set to github.com/qLu-jML/smoke-and-honey.
[2026-03-25 07:00:00 CST] Doubled wax drawing rate for better gameplay feel. Base rate from house_count * 0.30 to * 0.60, cap raised from 2000 to 4000 cells/day. Low-store multiplier floor raised (0.15 -> 0.30 at 1 lb stores, 0.45 -> 0.70 at 3 lbs). Forage floor raised 0.05 -> 0.10. A new package colony now draws ~400-800 cells/day in good forage instead of ~150-300, meaning visible comb progress within the first few days.
[2026-03-25 19:30:00 CST] Added GDD Section 8.4: NPC Quest Chains. Comprehensive quest design for all 6 NPCs: Uncle Bob (8 quests, tutorial backbone through Y1-Y3, unlocks Timber & Woodlot apiary), Darlene Kowalski (6 quests, queen management and craft mastery, emergency queen sourcing), Frank Fischbach (5 quests, market economy track, premium shelf space and county fair), Dr. Ellen Harwick (5 quests, science/diagnostics track, IPM certification and sentinel monitoring), Walt & Kacey Harmon (6 quests, farm neighbor track, unlocks Harmon Farm/Orchard apiary via pollination deal), Cedar Valley BKA (4 quests, community track, unlocks River Bottom apiary at Standing 500). Total: 34 quests providing 3,425 XP and 1,150 Standing across all chains. Includes quest interconnection map showing cross-NPC dependencies, total quest economy table, and design notes on pacing, failure recovery, and XP balance. Standalone quest design document also created at Smoke_and_Honey_Quest_Design.html with visual card layout matching the GDD color palette.
[2026-03-25 07:15:00 CST] Reworked wax/comb drawing rate to scale with multiple colony factors. Nectar stores is now the strongest driver (0.2x at near-starvation up to 1.5x with abundant stores of 25+ lbs). Forager count adds a secondary boost (more foragers = more incoming nectar stimulus, 0.3-1.2x). Hive health score is a tertiary factor (sick colonies draw slowly, 0.4-1.0x). Forage availability remains as an environmental modifier. Cap raised to 5000 cells/day. Example rates: new package (2lb stores) ~660 cells/day, established healthy colony (15lb stores) ~5000 cells/day, sick colony (40 health) ~168 cells/day.
[2026-03-25 07:30:00 CST] Moved hive tool to first inventory slot (far left, default selected). Gloves second, then build supplies.
[2026-03-25 07:30:00 CST] Tuned wax production rate so an average colony (4000 house bees, 9lb stores, 2500 foragers, 80 health, 0.7 forage) draws ~5% more cells/day than the queen lays (~1050 eggs/day). Base coefficient reduced from 0.78 to 0.39. Wax still scales dynamically with stores (strongest), foragers, health, and forage -- a booming colony with 25lb stores will draw much faster, while a struggling one falls behind.
[2026-03-25 08:00:00 CST] Enhanced dev mode nectar grade indicator. Now shows per-month nectar grade based on TimeManager.season_factor() mapped to S/A/B/C/D/F letters (S=0.95+, A=0.75+, B=0.55+, C=0.30+, D=0.10+, F=below). Also shows the seasonal quality roll from flower_lifecycle_manager in parentheses when available, e.g. "Grade: A (B)". Color-coded: gold for S, green for A, warm for B, orange for C, red-orange for D, red for F.
[2026-03-25 20:30:00 CST] Created karpathyResearch.html - Karpathy Auto Research Plan report. Identifies 7 scripts for incremental phased testing (HiveSimulation.gd, CellStateTransition.gd, flower_lifecycle_manager.gd, SaveManager.gd, InspectionOverlay.gd, player.gd, hive.gd). Total 41 testing phases across all scripts, organized from smallest isolatable unit to full system integration. Includes recommended execution order based on dependency graph. Dark amber/honey themed HTML matching project style.
[2026-03-25 10:17:10 CST] Created NPC_Quest_Design.html -- Extended 10-quest-per-NPC design document for all Cedar Bend NPCs. 6 NPCs x 10 quests = 60 total quests (~8,200 est. XP). Corrected GDD naming: Darlene Kowalski is the neighbor beekeeper (not the waitress); Rose is the Crossroads Diner waitress; Dr. Ellen Harwick is the extension agent scientist. NPCs covered: Uncle Bob Whitfield (Year 1-5 arc, emotional legacy), June the Postmaster (mail/ordering progression), Rose the Diner Waitress (honey commerce arc), Carl Tanner/Feed & Supply (equipment and merchant arc), Darlene Kowalski (queen mastery and generational legacy), Dr. Ellen Harwick (hive milestone progression 1-10, passes on 30-year research journal and unlocks full simulation data view at 10 hives). Amber/honey color scheme, Langstroth frame CSS header motif, quest cards with XP/Standing/Item/Unlock reward tags.
[2026-03-25 14:30:00 CST] Created Pixel_Art_Learning_Guide.html in project root. Comprehensive 9-section pixel art learning guide for the developer of BeeKeeper Pro, going from scratch to game-ready sprites. Sections: (1) What Is Pixel Art -- philosophy, tools (Aseprite, LibreSprite, GraphicsGale). (2) Core Concepts -- pixels/resolution, pixel grid, anti-aliasing, indexing, transparency. (3) Color Theory -- HSV explained, muted palettes, pillow shading, hue shifting, palette design; 5 palette swatches with hex codes and colored CSS swatches: BeeKeeper Pro Working Palette (all Langstroth Frame UI tokens from PIXELLAB_STYLE_GUIDE.md + world color approximations), ENDESGA 32, Sweetie 16, Nostalgia, Zughy 32. (4) Shading & Lighting -- light source consistency, 3-value rule, dithering patterns, rim lighting, specular highlights. (5) Sprite Construction -- full technical specifications table (all asset types, exact pixel dimensions from GDD section 10.1.1: player 120x120/960x2880 spritesheet, NPCs same, NPC portrait 64x64, tiles 16x16, hotbar items 32x32, hive overworld 16x24, bee particle 8x8, honey jar 16x16, inspection frame 320x192, buildings 80x64, etc.); walk cycle guide (8-frame keyframe breakdown); character tips (silhouette-first, Uncle Bob age rendering, monk baldness technique, flannel plaid dithering). (6) Common Mistakes -- 8 mistake cards covering pillow shading, too many colors, inconsistent light, banding, pure black outlines, scale mismatch, heavy dithering, copying without understanding. (7) Practice Exercises -- 10 exercises beginner to advanced, all specific to Smoke & Honey assets. (8) Resources -- 8 resource cards (Saint11, Mortmort, Brandon James Greer, Lospec, Aseprite docs, GDQuest, Adam Saltsman, Aseprite purchase). (9) Style Consistency -- how to maintain consistent art style across all sprites: locked palette system in Aseprite, one light source rule (upper-left always), no pure black outlines policy, resolution/detail-scale rules table per asset type, shading depth consistency, shadow sprite separation rule, 12-point per-session style checklist, Cainos Benchmark Test, style reference sheet system. Styled to match DevPlan/GDD amber Langstroth frame aesthetic (dark bg, amber accents, sidebar navigation).
[2026-03-27 18:51:00 CST] BUGFIX: Game load failure from bad .import files -- previous flower sprite integration session generated .import files with wrong Godot 4.6 [params] format (roughness/src_normal_path instead of roughness/src_normal, missing compress/uastc_level, process/channel_remap/* keys). Also accidentally modified 7 non-flower .import files. Restored non-flower imports from git, regenerated all 42 flower .import files with correct format, cleared .godot cache.
[2026-03-27 19:30:00 CST] Regenerated all 7 flower species (dandelion, clover, bergamot, coneflower, sunflower, goldenrod, aster) using Leonardo AI with corrected overhead/3-4 top-down perspective and Stardew Valley style reference prompts. Previous sprites were side-view (eye-level) and too large (filling entire 32x32 tiles). New sprites: mature flowers 15-22px content centered in 32x32 canvas, lifecycle stages (seed/sprout/growing/withered) ~8-12px content in 16x16 canvas. Processing pipeline: background removal via color distance thresholding, 5-column splitting, NEAREST-neighbor resize, bottom-aligned centering. Fixed aster processing failure caused by decorative dashed border in source image (cropped 50px from all edges before processing). Integrated 42 sprites into assets/sprites/world/forage/, regenerated .import files preserving all existing UIDs, cleared Godot import cache.
[2026-03-28 19:45:00 CST] BUGFIX: Flower sprites had brown/tan ground/shadow artifacts at their base that didn't blend with grass tiles. Leonardo AI drew flowers sitting on soil -- background removal only caught the cream backdrop, not the ground beneath the plants. Added per-pixel ground color removal in the bottom 40% of each sprite's content area: detects and removes brown/tan ground (warm, desaturated, R > B), dark shadows, and dark olive ground-grass transition pixels while preserving green stems and leaves. All 42 sprites (7 species x 6 lifecycle stages) reprocessed and reintegrated. Godot import cache cleared.
[2026-03-28 20:15:00 CST] Made scene transition zones visible and draggable in the Godot editor. Previously all exit zones (walk-to scene transitions) and door zones (building entry triggers) were created dynamically in code, invisible in the 2D editor. Changes: (1) Added @tool annotation and _draw() method to zone_exit.gd -- draws colored semi-transparent rectangles matching collision shapes in the editor, with label text overlay. (2) Created door_zone.gd (@tool Area2D script) for building door entry triggers with same editor debug draw. (3) Added ExitRight_CountyRoad, FarmhouseDoor, and HoneyHouseDoor as actual nodes in home_property.tscn with CollisionShape2D children -- these are now selectable and draggable in the editor. (4) Updated home_property.gd to detect scene-placed zones and skip dynamic creation when they exist (backwards compatible fallback). (5) Fixed bug: _on_door_entered() called SceneManager.change_scene() which doesn't exist -- changed to SceneManager.go_to_scene().
[2026-03-28 20:45:00 CST] Improved flower distribution: even spread + no spawning on dirt paths. (1) Added no-spawn mask system to flower_lifecycle_manager.gd -- on _ready(), scans TileMap layer 2 (path_ground) to identify dirt path tiles, layer 5 (decorations), and building footprints (Farmhouse, HoneyHouse with buffer). Converts 32x32 TileMap cells to 16x16 flower-grid coords and blocks spawning with 1-cell buffer around paths. (2) Replaced per-tile random density roll with grid-cell sampling for even distribution -- divides the map into 6x6 tile cells (~96x96px) and places a controlled flower count per cell via partial Fisher-Yates shuffle. Eliminates random clumping while keeping natural variation. (3) Added _can_spawn_at() check to both _initial_spawn() and _spread_flowers() so flowers never grow on or spread to blocked tiles. 111 path tiles + building footprints = ~280 blocked flower cells.
[2026-03-28 21:00:00 CST] Drastically reduced flower sprite size. Mature flowers were 16-22px content in 32x32 canvas -- nearly as wide as the player character (~30px). Reprocessed all 42 sprites: mature flowers now 7-10px content in 16x16 canvas, lifecycle stages 3-6px in 16x16 canvas. Flowers are now roughly 1/3 player width, matching the scale expected for small ground-level plants in a 3/4 top-down RPG. Also tightened per-tile visual variation (scale 0.90-1.10 instead of 0.85-1.10, rotation reduced).
[2026-03-28 22:15:00 CST] RESEARCH: Updated Karpathy research machine Phases 6-7 to use S-tier baseline system. (1) Phase 6 (environment_sim.py): imported S-tier calendar constants for 224-day year validation, updated test_5_year_forage_consistency() to verify S-tier seasons produce significantly higher forage availability than D/F seasons (verified with 5 years of rank-randomized season data). (2) Phase 7 (colony_behavior_sim.py): imported S-tier colony targets (55k-70k summer peak, 20k-30k winter minimum), updated test_healthy_colony_score() to use S-tier summer population and expect health >70, updated test_full_year_health_trajectory() to use S-tier seasonal population curve with S-grade queen (health >70 summer, 30-60 winter), updated test_5_year_colony_lifecycle() to use S-tier base populations (65k peak in High-Sun) and verify S-tier colony survives all 5 years with avg health >= 65 through year 3. All tests pass. ASCII encoding verified on all files.
[2026-03-28 23:30:00 CST] RESEARCH: S-Tier Baseline & Rank Modifier System - Complete overhaul of Karpathy research machine validation targets. Created s_tier_baseline.py as the shared source of truth for all grade-scaled colony performance targets. Philosophy: S-tier (top 5% real-world) is the baseline, and grades A through F derive from it via multipliers (S=1.00, A=0.85, B=0.70, C=0.55, D=0.35, F=0.00). S-tier numbers: queen 2000 eggs/day peak, 55-70k summer adults, 20-30k winter, 300-500 lbs theoretical honey (100-200 lbs in full integration). All 8 phases updated: (P1) brood cycle timing unchanged, full-year and 5-year tests verified. (P2) S-tier starting conditions (12k nurses, 14k house, 10k foragers, 2000 eggs/day queen), mortality rates tuned for 224-day year (summer nurse 0.23%, forager 3.7%, winter nurse 1.8%), validates 55-70k peak and 20-30k winter. (P3) S-tier foraging with efficiency 1.05-1.25x, validates 300-500 lbs theoretical production. (P4) Queen grade multipliers updated to S-tier scale (S=1.00 replaces old 1.25x offset), laying ranges recalibrated. (P5) S-tier varroa doubling 50-80 days (hygiene behavior), treatment validation updated. (P6) Environment forage tied to S-tier season expectations. (P7) Health scoring calibrated to S-tier population numbers (60+ summer health). (P8) Full integration rewritten: removed double sf multiplication in nectar formula (forage_level already includes seasonal bloom), added S-tier IPM varroa treatment (spring oxalic + fall formic acid), capped congestion penalty at 20%, proper S-tier integrated targets (55k+ peak, 100-200 lbs/year honey, 400-800 lbs 5-year total, mites < 50 year-end). 213 tests across 8 phases, 0 failures. Protected files verified (GDD 555,381 bytes, DevPlan 68,801 bytes).
[2026-03-29 01:00:00 CST] Queen Finder bee movement rework -- overhauled BeeOverlay.gd movement system from robotic/on-rails to organic insect-like behavior. 5 major improvements: (1) Speed variation with acceleration/deceleration curves (speed_mult 0.4-1.4x based on distance-to-target) plus random burst sprints (3% chance per frame, 1.8-2.5x speed). (2) Micro-wobble via perpendicular sine oscillation (0.3-0.8px amplitude) giving subtle lateral drift during walks. (3) Idle behavior variation -- 3 idle types (normal pause, antenna clean with subtle bobbing, 360 scan rotation) instead of uniform standing still. (4) Attendant orbit improvements -- drift speed variation (0.05-0.12 rad/sec) and orbit radius wobble so attendants don't look like synchronized satellites. (5) Queen movement polish -- micro-pauses (15% chance per new target, 0.3-0.6s), slower base speed (14-18 px/sec vs worker 28-36), dignified deliberate movement. All changes preserve existing rendering, hit-testing, and density code. ASCII-only verified, 887 lines.
[2026-03-29 02:00:00 CST] RESEARCH: Generated 48 Leonardo AI bee sprite candidates for Queen Finder minigame -- all 5 honeybee species (Italian, Buckfast, Carniolan, Caucasian, Russian) in worker and queen variants. Strict pixel art style: limited 16-color palette, no anti-aliasing, hard pixel edges, SNES retro style, top-down view matching game perspective. Created interactive HTML review report (research/queenFinder/Bee_Sprite_Review.html) with all 48 images organized into 12 generation sets, breed assignment dropdowns, variant selection, and breed color reference guide. Pending Nathan's review to select preferred sprites for spritesheet processing.
[2026-03-30 10:00:00 CST] CRAFTING SYSTEM -- Tree chopping and workbench crafting (Phase 4 feature). Full resource chain: chop trees on County Road -> earn logs -> convert logs to lumber at workbench -> craft frames, super boxes, deep bodies, hive stands, and lids. Changes: (1) GameData.gd: Added ITEM_LOGS, ITEM_LUMBER, ITEM_AXE constants; WORKBENCH_RECIPES array with 5 recipes (frames 10-pack/3 lumber, super box/4 lumber, deep body/5 lumber, hive stand/2 lumber, hive lid/2 lumber); chopped_trees regrowth tracking dictionary with 21-day regrow timer; chop_tree() and is_tree_chopped() helper functions. (2) chopping_minigame.gd: New CanvasLayer minigame -- oscillating power meter with green sweet spot and yellow OK zones. Player presses SPACE to time axe swings. 7 good hits to fell a tree, sweet spot shifts and meter speeds up after each swing. Earns 3-5 logs based on accuracy. Costs 15 energy, awards 15 XP. (3) harvestable_tree.gd: Interactable Node2D with colorblock trunk/canopy visuals, collision body, proximity hint label, and chopped/stump state. Tracks regrowth via GameData. (4) county_road.gd: Spawns 8 harvestable trees (2 oak, 2 ash, 1 maple, 1 walnut, 1 elm, 1 hickory) along the north tree line of the county road. Added _process() for hint visibility, _try_chop_nearest_tree() for E-key interaction, minigame launch/completion/cancellation handlers. (5) workbench_ui.gd: CanvasLayer crafting menu with recipe list, lumber/logs resource display, cursor selection (UP/DOWN/ENTER), C key to batch-convert logs to lumber, per-recipe energy costs. Visual feedback for crafting success/failure. (6) home_property.gd: Added workbench Node2D near buildings at (520,-20) with brown table colorblock visuals, E-key proximity interaction, workbench UI open/close. Registered as map POI. (7) player.gd: Added logs/lumber/axe stack sizes (20/20/1). Crafting is a cheaper alternative to buying at Tanner's Supply -- frames cost 3 lumber (free vs $18), super boxes cost 4 lumber (free vs $28), etc. Trees regrow after 21 in-game days.
[2026-03-30 10:30:00 CST] GDD UPDATES -- Documented crafting system in three GDD sections. (1) Section 6.8: Added "Lumber Crafting (Shed Workbench)" subsection with full recipe table (5 recipes with lumber cost, energy cost, output, and shop price equivalent), resource chain diagram, and economy balance note. (2) Section 7.2: Added "Shed Workbench" as a new apiary structure (available from start, requires lumber from County Road trees). (3) Section 13.2: Added "Harvestable tree line" asset row to County Road scene assets table (8 hardwood trees, 21-day regrowth, placement specs). Added "Tree Chopping Minigame" subsection with full mechanic specification table (swing meter, hit quality, progression, rewards, cost, cancellation). GDD grew from 558,285 to 562,748 bytes. DevPlan unchanged.
[2026-03-30 11:00:00 CST] TOOL REQUIREMENTS -- Axe and Hammer now required for crafting system. (1) GameData.gd: Added ITEM_HAMMER constant. (2) shop_screen.gd: Added Axe ($15, qty 1) and Hammer ($10, qty 1) to the merchant catalogue. (3) player.gd: Added hammer stack size (1). (4) county_road.gd: Tree chopping now checks for axe in player inventory before starting minigame; prints "You need an axe" if missing. (5) harvestable_tree.gd: Tree proximity hint shows "(Need Axe)" when player lacks axe, "[E] Chop" when they have one. (6) workbench_ui.gd: Crafting recipes now require hammer in inventory; shows "Need a hammer!" feedback if missing; resource display shows "[Hammer OK]" or "[No Hammer!]" status; recipe highlighting greys out when no hammer. Tools are not consumed on use -- one-time purchases that occupy 1 inventory slot each. (7) GDD updated: Section 6.8 Lumber Crafting now documents tool requirements and $25 upfront investment; Section 13.2 Tree Chopping Minigame table adds "Tool Required" row; shop pricing table adds Axe ($15) and Hammer ($10).
[2026-03-30 11:15:00 CST] Added Axe and Hammer to starting chest inventory in player.gd _stock_starting_chest(). Fresh games now include 1 Axe and 1 Hammer in the storage chest so the crafting system is testable immediately. GDD Section 6.3.6 Starting Inventory updated to include both tools in the chest list.

[2026-03-30 18:17:21 CDT] HIVE MGMT UI CLEANUP -- Rewrote hive_management.gd (gloves screen). Panel expanded from 240x150 to 300x220 for breathing room. Button text shortened and cleaned up: disabled states now show compact [bracket hints] instead of verbose '--' sentences. Added 5th action button: 'Remove Super for Harvest' -- calls new try_remove_top_super() method on hive.gd which removes the topmost super regardless of mark state and gives player ITEM_FULL_SUPER to carry to the Honey House. Also added try_remove_top_super() to hive.gd (inserted between try_add_super and remove_marked_super). All ASCII-clean, no := with ternary.

[2026-03-30 18:49:09 CDT] HIVE MGMT UI + CARRY MECHANIC -- (1) hive_management.gd: Complete UI fix for 320x180 viewport. Panel reduced from 300x220 to 262x160 to fit the native resolution. Added StyleBoxFlat backgrounds/borders to all buttons (normal, hover, pressed, disabled states) so they render as visible clickable boxes instead of floating text. Buttons now 14px tall with title bar strip, info bar strip, and close hint anchored to panel bottom. Added carry-limit check: if player already holds a full_super, Remove Super button shows [hands full\!] and rejects the action. (2) player.gd: Changed ITEM_FULL_SUPER max stack from 2 to 1 (carry one super at a time). Added _carried_super_sprite Sprite2D child node that loads hive_super.png and floats 10px in the players facing direction whenever ITEM_FULL_SUPER is in inventory. Sprite position updates every physics frame as player moves/turns. _update_carry_visual() is also called from update_hud_inventory() so it reflects immediately on pick-up and drop.

[2026-03-30 18:55:25 CDT] HIVE MGMT UI FIX 3 -- Rewrote hive_management.gd UI using the exact same absolute-pixel layout technique as the HUD dev panel (anchor=0, offset=absolute pixels). Previous StyleBoxFlat overrides were invisible because the 0.5-center-anchor approach does not render button backgrounds in this context. New approach: all controls positioned with anchor_left/top/right/bottom = 0 and offset_left/right/top/bottom = absolute pixel coords. Button factory now mirrors _create_dev_button() exactly: loops over all 5 states (normal/hover/pressed/disabled/focus), set_border_width_all(1), set_content_margin_all(0), gold C_ACCENT border, dark bg. Panel is 262x160 centered in 320x180 viewport with gold border ColorRect + inner dark ColorRect. All 5 buttons render as visible gold-bordered boxes.

[2026-03-30 18:59:09 CDT] HIVE MGMT: Smart empty-super removal. Added top_super_has_honey() method to hive.gd: finds the topmost super, iterates all frame cells (both sides) and returns true if any honey/nectar/curing/premium cell is found. Updated hive_management.gd Remove Super button to be context-aware: if top super has honey -> shows 'Remove Super for Harvest' -> gives ITEM_FULL_SUPER (carry limit 1 enforced); if top super is empty -> shows 'Return Empty Super to Inventory' -> gives ITEM_SUPER_BOX back (no carry limit). Status messages differ accordingly.

[2026-03-30 19:06:48 CDT] TEST TUNING -- (1) scraping_minigame.gd: Skip Side B for testing. _check_side_complete() now finishes the frame after Side A is 95% done instead of flipping to Side B. (2) extractor_minigame.gd: Rewrote tuning constants. Old values (decay 1.8, tap 0.15, threshold 60%) required 12 taps/sec -- humanly impossible. New values (decay 0.65, tap 0.22, threshold 40%) require ~3 taps/sec to maintain. Duration reduced from 20s to 10s. Progress rate doubled from 0.055 to 0.10. Threshold label updated to 40%.
[2026-03-30 19:30:00 CDT] SCRAPING MINIGAME VISUAL + BRUSH UPGRADE -- scraping_minigame.gd: (1) Frame now looks like a real Langstroth honey frame -- outer wood border bars (6px thick top/bottom rails + left/right stiles) in dark/mid brown, comb area background in dark amber, center mid-bar divider as an authentic Langstroth frame detail. (2) Cell colors updated: capped cells are golden wax (C_CAPPED), uncapped cells are bright amber honey (C_UNCAPPED). Color legend added to right-side info panel. (3) Brush width widened from 3 cells to 7 cells (BRUSH_HALF=3, range -3 to +3 inclusive) to simulate a wide uncapping knife sweeping across the frame. 7 cells = 56px native = ~336px at 1080p display.
[2026-03-30 20:00:00 CDT] HARVEST YARD PALLET SPRITES -- (1) Generated pallet_super.png (96x64 RGBA) pixel art sprite for the Super Pallet and Scraped Frame Pallet stations in the Harvest Yard. Design is a faithful top-down Langstroth pallet with 2x2 super-slot configuration: 5 vertical planks per quadrant, pale birch/pine colors, dark brown runners/beams top/bottom and left/right edges, center horizontal and vertical divider bars creating the 4 visible super slots, small wooden feet at the base. Saved to assets/sprites/objects/pallet_super.png with correct Godot 4.6 .import file. (2) harvest_yard.gd: Replaced both _draw_station_box ColorRect placeholder draws for SUPER_PALLET and SCRAPED_PALLET with real Sprite2D nodes loaded from pallet_super.png. Both pallets load the same texture and position via _create_pallet_sprites() called from _ready(). The extractor, bottling table, and honey bucket continue to use _draw() ColorRect visuals.
[2026-03-30 19:50:07 CDT] UI FIX -- InspectionOverlay.gd: Repositioned dev advance buttons (+ Day, + Month). Was stacked vertically at GRID_W+2 (overlapping the stats column). Now placed side-by-side in the footer blank space -- right of the instruction text, left of the stats column. bx_month = GRID_W - btn_w - 2 = 242, bx_day = 204, by = VP_H - FOOTER_H + 1 = 169.
[2026-03-30 20:08:14 CDT] HONEY HOUSE SPRITES + BUCKET CARRY -- (1) honey_spinner.png (128x96): top-down stainless steel honey extractor. Circular drum with rotor spokes, amber honey pool, honey gate/spigot at bottom, stand legs visible at corners. Replaces SPINNER_RECT ColorRect in honey_house.gd. (2) honey_bucket.png (32x32): white plastic 5-gallon bucket top-down view. Wire bail handle, amber honey fill visible through lid. (3) honey_house.gd: Added BUCKET station (tile 5,7 -- aisle below spinner). Bucket sprite hidden until spin completes; appears on floor for pickup. Added _player_carrying_bucket state: [E] near bucket picks it up; all station prompts suppressed while carrying except CANNING. Carry label shows lbs in header. Canning requires carrying bucket; auto-releases when drained. Fixed station prompt selection to nearest-only (was last-in-loop causing overlap interference).
[2026-03-30 20:18:36 CDT] UNCAPPING MINIGAME OVERHAUL -- ScrapeFrame.gd (new): minimal 70x35 super-frame class with cells/cells_b PackedByteArrays; fill_for_harvest(pct) populates with S_CAPPED_HONEY/S_CURING_HONEY/S_DRAWN_EMPTY; FrameRenderer-compatible (cells, cells_b, grid_cols/rows/size, get_cell). scraping_minigame.gd (full rewrite): exact InspectionOverlay visual -- same 320x180 viewport, same frame bar dimensions (T8/B5/L4/R4), same wood colours, lug knobs, wire guides, stats panel, and footer. TextureRect at (4,26) size (272,95) same as InspectionOverlay for a 35-row super frame. FrameRenderer.render_honeycomb() drives cell display; hit-detection uses exact FrameRenderer geometry (HEX_COL_STEP=26, HEX_ROW_STEP=15, HEX_ODD_SHIFT=13). Scraping changes S_CAPPED_HONEY -> S_CURING_HONEY live. [F] flips Side B; [ESC] cancels with energy refund. honey_house.gd: _action_super_prep generates real ScrapeFrame objects per frame; _action_uncapping launches overlay instead of auto-uncapping; signal handlers for complete/cancelled; _update_station_prompts returns early while overlay open.

2026-03-30 - Fix dev advance buttons invisible on initial hive inspection open
  - open() is called before add_child() so _apply_tier_visibility() ran before _ready()
    built the UI -- dev buttons were null and missed on the initial visibility sync
  - Moved dev button show/hide into _apply_tier_visibility() (single source of truth)
  - Added _apply_tier_visibility() call at end of _ready() after buttons are built
  - Removed duplicate dev button code from _on_dev_toggled() (now handled by the shared call)

2026-03-30 - Fix de-capping minigame: honeycomb texture not rendering + add scraper brush
  Root cause 1: _cell_rect (TextureRect) was a direct child of CanvasLayer.
    TextureRect needs a Control parent to render its texture in Godot 4.
    All UI nodes now go to _bg (a Control), matching InspectionOverlay's architecture.
  Root cause 2: _render() was called synchronously in _ready(), before the GPU
    texture pipeline was ready. Changed to call_deferred("_render") so it fires
    after the current frame settles -- same as InspectionOverlay's pattern.
  Scraper knife brush visual:
    - Semi-transparent gold highlight shows the 7-column scraping width
    - Spans full frame height (representing uncapping knife coverage)
    - Left/right edge lines suggest the blade edge
    - Visible on hover, updates live, hides on done/flip/ESC

2026-03-30 - Add comb scraper + bucket grip to starting player inventory
  ITEM_COMB_SCRAPER and ITEM_BUCKET_GRIP added to player._ready() starting slots.
  Removed duplicate copies from _stock_starting_chest().

2026-03-30 - Fix de-capping frame rendering dark + add scraper knife handle
  Problem 1: ScrapeFrame.fill_for_harvest() cells were not reliably propagating.
    Changed to build fresh local PackedByteArrays (new_a, new_b), fill them,
    then assign back to cells/cells_b -- avoids any copy-on-write ambiguity.
  Problem 2: scraping_minigame._render() was deferred; changed to direct call
    (same pattern as InspectionOverlay._refresh_frame() which works fine sync).
    Added safety net: if frame is null or has 0 capped cells on arrival,
    create/fill a default ScrapeFrame so the minigame always shows honey.
  Scraper knife handle: added _brush_handle + _brush_hnd_hi ColorRects below
    the frame bottom bar. Handle (dark wood, 8x20px) tracks the blade center,
    giving the visual of a real uncapping knife held from below. Blade width
    (BRUSH_HALF=3, ~27px = 1/10 of cell area) unchanged per user spec.

2026-03-30 - Dev buttons in InspectionOverlay: more visible + auto-toggle with G key
  Added "DEV:" amber label to the left of the +Day/+Month buttons.
  Increased button height from 10 to 11, font size from 4 to 5.
  Brighter button backgrounds (0.28, 0.20, 0.06 vs old 0.15, 0.10, 0.05).
  Border width increased from 1 to 2px.
  Font color changed to bright yellow-white so text stands out on dark footer.
  All three elements (label + 2 buttons) added to "dev_label" group so the
    G key auto-shows/hides them instantly without needing _apply_tier_visibility.

2026-03-30 - De-capping: premium honey cells + full-height blade scraping
  ScrapeFrame._pick_state: changed S_CAPPED_HONEY to S_PREMIUM_HONEY for
    harvest-ready frames (aged honey = state 9, deep amber visually).
  scraping_minigame._try_scrape: blade now covers ALL F_ROWS rows per column,
    not just the cursor row. One horizontal drag from edge to edge clears the
    entire frame. Render + progress update batched once per motion event
    (not per cell) so performance stays smooth even at 70x35 = 2450 cells.
  Refactored _uncap_cell -> _uncap_cell_silent (bool return, no render) with
    a legacy _uncap_cell wrapper for any single-cell callers.

2026-03-30 - Logo art: Almost There bellows-fix report (8 new variations)
  Replaced Almost_There.html entirely with 8 new Leonardo AI generations.
  Target: 2D composition (smoker left anchor, text right) + silver cylinder
  + correct small teardrop accordion bellows + hex-O + no drips.
  All 8 batches of 4 images = 32 total. Gen IDs captured. Prompts embedded.
  Variations: Classic Silver, Dramatic Backlit, Aged Patina/Wire Cage,
  Polished Steel/Engraved, Smoke Framed/Teal Wisps, Chrome/Honeycomb Bg,
  Ember Glow/Reddish Bellows, Rolling Smoke/Bees at Base.

2026-03-30 - Logo art: Almost There v3 -- flat-panel bellows fix (ref sketch)
  Replaced Almost_There.html again with 8 new Leonardo AI generations.
  Corrected smoker anatomy based on reference sketch provided by Nathan:
  - Bellows: flat rectangular leather boards like a closed book with rivet holes
    (NOT teardrop/pear shaped)
  - Lid: wide outward-flaring cone, narrow at base, broad open tube at top
  - Body: squat wide barrel cylinder (wider than tall)
  8 variations x 4 images = 32 total. Gen IDs and prompts embedded.

[2026-03-30 CDT] DESIGN EXPANSION: Gameloop Downtime Analysis -- new GDD sections + DevPlan Phase 4 expansion.
  REPORT: Created research/reports/Gameloop_Downtime_Report_2026-03-30.html
    - Full month-by-month activity audit across all 8 Cedar Bend months
    - Identified ~70-80 days/year of downtime (winter = 56 days, highest risk)
    - Suggested time-filler categories: crafting (major), property maintenance,
      forage/land, community, knowledge/planning
    - Prioritized implementation table with effort/impact ratings
    - Key finding: farmhouse mead (Y1 unlock) + passive crafting queue = highest impact
  GDD: Section 6.8 (Crafting System) completely rewritten and expanded with 9 subsections:
    - 6.8.1 Master Recipe Table: expanded to ~20 products with prices
    - 6.8.2 Passive Crafting Queue: background tick system, check-in actions
    - 6.8.3 Infused Honey System: lavender/hot honey/spiced variants, cross-season loop
    - 6.8.4 Candle Variety System: tea light/taper/pillar/novelty, flower pressing mechanic
    - 6.8.5 Honey Gift Sets: packaging station, 3 set types, 25-30% price premium
    - 6.8.6 Farmhouse Mead: Y1 unlock (no Fermentation Room), 8-12 weeks passive
    - 6.8.7 Propolis Tincture: inspection collection, 4-6 weeks passive, Dr. Harwick quest
    - 6.8.8 Beeswax Furniture Polish: dual use (sell or apply to hive box +10 condition)
    - 6.8.9 Crafting Skill Track: 6 Knowledge Log milestones with quality modifiers
  GDD: New Section 6.10 (Downtime Activity Systems) added with 11 subsections:
    - 6.10.1 Hive Equipment Repair & Maintenance (shed sorting UI, traffic-light system)
    - 6.10.2 Tool Maintenance (wear tracking, neglect penalties)
    - 6.10.3 Property Expansion & Off-Season Construction (4 multi-day projects)
    - 6.10.4 Winter Tree Care & Pruning (dormant pruning, spring fertilization)
    - 6.10.5 Seed Saving & Winter Catalog Orders (Tanner's specialty seeds)
    - 6.10.6 Cover Cropping & Soil Work (winter rye, buckwheat, crimson clover)
    - 6.10.7 CVBA Meeting Expansion (5 optional meeting activities)
    - 6.10.8 Crossroads Diner Regular Status (4-visit streak, packed lunch reward)
    - 6.10.9 Bulletin Board Winter Micro-Quests (Standing 100 unlock, 5 rotating quests)
    - 6.10.10 Beekeeper's Journal & Annual Review (Kindlemonth, +70 XP base)
    - 6.10.11 Spring Planning Map (winter overlay, drag-and-drop markers)
  DEVPLAN: Phase 4 expanded from 4-task crafting block (4.23-4.26) to 33-task system (4.23-4.55):
    - 4.23-4.26 Core structures (CraftingManager autoload, Honey House tiers, Fermentation Room, Queen Rearing)
    - 4.27-4.29 Passive queue (tick system, vessel items, weekly check-in dialogue)
    - 4.30-4.32 Candles (recipe variants, flower pressing, mold catalog)
    - 4.33-4.34 Infused honey (recipes + queue, botanical drying)
    - 4.35-4.37 Mead (farmhouse Y1, traditional Y2, melomel Y3)
    - 4.38-4.39 Propolis (inspection collection, tincture recipe)
    - 4.40-4.41 Gift sets (packaging materials + winter catalog, assembly recipes)
    - 4.42 Beeswax polish (recipe + dual use)
    - 4.43-4.44 Crafting skill (milestone tracking, Knowledge Log entries)
    - 4.45a-4.46b Equipment repair (shed UI, repair wiring, tool wear, maintenance)
    - 4.47a-4.47c Tree care (health system, dormant pruning, spring fertilization)
    - 4.48a-4.48c Seeds (seed saving, winter catalog, bed planning)
    - 4.50a-4.50d Off-season construction (project system, shed lean-to, fence, water station)
    - 4.51a-4.51b CVBA expansion (optional activities, bring-a-guest)
    - 4.52 Diner Regular (attendance tracking + status)
    - 4.53a-4.53b Bulletin board (scene + interaction, winter micro-quest pool)
    - 4.54 Beekeeper's Journal (annual activity)
    - 4.55 Spring Planning Map (overlay)
    - Art callout added: 15+ new sprites + 3 new structures + new props
    - Sidebar nav updated with 18 new Phase 4 anchor links
  Protected files verified: GDD 603,634 bytes, DevPlan 139,015 bytes.

[2026-03-30 CDT] FIX: Harvest yard bucket placement -- accept Bucket Grip or Honey Bucket at bottling table
  Root cause: _action_bottling() checked held == ITEM_HONEY_BUCKET, but player
  still had Bucket Grip selected after picking up the bucket. The prompt showed
  "Carry honey bucket here first" even while visibly carrying the bucket.
  Fix: both the prompt check and _action_bottling() now accept ITEM_HONEY_BUCKET,
  ITEM_BUCKET_GRIP, or count_item(ITEM_HONEY_BUCKET) > 0 as valid carry states.
  Player can walk to the bottling table with bucket grip still selected and press E.

[2026-03-30 CDT] CLEANUP: GDScript debugger warnings (65 -> ~10 expected)
  - flower_lifecycle_manager.gd: removed dead keys_to_remove loop (leftover from
    exclusion-zone revert), renamed unused param advance_day_with_global(new_day)
    to _new_day.
  - ForageManager.gd: renamed unused param get_dominant_plant(world_position) to
    _world_position; added @warning_ignore("UNUSED_SIGNAL") on forage_updated.
  - HiveSimulation.gd: renamed unused param _draw_comb_with_nu(foundation_remaining)
    to _foundation_remaining.
  - NurseSystem.gd: renamed unused param process(stunted_brood) to _stunted_brood.
  - NotificationManager.gd: removed unused local var target_y (line 186).
  - hud.gd: added @warning_ignore("UNUSED_PRIVATE_CLASS_VARIABLE") on _menu_open
    and _honey_energy_pct_lbl.
  - weather_particles.gd: added @warning_ignore("UNUSED_PRIVATE_CLASS_VARIABLE")
    on _spawn_rect.
  - hive.gd: renamed local const GAP_PX to _GAP_PX.
  - scenes/home_property.tscn: fixed stale UID (uid://ykw0xvtgsnlg -> uid://6dyvoe4x0ot4)
    for home_property.gd -- was causing "ext_resource, invalid UID" error on load.
  Protected files verified: GDD 603,634 bytes, DevPlan 139,015 bytes.

[2026-03-30 CDT] FIX: Two-step bottling workflow -- place bucket, then select jars
  Bottling table (harvest_yard.gd) now uses a two-step flow:
  Step 1: Press [E] with bucket carried -> places bucket on table (stays visible).
    Bucket drawn on right side of bottling table with honey fill level.
    New state var _bucket_on_bottling_table tracks this.
  Step 2: Select Empty Jars in inventory, press [E] -> starts jarring minigame.
    Prompt shows "Select Empty Jars to start" until jars are in active slot.
  After jarring completes and jars collected, _bucket_on_bottling_table resets.
  Previously the bucket was consumed and minigame started in one step, which
  was confusing and gave no visual feedback of the bucket on the table.

[2026-03-30 CDT] FEATURE: Step 2 Weather System -- tasks 2.5-2.10 complete
  Tasks already done (confirmed wired):
    2.5  WeatherManager autoload: 8-state system (Sunny/Overcast/Rainy/Windy/Cold/
         HeatWave/Drought/Foggy), season-weighted Iowa climate probabilities, daily
         temperature + wind + humidity generation, full signal system.
    2.6  ForagerSystem wired: HiveSimulation passes WeatherManager.get_forage_multiplier()
         as weather_season_mult into ForagerSystem.process(). Weather directly cuts
         daily NU/PU collection (Rainy/Cold = 0.0x, Windy = 0.4x, Sunny = 1.0x, etc.)
    2.7  HUD weather display: _weather_lbl in top bar, _on_weather_changed() signal
         connected, _refresh_weather() updates text + color on each weather roll.
         Dev panel also shows current weather + probability breakdown.
    2.10 Weather serialization: WeatherManager.collect_save_data() / apply_save_data()
         wired into SaveManager. All weather state persists across saves/loads.
  New work this session:
    2.8  Inspection weather gate (player.gd): After the 7-day colony lockout check,
         player.gd now calls WeatherManager.can_inspect(). Rainy/Cold states block
         hive opening with weather-specific toasts:
           - "Too wet to open hives -- wait for dry weather!" (Rainy)
           - "Too cold to inspect -- bees are clustered!" (Cold)
         Sting multiplier already defined in WeatherManager.INSPECTION_RULES; full
         sting probability system wires in at Step 4 (tasks 2.13-2.16).
    2.9  Season transition banner (hud.gd): _show_season_banner() creates a 200x28
         centered panel that fades in (0.5s), holds (2.5s), fades out (0.8s) on each
         season change. Season-themed color pairs (green/Spring, gold/Summer,
         orange/Fall, blue/Winter). Triggered from _on_season_changed() signal.
         NotificationManager toast still fires alongside it (no duplication -- they
         serve different visual purposes: banner = prominent center, toast = log).
  DevPlan updated: tasks 2.5-2.10 marked DONE.
  Protected files verified: GDD 603,634 bytes, DevPlan 139,015 bytes.

[2026-03-31 CDT] FEATURE: Startup screen -- logo, main theme, Spring/Fall start modes
  MainMenu.gd rewritten as a full startup screen:
    - Game logo (assets/ui/logo/game_logo.png) displayed centered in upper half
      of viewport above the button panel.
    - Main theme music (assets/audio/main_theme/mainTheme.mp3) plays on entry
      via new MusicManager.play_title_theme() and MusicManager.resume_seasonal_music().
    - "New Game" replaced with two distinct start buttons:
        [Start in Spring]              -- day 1 (Quickening), overwintered S-grade
                                          Carniolan colony, standard new game.
        [Start in Fall | 2 Full Supers] -- day 113 (Full-Earth), A-grade Carniolan
                                          colony at peak population with 2 honey supers
                                          filled and capped, ready for fall harvest.
    - Continue and Quit remain; Settings stub removed.
  New files / functions:
    - GameData.new_game_mode (int): 0 = spring, 1 = fall. Reset to 0 after hive spawns.
    - MusicManager.play_title_theme(): plays mainTheme.mp3, resets seasonal index.
    - MusicManager.resume_seasonal_music(): hands music back to seasonal system.
    - HiveSimulation.init_as_fall_harvest(): strong fall colony, 2 filled supers,
      honey_stores=24 lbs, population ~32k bees, manageable mite load (45).
    - hive.gd place_as_fall_harvest(): wraps init_as_fall_harvest(), bypasses lockout.
    - home_property._spawn_starter_hive(): branches on GameData.new_game_mode:
        mode 0 -> place_as_overwintered("Carniolan","S")
        mode 1 -> place_as_fall_harvest("Carniolan","A") + GameData.new_game_mode = 0
  Protected files verified: GDD 603,634 bytes, DevPlan 139,437 bytes.

[2026-03-31 CDT] OVERNIGHT ITERATION: Phase 2 + Phase 3 foundations implemented
  Tasks 2.11-2.16: Smoke & Sting System
    - ITEM_SMOKER and ITEM_BEE_SUIT added to GameData constants
    - ITEM_PROPOLIS added as collectible resource
    - smoker + bee_suit added to Feed & Supply shop catalogue ($25 / $45)
    - Smoker mechanic: hold smoker and press E near hive to puff it
      _smoker_active=true, _smoker_puffs=3; resets after inspection closes
    - Sting probability: 25% base chance per inspection
      x weather mult (WeatherManager.get_sting_multiplier())
      x 0.3 if bee suit worn
      x 0.2 if smoked
      Energy penalty: 5 (with suit) or 10 (no suit)
    - InspectionOverlay.closed signal wired -> player._on_inspection_closed()
    - Notification toasts: "Ouch! You got stung! (-X energy)" on sting
  Tasks 2.22-2.23: Harvest completion
    - 2.22 Beeswax tracking: confirmed already implemented in harvest_yard.gd
      (~0.735 lbs per frame, accumulated in _bucket_beeswax_lbs, added to inv on collection)
    - 2.23 Winter reserve warning: player.gd warns when removing super in Fall
      with < 60 lbs honey stores ("colony only has X lbs honey -- 60 lbs needed for winter!")
    - hive.gd: added get_honey_stores() -> float method
  Tasks 2.25-2.30: Saturday Market + Community Standing
    - 2.25-2.27: Confirmed cedar_bend.gd already wires Saturday Market + sell_screen
      Sell screen shows "Hey [player_name], I'll pay $X/jar!" (name injection via PlayerData)
    - 2.28: GameData 5-tier Community Standing system:
        Stranger(0), Neighbor(100), Known(250), Trusted(500), Respected(750)
        Price mults: 1.0x / 1.05x / 1.10x / 1.15x / 1.20x
        get_standing_tier(), get_standing_name(), get_standing_price_mult()
    - 2.29: Standing gains: +3 per Saturday Market sale, +1 per Feed & Supply sale
    - 2.30: cedar_bend._open_market_sell() applies get_standing_price_mult() to base $10/jar
  Tasks 3.0-3.0d: Character System
    - PlayerData autoload created (scripts/autoloads/PlayerData.gd)
      Fields: player_name, pronoun_they/them/their/theirs/themself, backstory_tag,
              character_created flag
      Methods: set_pronouns_they_them/she_her/he_him(), inject_tokens(text),
               collect_save_data(), apply_save_data(data)
    - CharacterCreator scene + script (scenes/ui/CharacterCreator.tscn +
      scripts/ui/character_creator.gd):
      Keyboard UI: name entry (type name), pronoun selection (A/D), backstory (A/D),
      confirm [E]. Displays before home_property loads on New Game.
    - MainMenu._show_character_creator(mode) wraps both start modes;
      creator_finished signal -> _start_game() -> loading screen
    - project.godot: PlayerData="*res://scripts/autoloads/PlayerData.gd" registered
    - SaveManager: player_data collected and applied in save/load cycle
  Tasks 3.1 + 3.3: QuestManager + Daily Tasks
    - QuestManager expanded: INACTIVE/ACTIVE/COMPLETED/FAILED states,
      objective tracking, rewards (xp, money), quest_completed signal
    - 4 quests defined:
        bob_intro: "Meet Uncle Bob" (+25 XP, Knowledge Log unlock)
        daily_inspection: "Inspect Your Hives" (target 1 inspection, +10 XP, +$5)
        daily_harvest: "Harvest a Full Super" (target 1 super, +15 XP, +$10)
        daily_market: "Sell at Saturday Market" (target 1 sale, +20 XP)
    - Daily quests auto-reset at day start via TimeManager.day_advanced
  HUD Updates
    - hud.gd: "Standing: [tier]" label in bottom bar (amber, polls each frame)
    - hud.gd: "SMOKED" indicator (orange) visible when player._smoker_active=true
  Protected files verified: GDD 603,634 bytes, DevPlan 139,726 bytes.
2026-03-31 - Updated Smoke_and_Honey_Master_Art_Sheet.xlsx: Comprehensive master pixel artist commission sheet. 291 total assets across 4 phases (P1: 81 core loop + location layouts, P2: 61 NPCs/buildings/interiors, P3: 78 environment/props/trees, P4: 71 polish/minigames/wildlife). Includes all 10 location maps (Home, Back Field, County Road, Cedar Bend, Timber, Harmon Farm, Orchard, River Bottom, Fairgrounds, County Map). Added missing NPCs (Lloyd Petersen, Terri Vogel), truck, fairgrounds, ambient wildlife, lighting overlays, and all GDD Section 13 location-specific assets. Protected files verified: GDD 603,634 bytes, DevPlan 139,726 bytes.

[2026-03-31 11:08:37 CDT] DOCS: Created Testing_Guide_2026-03-31.html in research/reports/. Comprehensive QA playtester guide covering all 20 implemented game systems: Main Menu/Start Modes, Character Creator, Player Movement/Controls, HUD, Time & Calendar, Weather System, Hive Building, Hive Management, Inspection (with sting mechanics), Smoker & Bee Suit, Harvest Yard (5-station pipeline), Saturday Market, Feed & Supply Shop, Tree Chopping Minigame, Workbench Crafting, Quest System, Save/Load, Zone Transitions, Dev Mode, Music System. Includes Quick Controls Reference table, per-system Expected Results and Edge Cases callouts, and Test Results Checklist. QR code/feedback form placeholder included (URL to be filled in). 70,659 bytes. Protected files verified: GDD 603,634 bytes, DevPlan 139,726 bytes.

[2026-03-31 11:10:56 CDT] BUGFIX: Main menu not appearing on game launch. project.godot run/main_scene was pointed at res://scenes/home_property.tscn instead of res://scenes/ui/MainMenu.tscn. Fixed -- game now boots to the title screen with logo, Start in Spring / Start in Fall buttons, Continue, and Quit. Protected files verified: GDD 603,634 bytes, DevPlan 139,726 bytes.

[2026-03-31 11:13:53 CDT] FEATURE: ESC menu now accessible + music controls on main pause panel.
  (1) hud.gd: Added KEY_ESCAPE alongside KEY_P in _unhandled_key_input to trigger
      _toggle_pause(). ESC now opens the pause menu on first press (previously only P
      worked, and only after lazy-loading the PauseMenu scene).
  (2) PauseMenu.gd: Music volume slider and Mute Music checkbox added directly to the
      main pause panel below the Resume/Save/Settings/Quit buttons. No longer need to
      click into Settings to adjust music volume. Slider and checkbox sync bidirectionally
      with the Settings sub-panel controls. Dragging the slider above 0% auto-unchecks
      mute. Mute checkbox toggles AudioServer bus mute on the Music bus.
  (3) Settings sub-panel: Also received a Mute Music checkbox (synced with main panel).
      Panel height expanded from 90 to 105. Main panel height expanded from 130 to 156.
  All files ASCII-verified. Protected files verified: GDD 603,634 bytes, DevPlan 139,726 bytes.

[2026-03-31 11:26:24 CDT] UI: MainMenu.gd completely rewritten for clean visual layout.
  Problems fixed:
    - Logo was not loading (load() needed import cache; added Image.load_from_file()
      fallback to bypass Godot's import pipeline, same pattern as player spritesheet)
    - Hex dot pattern looked like chunky squares at 6x window scale -- removed entirely
    - Vignette rectangles were harsh dark blocks -- replaced with subtle warm center glow
    - Button panel was too low and cramped -- buttons now vertically centered below logo
    - Panel border/double-border styling was overly heavy -- simplified to clean buttons
  New layout (320x180 viewport):
    - Dark warm background with subtle center glow and thin amber decorative lines
    - 80x80 game logo centered in upper half of screen
    - 4 buttons centered below: Start in Spring (green accent), Start in Fall (orange),
      Continue (amber, greyed if no save), Quit (muted)
    - Thin decorative dividers above/below button group and between new-game/continue
    - Version line at bottom: v0.1 dev -- Cedar Bend, Iowa
    - 1-second fade-in animation on load
  All functionality preserved: Spring/Fall start modes, character creator flow,
  continue/save detection, MusicManager title theme, ESC to quit.
  ASCII verified. Protected files verified: GDD 603,634 bytes, DevPlan 139,726 bytes.

[2026-03-31 11:33:29 CDT] UI: MainMenu.gd v3 -- fixed logo loading + button layout.
  Logo fix: Now uses Image.load_from_file() as PRIMARY method (same proven pattern
  as player spritesheet loading). Tries PNG then JPG. Falls back to load() as last
  resort. Logo displayed at 160x80 viewport pixels (960x480 at 6x) with
  EXPAND_FIT_WIDTH_PROPORTIONAL + STRETCH_KEEP_ASPECT_CENTERED.
  Button fix: Reduced font_size from 7 to 6, BTN_H from 14 to 12, added
  set_content_margin_all(1) to all StyleBoxFlat overrides so Godot does not
  expand buttons beyond their assigned size. custom_minimum_size set explicitly.
  Removed corner radius. Thinned borders. Cleaner visual hierarchy.
  Protected files verified: GDD 603,634 bytes, DevPlan 139,726 bytes.

[2026-03-31 11:47:14 CDT] DOCS: Testing Guide updated to v2. Rewrote Section 1 (Main Menu) to match new MainMenu.gd layout (logo loading, 4-button layout, dark background). Added new Section 21: ESC/Pause Menu covering music volume slider, mute checkbox, bidirectional sync, Resume/Save/Settings/Quit. Updated Quick Controls reference (ESC key). Added row 21 to Test Results Checklist. 77,000 bytes. Protected files verified: GDD 603,634 bytes, DevPlan 139,726 bytes.

[2026-03-31 21:19:09 CDT] DOCS: Testing Guide recreated from scratch as v3.
  Full code audit of all game scripts, GDD, and DevPlan performed before writing.
  Comprehensive 66,539-byte HTML guide covering all 21 implemented systems:
  Main Menu (3-button layout, logo loading), Character Creator, Player Movement,
  HUD, Pause Menu (exit confirmation, music controls), Time/Calendar, Weather,
  Hive Management, Forage System, Harvest Loop, Inventory, Economy/Merchants,
  Quest System, NPC/Dialogue, Save/Load, Music/Audio, Notifications, Scene
  Transitions, Seasonal Content, Keyboard Controls, and Performance.
  Includes QR code link to feedback form, test results checklist, and
  GDD gap analysis. Saved to research/reports/Testing_Guide_2026-03-31.html.
  Protected files verified: GDD 603,634 bytes, DevPlan 139,726 bytes.

[2026-03-31 21:23:45 CDT] DOCS: Testing Guide converted to PDF format.
  Built 21-page styled PDF (56,249 bytes) from HTML source using reportlab.
  Amber/honey themed: section circles, colored prerequisite/expected/edge-case
  boxes, controls reference table with alternating rows, test results checklist.
  Saved to research/reports/Testing_Guide_2026-03-31.pdf alongside HTML version.
  Protected files verified: GDD 603,634 bytes, DevPlan 139,726 bytes.

[2026-03-31 22:45:00 CDT] DOCS: Playtester feedback form and PDF QR code.
  Created Google Form "Smoke & Honey - Playtester Feedback" in Five Cats
  Gmail workspace with 7 questions: Name/Tester ID (short answer), Systems
  tested (checkboxes, 16 options + Other), Overall rating (1-5 linear scale),
  Bugs encountered (paragraph), What enjoyed most (paragraph), Suggestions
  for improvement (paragraph), Additional comments (paragraph).
  Form URL: https://docs.google.com/forms/d/1eefNpvpHW6dEfcLv4hu2XpeNgOsY27avnfmcHZ-1oaI/viewform
  Generated amber-colored QR code (450x450px) linking to the form.
  Rebuilt Testing Guide PDF (66,749 bytes) with embedded QR code on page 1.
  Updated to research/reports/Testing_Guide_2026-03-31.pdf.
  Protected files verified: GDD 603,634 bytes, DevPlan 139,726 bytes.

## 2026-03-31 -- Flower Scale Reduction
- Reduced wildflower scatter scale from 0.7-1.1 to 0.32-0.48 (scripts/world/wildflower_scatter.gd)
- Reduced lifecycle manager flower scale from 0.42-0.58 to 0.22-0.30 (scripts/world/flower_lifecycle_manager.gd)
- Flowers were appearing too large relative to the player and environment
- Protected files verified: GDD 603,634 bytes, DevPlan 139,726 bytes.

## 2026-03-31 -- Flower Scale Reduction (Round 2)
- Reduced wildflower scatter scale from 0.32-0.48 to 0.08-0.12 (25% of previous)
- Reduced lifecycle manager flower scale from 0.22-0.30 to 0.055-0.075 (25% of previous)
- Protected files verified: GDD 603,634 bytes, DevPlan 139,726 bytes.

## 2026-03-31 -- Fix: Flowers rendered as TileMap tiles, not Sprite2D
- ROOT CAUSE: Flowers were baked as 32x32 TileMap tiles on Layer 3 (flowers_forage),
  rendered at full tile size regardless of Sprite2D scale changes.
- FIX: FlowerLifecycleManager now clears TileMap Layer 3 on _ready, since it handles
  all flowers dynamically via Sprite2D nodes.
- Reset Sprite2D scales to reasonable values: lifecycle manager 0.4-0.55, scatter 0.35-0.55
  (roughly half a tile, ~14-18px on screen from 32px textures).
- Protected files verified: GDD 603,634 bytes, DevPlan 139,726 bytes.

## 2026-04-02 -- Fix Workbench Craft Button Overlap
- Craft buttons were overlapping vertically because Godot Button nodes enforce
  a minimum internal height larger than the 10px we set
- Increased ROW_H from 18 to 20, PANEL_H from 162 to 176, PANEL_Y from 6 to 2
  to give recipe rows enough vertical space
- Changed RECIPE_START_Y from 68 to 62 to reclaim space
- Increased button dimensions (BTN_W=40, BTN_H=14) for all buttons
- Added set_deferred("size", ...) after add_child on all buttons to force
  Godot to respect our intended size after layout pass
- Protected files verified: GDD 603,634 bytes, DevPlan 139,726 bytes.

## 2026-04-02 -- Fix County Road Tree Sprites
- Replaced all broken Cainos sprite fragments with complete tree sprites
  across all 30 harvestable trees in county_road.tscn
- Broken fragments (02, 03, 04, 05, 08, 09, 10) were partial canopy pieces
  from incorrectly split sprite sheet -- showed as visual mess in-game
- Reassigned tree species to complete sprites:
  Oak -> cainos_tree_13 (large broad canopy, light bark)
  Walnut -> cainos_tree_12 (tall deciduous, medium canopy)
  Maple -> cainos_tree_01 (bushy dense green, already correct)
  Elm -> cainos_tree_07 (tall slender trunk, already correct)
  Hickory -> cainos_tree_07 (tall slender)
  Pine -> cainos_tree_01 (bushy dense green)
  Ash -> cainos_tree_06 (small-medium with visible trunk)
- Protected files verified: GDD 603,634 bytes, DevPlan 139,726 bytes.

## 2026-03-31 -- County Road Rebuilt with Forest
- Expanded county_road scene from 640x200 to 3200x2400 pixels
- Added dense forest zone (upper 2/3 of scene) with 120+ decorative trees
  using extracted Cainos Village Pack tree sprites + existing oak/pine sprites
- Extracted 18 tree/bush sprites from Cainos TX Village Plant.png spritesheet
  into assets/sprites/environment/cainos_trees/
- Road now runs left-right at y=330-420 with ditches on both sides
- Harmon farm field visible in bottom-left section
- Harvestable trees expanded from 8 to 20, spread throughout the forest
  (14 in deep forest, 6 near the road for easy access)
- Updated ExitHelper to support custom scene bounds via optional Rect2 parameter
  (backward compatible - default bounds match original 1400x500 scenes)
- Updated ExitHelper.position_player_from_spawn_side() to use bounds-relative
  positioning with 80px margin
- Background scenery: grain elevator silhouette, water tower (distant right)
- 60 undergrowth bushes from Cainos bush sprites for forest floor detail
- All GDScript ASCII-verified, no non-ASCII characters
- Protected files verified: GDD 603,634 bytes, DevPlan 139,726 bytes.
