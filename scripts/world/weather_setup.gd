## weather_setup.gd -- Kept for reference but no longer used directly.
## Each exterior scene now has its own _setup_weather() function instead.
## This avoids class_name resolution issues during Godot scene parsing.
extends RefCounted
